# 测试交接文档

> 测试人员：王健琨 | 测试周期：2026-09-08 ~ 2026-09-09


## 📊 测试结果总览

| PR   | 标题                              | 结果   |
| ---- | --------------------------------- | ------ |
| #6   | C0 固定步长时钟与 Canvas 边界骨架 | ✅ 通过 |
| #7   | feat(replay): r0 工程与契约门禁   | ✅ 通过 |
| #8   | 第一关地图及部分机关设置          | ✅ 通过 |
| #10  | c1 框架与 JavaFX 窗口             | ✅ 通过 |


## 📋 标准化测试步骤（直接照着做）

### 步骤 1：拉取 PR 代码

```bash
git fetch origin pull/[PR编号]/head:pr-[PR编号]
git checkout pr-[PR编号]
```


### 步骤 2：查看变更文件

```bash
git diff main --name-status
```


### 步骤 3：运行自动化测试

```bash
.\mvnw.cmd test
```

**通过标准**：`BUILD SUCCESS`，`Tests run: N, Failures: 0`


### 步骤 4：手工验收

- **无 GUI 的 PR**（#6/#7/#8）：运行 `java -jar` 报 `没有主清单属性` 属正常，不阻塞合并
- **有 GUI 的 PR**（#10）：运行 `.\mvnw.cmd javafx:run`，确认窗口正常弹出


### 步骤 5：获取提交哈希

```bash
git log --oneline -1
```


### 步骤 6：发布测试评论

在 PR 评论区粘贴测试结论模板，点击 Comment。


### 步骤 7：提交任务卡

```bash
git add docs/testing/TEST-PR[编号]-任务卡.md
git commit -m "docs(test): 添加 PR [编号] 测试任务卡"
git checkout -b test/pr-[编号]
git push origin test/pr-[编号]
```


### 步骤 8：综合归档

将各 PR 记录汇总到 `TEST-SUMMARY-PR6-PR10.md`，提交到 `test/summary-pr6-pr10`。


## ⚠️ 常见问题

| 问题                                   | 解决方案                             |
| -------------------------------------- | ------------------------------------ |
| `mvnw.cmd not recognized`              | 确保在项目根目录 `cd time-trace-lab` |
| `Could not resolve host: github.com`   | 换手机热点 / `ipconfig /flushdns`    |
| `refusing to fetch into branch`        | 先 `git checkout main` 再 fetch      |
| `refspec test/pr-N does not match any` | `git checkout -b test/pr-N` 再 push  |
| `没有主清单属性`                       | 用 `mvn javafx:run` 代替 `java -jar` |


## ✅ 已交付物

| 文件                       | 分支                    |
| -------------------------- | ----------------------- |
| `TEST-PR6-任务卡.md`       | `test/pr-6`             |
| `TEST-PR7-任务卡.md`       | `test/pr-7`             |
| `TEST-PR8-任务卡.md`       | `test/pr-8`             |
| `TEST-PR10-任务卡.md`      | `test/pr-10`            |
| `TEST-SUMMARY-PR6-PR10.md` | `test/summary-pr6-pr10` |


## 📌 待后续跟进

1. **集成测试**：待所有 PR 合并后，统一做端到端测试
2. **JAR 包配置**：PR #10 的 `pom.xml` 需添加 `maven-jar-plugin` 的 `mainClass`
3. **PR #8 补充测试**：数据模型类后续补充单元测试

*本交接文档已完整覆盖所有测试操作步骤，后续接手人员可直接按步骤执行。*