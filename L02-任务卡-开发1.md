# 任务卡 · 第二关「双残影可读性 + 门房视觉」（开发一）

> 任务 ID：**L02-A-DEV1**（+ L2-B 的射线渲染）
> 主责：**开发一**（`core/**`、`entity/**`、`render/**` + 其测试）｜ 依据：`L02-门房与双残影-PM裁决.md`
> 基线：`origin/develop` = **`f8f414c`**（清洁复跑 410 / 0 / 0，无并发实跑）
> 状态：**L2-A 开工**；L2-B 等 L2-A 合入
> 日期：2026-09-14

## 一、目标（L2-A）

L2 的画面要能回答一个问题：**「哪一代残影在压哪块板？」**——这是本轮教学的核心，画不出来玩家就学不会。

## 二、交付物（L2-A）

1. **两条残影轨迹可区分**（README §三）：较新残影实线、较旧残影虚线；两条重叠时横向错开 2–4 像素；
   按 `H` 增强相关路径。现状 `EchoTrailLayer` 已按 `sourceRound` 取色，请**确认并补强**：
   - `EchoTrail` 需要能表达**代际**（若 `RenderViews.EchoTrail` 缺少"是第几代"的语义，按 PM 批准的方式**新增字段**——
     这是**新增**不是复用，需在卡内写明并同步 `app` 侧构造点，PM 会改 `app/**`）；
   - 测试：两条轨迹同刻重叠时**像素/结构可区分**（沿用你既有的离屏回归范式，别断言具体颜色值）。
2. **门房视觉**：`MechanismKind.DOOR` 的绘制分支**这次真正用到**（第二关有两个门）：
   - 房门「关闭」必须有实体感（挡住入口）；「打开」要有明确通行暗示；
   - 注意**与 L1 的一致性**：L1 的闸门终点用 `EXIT` 的 `active` 表达，L2 的房门是独立 `DOOR` 条目 →
     两者外观需能区分「门」与「闸门终点」，但风格统一；
   - 测试：`DOOR` 开/关两态渲染差异。
3. **压力板视觉**：三块 `dock_plate` 在 L2 都是**普通驻留板**（不是开关），沿用 `PLATE`；
   确认「占用/空闲」两态在**多块板同时在场**时仍清晰（不要只靠位置区分）。
4. **HUD 代际标签**（与开发三协作）：HUD 需显示 `/E1 / E2` 一类代际标识（README §三）；
   `ui/**` 的字段改动由开发三做，你负责**渲染层如何画出代际标识**（如果 HUD 由 `SharedHud` 绘制，按现有分工处理，
   需要跨 `ui/**` 时先与开发三对齐，不要各自改同一个类）。

## 三、交付物（L2-B，等 L2-A 合入）

1. **射线渲染**：预警带（周期启停的可见提示）+ 射线本体；命中时的减速反馈要**不引起"受伤"误解**
   （README §三：命中只写时滞，不死亡、不失败）；
2. **相位下潜状态**：`PHASED` 期间角色外观需明确变化（俯身/半透明等），且**不改变**速度与路径；
3. 测试：射线三态（充电/激活/冷却）可区分；`PHASED` 与非 `PHASED` 外观可区分；离屏回归即可，不要依赖具体像素。

## 四、允许 / 禁止路径

**允许**：`src/main/java/org/example/timeloop/{core,entity,render}/**`、
`src/test/java/org/example/timeloop/{core,entity,render}/**`、`docs/development/开发1/**`。

**禁止**：`app/**`（PM）、`mechanism/**`、`level/**`、`ui/**`（除 §二.4 明确协作项）、`replay/**`、`snapshot/**`、
`pom.xml`、`README.md`；**不得**改 `RenderViews.Mechanism` 记录结构（要改先提小提案）。

## 五、验收

1. 两条残影轨迹在同刻重叠时可区分（截图 + 离屏回归）；
2. 门开/关、板占用/空闲、射线三态、相位两态全部可辨认；
3. `.\mvnw.cmd -o clean test` 退出码 0、**任何单测稳定 ≤ 1 s**、数字回填（基线 410）；
4. `git diff --check` 干净。

## 六、证据与回传

```
branch: codex/l02-a-dev1
SHA:    <40 位>
代际表达: <RenderViews.EchoTrail 是否新增字段；若是，给出新签名>
截图:   双残影重叠 / 门关 / 门开（可选，PM 会补目标机截图）
clean test: ? / ? / ? / ? ；最慢单测 <名称> <秒>
```

**交付以远端提交为唯一凭据**；禁止 `git add -A`，精确白名单提交。

## 七、停止条件

1. 需要改 `RenderViews.Mechanism` 记录结构 → 停手回报 PM（走小提案，不要自行扩面）；
2. 需要改 `app/**` 的构造点才能表达代际 → 停手回报，PM 在同一批里改（**不要**在开发一分支里改 `app`）；
3. 单测稳定 > 1 s 且无法解释 → 停手回报。
