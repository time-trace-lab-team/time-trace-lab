# L02 门房 + 双残影 · PM 裁决（README 对齐）

> 任务 ID：**L02-GATE-PLATE-ECHO2** ｜ 提出：**项目方** ｜ 裁决：**PM（架构与集成）**
> 基线：`origin/develop` = **`f8f414c`**（clean 复跑 **410 / 0 / 0**）
> 状态：**已裁决，拆卡见 §八**
> 日期：2026-09-14

## 一、README 对齐分析（先对齐，再发卡）

项目方的第二关设计：**门 + 压力板（驻留板）开门 + 首次使用多残影**，三轮成链（第一轮留下开门残影、
第二轮借残影开门进房踩内板、第三轮两条残影各压一块板 → 玩家按 `E` 通关）。与 README 现状逐条比对：

| # | README 现状 | 新设计需要 | 处置 |
| --- | --- | --- | --- |
| 1 | §六 第二关 = 「低姿穿行」：**一束低风险射线 + `Space` 相位下潜**，`16 s / L=1 / maxRounds=3` | 门 + 三块压力板 + **双残影**，20 s 级轮长 | **§六 L2 行改写**：保留射线教学段，追加门房链；参数 `20 s / L=2 / maxRounds=4` |
| 2 | §五 驻留板「使用关卡」= 第一、三关（+四、五中继）；普通门 = 第一（合并为闸门终点）、三、四、五关 | 第二关要用 3 块驻留板 + 2 个门 | **§五 两行加「第二关」**（门/板都在 §五 内 → **不触及 §七 范围冻结**，无新机关类型） |
| 3 | §十六 #11「**第二关**使用 `Space` 相位下潜而非转向绕路；整关正式解法最多两次相位输入」 | 若 L2 去掉射线，此条须重写 | **不改**（见 §二裁决：L2 保留射线走廊）→ **不需要范围变更签署** |
| 4 | §十三 时间线：第 2 天「相位下潜原型」、第 3 天「完成第一、二关」 | L2 变成两段式（射线 + 门房链） | **§十三 微调**：第 2 天保留相位原型；第 3 天改为「L2-A 门房链竖切片」，射线走廊（L2-B）排到第 4 天前段 |
| 5 | §十六 #4「单残影先验收，随后至少 2 个不同代际残影同步回放」 | L2 就要双残影 | **不改**，且**提前达成**（L2 即验证双残影） |
| 6 | §九 轮长「默认 20 s，可调 12–25 s」 | L2 需要长驻留 | **采用 20 s 默认值**（L1 仍 16 s），实测定稿 |

**结论**：只有 2 处 README 正文需要改（§六 L2 行、§五 两行 + §十三 时间线），**没有范围变更**，
不需要重写 §十六 #11，也不需要新增机关类型。

## 二、裁决 1：L2 = 「短射线走廊（**保留**）+ 门房双残影链（**新增**）」

- **保留 README §六 的射线教学目标**：L2 仍有一段**一束低风险射线**的走廊，玩家用 `Space` 相位下潜通过；
  这样 §十六 #11 无需重写（避免一次范围变更审批），且射线在第五关高压使用前先被教过。
- **新增门房链**：门 + 三块压力板 + 双残影，作为本轮主教学目标。
- 射线段**短**（一段走廊、一束射线），门房段是主体；轮长 20 s 保证两段都做得完。
- 若项目方希望 L2 完全不含射线：可以，但**§十六 #11 必须重写为「第三关…」**（射线教学后移），
  这属于范围变更，需项目方一句话确认；**PM 默认按「保留」执行**，卡已按此发。

## 三、裁决 2：判定链与官方解（三轮）

**三块驻留板**（全部 `dock_plate` + `autoDock=true`）与**两个门实例**：

| 机关 | ID（建议） | 判定 (`requiredPlateIds`) | 作用 |
| --- | --- | --- | --- |
| 房门 | `L02_door_room` | `{L02_plate_door}`（门外板） | **锁着时挡住房间入口节点**；板一离开即回锁（与 README §三 一致） |
| 终点闸门 | `L02_door_exit`（**与出口同格**，同 L1） | `{L02_plate_inner, L02_plate_main}`（内板 + 主驻留板） | 两块板**同时**被占才解锁；解锁后玩家在闸门旁按 `E` 结算 |
| 门外板 | `L02_plate_door` | — | 开门用；占即开、离即关（**不锁存**） |
| 内板 | `L02_plate_inner` | — | 房间内 |
| 主驻留板 | `L02_plate_main` | — | 房间外主任务板 |

**官方解（三轮成链，作为测试基准）**：

```
R1：玩家踩门外板 → 房门开 → 借窗口进房踩内板（短驻留）→ 出房 → 停在主驻留板到轮末
    → E1 = [门外板驻留 + 进房内板 + 主驻留板]
R2：E1 复现：先压门外板（开门窗口）→ 玩家借窗口进房踩内板并驻留到轮末（本轮不再出来）
    → E2 = [借窗口进房 + 内板驻留]
R3：E1 压门外板（开门窗口）+ 稍后压主驻留板；E2 借窗口进房压内板
    → 内板 + 主驻留板同时被占 → 终点闸门解锁 → 玩家走到闸门旁按 E 通关
```

**必须成文的硬约束**（开发三给证据）：

1. 房门节点**不是**「出生点 → 门外板 / 主驻留板 / 出口」的唯一通路（锁着时不得堵死其它路线）；
2. 终点闸门与**内板**的距离 > 1.5 × tileSize（否则玩家站在内板上就能按 `E`，双残影链失去意义）；
3. 房门到内板的通路长度必须**短于**门外板的驻留窗口（否则 R2 玩家来不及进房 → 无解）；
4. 内板与主驻留板都必须能在**不经过房门**的情况下到达（R1 的路线成立）；
5. 射线束不得覆盖压力板驻留点（否则驻留期间会被反复命中减速 → 轮长不可控）。

## 四、裁决 3：关卡参数（README §六 / §九 对齐）

| 参数 | 值 | 理由 |
| --- | --- | --- |
| `durationTicks` | **1200**（20 s × 60） | README §九 默认 20 s；L2 有两个教学目标 + 长驻留，16 s 不够；允许后续按 §九 在 12–25 s 内实测调整 |
| `maxRounds` | **4** | 官方解需要 3 轮成链 → 留 1 轮容错（与 L1「2 轮解 / 3 轮容错」同构，也与 L3–L5 的 4 轮一致） |
| `echoLifeL` | **2** | 首次多残影；`EchoQueue.CAPACITY = 2` 已是硬上限，**不扩容** |
| 相位输入上限 | ≤ 2 次 | §十六 #11 保持 |

> 注意：`maxRounds = 4` 只给容错，**官方通关仍在第 3 轮**；第 4 轮若首两轮残影代际变化（`E1` 消散），
> 属"补救轮"，不要求可解，但**不得崩溃或残留旧状态**。

## 五、裁决 4：分两阶段交付（控制单 PR 体积）

| 阶段 | 内容 | 依赖 |
| --- | --- | --- |
| **L2-A** | 门房链：3 板 + 2 门 + 双残影回放 + 关卡数据 + 装配/选关 + 端到端 | 本批卡 |
| **L2-B** | 短射线走廊 + 相位下潜段 + 射线渲染/预警 | L2-A 合入后 |

L2-B 完成才算 README §六 L2 行兑现；但 L2-A 合入即可开始人工试玩门房链（射线段先留空走廊）。

## 六、裁决 5：`mechanism/ray/Ray.java` **不再删除**（修订 BUG-002 P2）

- L2-B 需要真实的射线机制，`Ray.java` 从「零引用死代码」变成「有待接线需求」；
  → **`BUG-002-LIFECYCLE-Phase2-任务卡-开发三.md` 中「删除 Ray.java」一条作废**，改为
  「L2-B 按注入端口接线（`GameEventBus` + 只读 `roundTick`），不得使用兼容单例」。
- **R5-B §10.2 的端口结论仍然成立**：射线**不需要快照端口**（无持久状态，行为由共享 `roundTick` 决定）
  → 聚合器不得为其加具体类旁路；若实现中发现需要状态位，必须走新提案。
- 相位下潜（`PHASED`）与减速（`MovementState.SLOWED`）已在 `core/entity` 内，**不需要新机制类型**。

## 七、公共接口影响：**零新增类型、零契约变更**

已核（`develop @ f8f414c`）：

- `LevelData` 已支持**任意数量** `EntitySpawnInfo`（板/出口）与 `List<DoorInfo>`（多门）、
  以及每关 `durationTicks / maxRounds / echoLifeL` → **L2 不需要改数据模型**；
- `Door` 天然支持多实例（每个自带 `requiredPlateIds`），`ExitTerminal` 只需挂到 `door_exit` 的 ID；
- `EchoQueue` 已有 `CAPACITY = 2`、寿命淘汰 + 容量淘汰、`activeEchoes(round)` → **L=2 是纯参数**；
- 需要新代码的只有：**关卡数据（L2）**、**app 装配泛化与选关**、**多门阻挡**（`isPassable` 现在只查一个门）、
  **双残影渲染区分**、**射线段（L2-B）**。

因此本批**不涉及任何公共契约冻结变更**（除 §六 对 BUG-002 P2 的一项修订）。

## 八、拆卡（单板块）

1. `L02-任务卡-开发3.md` —— 机制/关卡/UI：3 板 + 2 门 + L2 关卡数据 + 几何与可解性 + HUD 代际文案；
2. `L02-任务卡-开发2.md` —— 回放/快照：**L=2 双残影首次真跑**、轮末入队/淘汰、事件归属、快照聚合覆盖 3 板 2 门；
3. `L02-任务卡-开发1.md` —— core/entity/render：**两条残影轨迹的区分**、门/板/房间视觉、射线预警与相位态（L2-B）；
4. `L02-任务卡-PM接入.md` —— app：装配泛化（`Level01Assembly` → 可切换关卡）、最小选关入口、多门阻挡、
   L2 端到端测试、README 同步。

## 九、验收（整批）

1. headless 三轮链模拟通过（开发三）+ app 端到端三轮通关（PM）；
2. **双残影同刻回放确定性**：同输入脚本重复跑，两条轨迹逐刻一致（开发二）；
3. 人工试玩：能口述「为什么要留两条残影」，且房门开/关、三块板占用、闸门开/关可辨认；
4. `.\mvnw.cmd -o clean test` 退出码 0、**任何单测稳定 ≤ 1 s**（测量时不得与 JavaFX 实跑并行）、数字回填；
5. README §五/§六/§十三/§十六 与本裁决一致；`git diff --check` 干净。

## 十、不做 / 边界

- **不扩容** `EchoQueue.CAPACITY`（仍为 2）；不引入第三残影；
- **不新增机关类型与事件类型**（§五 表外无新机关，§七 冻结不破）；
- 不做存档、设置、关卡进度落盘（§十六 #6 属独立批次）；
- 不改 L1 的既有行为与参数；
- 阶段顺序不可倒：**L2-A 未合入前不做 L2-B**（否则射线段无宿主关卡）。

## 十一、追加裁决 1：房门**不能单人穿过** → 官方解改为「R1 不进房」

> 触发来源：开发三按 `L02-任务卡-开发3.md` §七-2 停手回报（2026-09-14）。**PM 复核：其证明成立，本追加裁决取代 §三 的官方解与 §三 的约束 1/3/4。**

### 11.1 事实复核（三条全部成立，均以当前代码为准）

| # | 事实 | 证据 |
| --- | --- | --- |
| 1 | 门未解锁时**挡门自己那一格** | `app/Level01Assembly.isPassable()`：`if (door.isUnlocked()) return true; return !samePosition(target.center(), door.getPosition())` |
| 2 | 门是**实时门**，板一离开立刻回锁 | `mechanism/Door.updateDoorState()`：`allOccupied ? UNLOCKED : LOCKED`，每次板进/出**同刻重算** |
| 3 | 驻留板离开**同刻**释放占用 | 开发三规格 §5.2（合法出口方向按下即同刻离开并释放） |

**推论**：踩板开门的人**一离开板，门在同刻回锁** → **任何单人都不可能穿过板控门**（连相邻格也不行：释放与回锁在同一刻，先于跨格移动的通行判定）。

→ **PM 在 §三 写的 R1「踩门外板 → 借窗口进房踩内板 → 出房」自相矛盾**（那是单人动作序列）。此错由 PM 承担并改正。

### 11.2 裁决：**按 B 的零契约路线；但房间不给旁路**

- **驳回 A**（改门的阻挡语义／让门只挡一侧）：会动 `Door` 与 app 的通行契约，并改变 **L1 已验证**的「闸门锁着挡自己那一格」语义 → 不采纳；
- **不采纳 B 的「长通路绕行」**：若房间永远有旁路，**门就从必要条件退化成可选装饰**，本关的教学点随之消失；
- **采纳**：**房间不设旁路，房门是唯一入口**；同时 **R1 根本不进房** —— 这正好回到项目方原始设计
  「第一轮玩家先触发门的驻留板并驻留足够长的时间，让第二轮的玩家能借第一轮残影进入房间」。

### 11.3 修正后的官方解（三轮，取代 §三）

```
R1：玩家踩【门外板】并【驻留足够久】（造窗口）→ 离开 → 走到【主驻留板】停到轮末   （全程不进房）
    → E1 = [门外板窗口 + 主驻留板]
R2：E1 复现门外板窗口 → 玩家在窗口内穿门进房 → 踩【内板】并驻留到轮末（进房即承诺）
    → E2 = [进房 + 内板驻留]
R3：E1 = [门外板窗口 + 主驻留板]；E2 = [进房 + 内板] → 内板 + 主驻留板同刻被占 → 终点闸门解锁
    → 玩家（本轮不需要进房）走到闸门旁按 E 通关
```

**为什么窗口天然对齐**：`E1` 与 `E2` 都从 `roundTick = 0` 同时开始回放，而 **`E2` 的录制本来就是在 `E1` 的窗口内完成的**，
故 R3 中 `E2` 的进房时刻必然落在 `E1` 的窗口内——**时间环自洽，不需要任何额外同步机制**。
解锁后两块板都保持到轮末，玩家有整个后半轮去按 `E`。

### 11.4 约束修订（**以本节为准**）

| 原约束 | 处置 |
| --- | --- |
| 1「房门节点非割点」 | **精确化**：锁门**只挡房间** —— 删除房门节点后 `spawn→门外板`、`spawn→主驻留板`、`spawn→闸门` **仍连通**（房间内部不要求连通） |
| 2「闸门与内板距离 > 72」 | **保留**（防站在内板上直接按 `E`） |
| 3「房门→内板通路 < 门外板驻留窗口」 | **精确化**：**窗口时长 `(b − a) ≥ 房门内侧邻点 → 内板 的走行刻 + 安全余量**（R2 玩家可提前在门外等待，故只受窗口**长度**约束；建议余量 ≥ 0.5 s） |
| 4「内板与主驻留板都能不经房门到达」 | **作废**（它服务于 PM 写错的 R1）。替代：**房间不设旁路**，门是唯一入口 |
| 5「射线不覆盖驻留点」 | **保留**（L2-B） |
| **新增 6** | **进房即承诺**：R2 玩家进房后到轮末**无法离开**（门已回锁）→ HUD 必须提示「进房后需驻留到本轮结束」，门口要有明显提示；且**不允许**把房间做成"能误入但无事可做"的死路 |
| **新增 7** | **门外板与主驻留板不得相邻**（否则一步就能从一块踩到另一块，"两个任务"的教学被模糊）；建议间隔 ≥ 3 格 |

### 11.5 连带影响

- `L02-任务卡-开发3.md` 的官方解、约束清单、测试用例名已按本节同步；
- **不需要任何契约变更**（裁决 §七「零新增类型、零契约变更」继续成立）；
- 开发三要求先出「轮次 × 格数 × 刻」时间表：**批准**，作为关卡数据的前置证据（用它验证 `(b − a)` 与走行刻）。

