# 任务卡 · 自由移动收口 v3（开发一）

> 任务 ID：**X-MOVE-COLLAPSE-01-DEV1-v3**（替代 v2 卡；v2-A/B/C 你已完成，本卡只剩「交付落盘 + 配合接线 + 画面取证」）
> 板块：**开发一**（`render/**`、`entity/**` 测试、`docs/development/开发1/**`）
> 状态：**已发卡，可开工**
> 当前基线：`origin/develop` = `6dc5cad`（PM 实测 **313/0/0**）；你的 v2 分支基线 `ed5bcac` **已落后**
> PM 侧在途：**PR #57**（APP-2 + ECHO-ACTOR E-1，`316/0/0`，clean）
> 日期：2026-09-11

## 一、最高优先：把 v2 分支推上来（第三次同一件事）

`codex/dev1-move-v2` **不在 origin**（`git ls-remote --heads origin` 只看到 `dev1-c3`、`dev1-next-module`）。
在你 push 之前，PM **无法**复核 303/0/0，也**无法**执行 R-2 的 app 接线（缺 `RenderViews.PathNodeMarker`
与 `PathNodeHintLayer` 类型，app 编译不过）。

```powershell
git fetch origin
git merge origin/develop          # 你的 ed5bcac 落后 develop（含 #55/#56/#51–#53）
git push -u origin codex/dev1-move-v2
```

## 二、数字自证

你报的新用例 5（v2-A）+ 4（v2-B）+ 3（v2-C）= 12 条，而 `ed5bcac` 实测 **288/0/0** → 288 + 12 = **300**，
与你报告的全量 **303** 差 **3** 条。请在交付文档里列出**测试类 → 条数**清单，说明差额来源（很可能是 PM 漏算）。

## 三、配合 PM 接线（push 后你待命，不需要你改 `app/**`）

PM 会做两处（父卡 §13.3）：

1. `Level01Assembly` 构造期把 `levelData.getPathNodes()` 投影成 `List<RenderViews.PathNodeMarker>` 并暴露只读访问器；
2. `TimeTraceLabApplication` 注册 `new PathNodeHintLayer(frames, assembly.pathNodeMarkers(), 48.0, transform)`。

你的职责：**若 PM 接线遇到 API 不匹配（构造参数顺序、record 组件名、可见性），10 分钟内修 `render/**` 侧签名**，
不要顺手改 app。

## 四、补一条渲染矩阵测试（可读性门禁前置）

`PlayerVisualProjection` 已有 4 条测试；请补 **`movementState × phased` 组合矩阵**（`IDLE/CRUISING/SLOWED/DOCKED` ×
`phased true/false` 共 8 组），断言每组都有明确投影、**没有任何组合落入 default 分支或抛异常**。
理由：你已显式为 `DOCKED` 兜底，说明存在 default 分支；矩阵测试能防止后续新增状态时静默退化
（README §十六 门禁第 4 条依赖三态可辨识）。

## 五、人工画面取证（与 PM/测试任一方协作）

窗口跑起来后出四张图：**IDLE / CRUISING / SLOWED** 三态各一张 + 「接近节点：菱形 远(隐藏)→中(暗)→近(亮)」一张，
交给 PM 登记为 `FINAL-ACCEPT` §A 第 4 条的取证材料。若你无法起窗口，请给出启动命令与预期现象，由 PM/测试执行。

## 六、允许 / 禁止

- 允许：`render/**`、`src/test/java/org/example/timeloop/{render,entity}/**`、`docs/development/开发1/**`。
- 禁止：`app/**`（PM）、`core/**`（除已交付项外）、`mechanism/**`、`level/**`、`replay/**`、`snapshot/**`、
  `ui/**`、`pom.xml`、`src/main/resources/**`。

## 七、交付

push 后的分支名与 tip 哈希 + `Tests run/Failures/Errors` + 测试类清单 + 四张画面证据（或启动命令），
写进 `docs/development/开发1/X-MOVE-COLLAPSE-01-v3-交付.md`。
