# 任务卡 · 自由移动收口（开发一）

> 任务 ID：**X-MOVE-COLLAPSE-01-DEV1**（父卡 `跨模块契约卡-四方向移动收口-X-MOVE-COLLAPSE-01.md`）
> 所属板块：**开发一**（`core/**`、`entity/**` + 对应测试）
> 状态：**可开工**（`develop` = `202e7ad`，C-PLAYER-MOVE-02 已合入且 PM 轮转接线已提交，全量 286 条 0 失败）
> 目标：把「角色自由移动」在 core/entity 侧彻底收口，**每步都能单独跑出全量绿**，不再有靠 app 兜底的语义。
> 提交顺序与边界已由 PM 批准（父卡 §六）。下游：开发三 `ENT-2a`、PM 的 `APP-1` 与 app 轮转接线。

---

## 〇、PM 授权答复（2026-09-10，逐条对应开发一的 6 个问题）

| # | 问题 | PM 裁决 |
| --- | --- | --- |
| 1 | 是否采用任务 ID `CORE-1` 并挂接 `P-MOVE-ALIGN` | **采用**。权威卡号 = `X-MOVE-COLLAPSE-01`，子项编号 = `CORE-1/2/3`、`ENT-1/2/3`；`P-MOVE-ALIGN` 是开发三那张关卡卡的 ID，**允许写成别名** `P-MOVE-ALIGN/CORE-1`（便于与开发二/三的交接文档互相检索），但**规格一律以 `X-MOVE-COLLAPSE-01` 为准**，两者冲突时以本卡为准 |
| 2 | 是否批准 `Optional<Direction> direction()`、`Set<Direction> heldDirections()` | **批准**（精确签名见 §一 第 1 项）。不得改成 `Direction`/`List<Direction>`，不得加第三个方法 |
| 3 | 非方向键统一 `Optional.empty()`、held 投影不可变 | **确认**：`INTERACT`/`PHASE` → `Optional.empty()`；`heldDirections()` 返回不可变集合，空集用 `Set.of()` |
| 4 | 本卡是否不修改 `InputAccumulator`/`Level01Assembly` 及其他调用方 | **确认：本卡只加方法，不动任何调用方**。旧映射暂时保留是预期的；PM 会在 CORE-1 合并后**成对**提交 app 侧改造（`InputAccumulator.toDirection` → `LogicalKey.direction()`；`Level01Assembly.heldDirections` → `InputIntent.heldDirections()`）。因此 `grep "case DIR_UP"` 收敛**不是**你的验收项 |
| 5 | 在当前分支继续，还是用新的 `codex/` 分支 | **允许在当前分支继续**，前提：base 是 `origin/develop`（现 `202e7ad`）且**一个提交只做一件事**。若当前分支 base 更旧或夹带其他改动，改为从 `origin/develop` 切 `feature/player-move-align`。分支名沿用你已批准的 `feature/player-move-align`；提交/PR 标题写 `X-MOVE-COLLAPSE-01 / CORE-1 ...` |
| 6 | 本卡完成后再单独拆 CORE-2/3、ENT-1/2；本卡不顺手做 | **确认**：一张卡一个子项，**不得顺手**。顺序仍为 CORE-1 → CORE-2 → ENT-1 → ENT-3 → CORE-3（与 PM 的 `APP-1` 配对）→ ENT-2b（等开发三 `tryLeave`） |

> 开发一可在收到本条答复后立即恢复编码。

---

## 一、任务清单（按此顺序分提交，每步跑全量）

### 1. CORE-1 · 四方向输入契约（新增，非破坏）

**PM 已批准的精确契约（2026-09-10，照此实现，不要扩签名）**

```java
// core/input/LogicalKey
/** 方向键 → core.Direction；非方向键（INTERACT / PHASE）返回 Optional.empty()。 */
public Optional<Direction> direction();

// core/input/InputIntent
/** 本刻刻末仍按住的方向（只投影 4 个 DIR_*）；返回不可变集合，空集为 Set.of()。 */
public Set<Direction> heldDirections();
```

- **返回类型已批准**：`Optional<Direction>` 与 `Set<Direction>`，不要改成 `Direction`（非方向键无值）或 `List<Direction>`（`held` 无序；顺序语义由既有 `directionEdges()`/`lastDirectionEdge()` 承担）。
- **空值/不可变语义已确认**：非方向键统一 `Optional.empty()`；`heldDirections()` 返回不可变集合（`Set.copyOf`/`Collections.unmodifiableSet` 皆可），**不得**返回内部可变实例、不得每次返回同一个可变实例。
- **验收（在开发一分支上）**：两个新方法的测试通过 + 全量 `.\mvnw.cmd -o test` 绿 + `InputIntentTest` 补
  「按住前进+垂直」「同刻相反」两组 `heldDirections()` 断言 + `LogicalKeyTest`（若无则新建）断言
  `INTERACT`/`PHASE` → `Optional.empty()`、四个 `DIR_*` → 对应方向。
- **注意：`grep -rn "case DIR_UP" src/main` 收敛到零不是你的验收项**——那需要改 `app/InputAccumulator`
  与 `app/Level01Assembly`，属 PM 的配对提交；你的分支上保留旧映射是**预期且允许**的。
- **本卡不修改任何调用方**（见 §〇 第 4 条）。
- 全量：✅ 绿。

### 2. CORE-2 · 速度参数冻结 + 减速端口（1 处破坏性）

- 删除 `entity/PatrolConfig.turnLockDistance`（`PatrolController` 已零引用，只剩 `PatrolConfigTest` 断言 7.2），
  同步更新 `c2Greybox()` 与测试；冻结 `tileSize=48`、`baseSpeed=2`、`epsilon=4.8e-5` 为唯一速度来源。
- 新增 `entity/SpeedModifierPort`（本刻乘子，`1.0` = 正常）；`PatrolController` 按 `baseSpeed × 乘子` 推进，
  乘子 < 1 时输出 `MovementState.SLOWED`。
- **射线的写入端不在本卡**（`mechanism/ray` 尚未实现，且属开发三路径）：本卡只做「端口 + 移动层消费 + 用假端口写的单测」。
- 验收：`grep -rn "turnLockDistance" src/main` 为空；新增测试「乘子 0.5 → 每刻位移 1.0 + `SLOWED`」「恢复 → `CRUISING`」。
- 全量：✅ 绿（`app` 只用 `PatrolConfig.c2Greybox()`，不受影响）。

### 3. ENT-1 · 单槽方向意图归位（`advance` 签名不变）

- 把 PM 在 `app/Level01Assembly.pendingTurn` 实现的状态移进 `PatrolController`：
  **本刻新按下的 90° 方向在按住期间保留，到达节点中心时提交转向**。
- 三条作废规则必须与 app 现有实现一致，并在 javadoc 写明：
  ① 松手（本刻 held 不含该方向）作废；② 提交成功（`direction == 意图`）作废；③ 意图方向非 90°（等于当前朝向或反方向）不参与提交。
- 现状证据：一格的行程是 24 刻，若 `newestEdge` 只在本刻有效，转向窗口只有 1 刻（16ms），等于不可玩；
  你自己的 `PatrolControllerHeldDirectionTest#holdingDirectionAndSide_thenEdgeCommittedAtNode`
  连续两刻都传 `Optional.of(RIGHT)`，说明设计意图就是保留到节点。
- 验收：新增「按住自动保留直到节点」「松手后不再转向」「反方向意图不提交」三条测试；PM 随后成对删除 `app.pendingTurn`。
- 全量：✅ 绿。

### 4. ENT-3 · 轮初复位 API（**新增，PM 急需**）

- 现状：`PatrolController` 只有 `position()/direction()/config()`，**没有任何复位入口**；
  而 `RecordingSession.completeNormalRound` 结束时时钟停在 `READY`（`clock.transition(GamePhase.READY)`），
  下一轮开始后玩家仍停在上轮位置（甚至停在门前死路或驻留板上）→ 试玩会直接表现为「自由移动坏了」。
- 需求：提供 `resetTo(String startNodeId, Direction initialDirection)`（或等价的 `reset()` 回到构造时的起点/朝向），
  语义：① 位置 = 起点节点中心；② 朝向 = 初始朝向；③ **不清空残影、不写帧、不发事件**（那是 `replay`/`app` 的事）；
  ④ 复位后必须能立即正常推进（内部 segment 与单槽意图一并清空）。
- 验收：新增测试「走若干刻 → `resetTo` → 位置/朝向恢复 → 再推进位移正确」「复位清空未提交的方向意图」。
- 全量：✅ 绿。
- 说明：PM 会在 app 的轮初调用它；请把方法签名与异常条件写进交付文档。

### 5. CORE-3 · 自动选路清零（破坏性，**预期 app 编译失败**）

- 删除 `core/path/PathExitDecision.java`、`PathExitSelector.select(...)`、core `PathNode.defaultExit`（含构造校验），
  **保留 `isPassable(...)`**（真死路豁免依赖它）。
- 现状证据：`select` 在生产代码零调用（仅 `PathExitSelectorTest`）；`defaultExit` 只被 `select` 与
  `app/PathGraphBridge`（L44-L51）使用。
- **本提交后 `app/PathGraphBridge` 必然编译失败，这是预期的**：push 后立刻通知 PM，
  PM 在同一集成分支补 `APP-1`（bridge 退化为纯几何换算），然后全量必须绿。
- 验收：`grep -rn "defaultExit" src/main/java/org/example/timeloop/core` 为空；`grep -rn "PathExitSelector.select" src` 为空。

### 6. ENT-2b · autoDock 离开刻（**等开发三 ENT-2a，勿先做**）

- 开发三放宽 `AutoDockOccupancyPort.tryLeave` 后（`AutoDockService` 现在仍是
  `if (region.contains(worldPosition)) return NOT_OUTSIDE_REGION;`），`C3DockController` 才在
  **离开边沿成立的同一刻**发 `DOCK_LEFT` 并释放占用。
- 提前做的后果：事件说「已离开」而占用仍被 `NOT_OUTSIDE_REGION` 拒绝 → 门与驻留板双源不一致。
- 状态：**本卡暂不提交**，等 PM 通知 ENT-2a 已进 develop。

---

## 二、允许 / 禁止修改路径

- 允许：`core/**`、`entity/**`、`src/test/java/org/example/timeloop/{core,entity}/**`、`docs/development/开发1/**`。
- 禁止：`app/**`、`level/**`、`mechanism/**`、`replay/**`、`snapshot/**`、`render/**`、`ui/**`、`persistence/**`、
  `audio/**`、`pom.xml`、`src/main/resources/**`。
- **遇到 `app/` 编译失败：停下来交接，不要为了凑全绿去动 app。**

## 三、交付

1. 分支 `feature/player-move-align`（PR 标题带 `X-MOVE-COLLAPSE-01`）；
2. 交付文档 `docs/development/开发1/X-MOVE-COLLAPSE-01-交付.md`：每步的提交哈希、`Tests run` 数字与耗时、
   公共签名变化清单（`turnLockDistance` 删除、`SpeedModifierPort` 新增、`resetTo` 新增）、
   以及「需要 PM 在 app 侧配合的调用点清单」；
3. 公共签名变更（父卡 §三 表）需 provider + consumer + PM 三方确认，**除本卡已列项外不得自行扩签名**。

## 四、验收（本卡）

1. `.\mvnw.cmd -o test` 退出码 0，**任何单测 ≤ 1 秒**（死循环防线），CORE-3 提交除外（该步由 PM 配对后转绿）；
2. `grep -rn "case DIR_UP" src/main` 只剩 `LogicalKey.java`；`turnLockDistance` 为空；core `defaultExit` 为空；
3. `MovementState.SLOWED` 有生产者与测试；
4. `PatrolController.resetTo` 有测试且不产生帧/事件/残影副作用；
5. 交付文档里列出 app 侧待接的三处调用点：`heldDirections()/direction()` 改用、`resetTo` 用于轮初、删除 `pendingTurn`。
