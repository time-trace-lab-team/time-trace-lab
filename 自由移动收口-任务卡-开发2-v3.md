# 任务卡 · 自由移动收口 v3（开发二）

> 任务 ID：**X-MOVE-COLLAPSE-01-DEV2-v3**（替代 v2 卡；R1–R4 与 E-3 契约注记你已交付）
> 板块：**开发二**（`replay/**`、`snapshot/**` + 测试）
> 状态：**已发卡，可开工**
> 当前基线：`origin/develop` = `6dc5cad`（313/0/0）；你的交付在 `origin/codex/dev2-x-move-collapse`
> （`0d28063` R1–R4 + `fe1f0d0` E-3 契约注记，**尚未开 PR**）
> 日期：2026-09-11

## 一、最高优先：开 PR（并先同步 develop）

1. 把 `origin/develop`（`6dc5cad`）并入你的分支，跑一次全量；
2. 开 PR → `develop`，标题 `X-MOVE-COLLAPSE-01 R1-R4 录制/回放闭环 + E-3 回放 actor 契约`；
3. PR 描述写清：**行为改动只有 `RecordingSession`/`RoundClock` 的 javadoc**（轮末停靠契约 = 停在 `READY`、
   由调用方 `transition(PLAYING)`），其余为新增测试；附基线哈希 + `Tests run/Failures/Errors` + 最慢单测耗时。

## 二、E-4（新，来自 PM 实测）：`echoDisappeared` 没有派发点 —— 补最小 replay 侧支持

**证据**：`GameEvent.echoDisappeared(...)` 在 `src/main` 内**零调用**；唯一消费方
`DockingPlate.onEvent(ECHO_DISAPPEARED)`（`mechanism/DockingPlate.java:95-99`）因此是**死代码**。
第一关 `ECHO_LIFE_L = 1` 且轮末会 `plate.reset()`，所以现版本观察不到；**L≥2 的关卡之前必须补**，
否则「残影寿命到期消失」不会释放它占住的驻留板（门被永久顶开 / 该板对后续轮次锁死）。

**分工（PM 裁决）**：`replay/**` **只暴露数据**，派发由 app 负责（保持分层：`replay/**` 不得 import `mechanism/event`）。

要求：

1. 让淘汰可被调用方观察，二选一（你定，写进契约）：
   - `EchoQueue.addOnRoundEnd(...)` 返回**被淘汰的 `EchoState` 列表**（`void` → `List<EchoState>` 对既有调用方源码兼容）；
   - 或提供 `List<EchoState> evictionsOnLastRoundEnd()` / `evictedSince(...)` 之类只读访问器 + 清空语义。
2. 契约写明：**淘汰 tick = 轮末边界刻**（即刚结束那轮的最后一刻，与 `completeNormalRound` 的事务刻一致），
   由调用方以该 tick 派发「残影消失」；同时写明容量淘汰（`CAPACITY = 2`）与寿命淘汰**都要**出现在该列表里。
3. 测试：`EchoQueueTest` 增「`L=1` 且第 3 轮入队 → 第 1 轮残影出现在淘汰列表且不再 `activeEchoes`」、
   「容量淘汰（同一轮入队超容量）」两条；并在 `EchoState.eventsAt` 的 javadoc 里补一句
   「残影消失的机关语义由调用方派发，`replay/**` 不持有机关状态」。
4. 交付时给 PM 一句**调用样例**（app 轮末如何用该列表派发 `GameEvent.echoDisappeared`）。

## 三、可选：事件 `sourceRound` 语义结论

活玩家事件的 `sourceRound` 目前恒为 app 的常量 `0`；有了 E-3（回放 actor 改写为 `echo_<N>`）后占用侧已不冲突。
若你认为需要「事件带当轮轮次」，先给一页方案（改动面、对既有残影数据的兼容性），由 PM 决定是否单独发卡；
**本卡不做该改动**。

## 四、允许 / 禁止

- 允许：`replay/**`、`snapshot/**`、对应测试、`docs/development/开发2/**`。
- 禁止：`app/**`、`core/**`、`entity/**`、`mechanism/**`、`level/**`、`render/**`、`ui/**`、`pom.xml`、
  `src/main/resources/**`；**`replay/**` 不得 import `mechanism/event` 或 `mechanism/autodock`**。

## 五、交付

PR 链接 + tip 哈希 + 数字 + E-4 的 API 形态与调用样例，写进 `docs/development/开发2/X-MOVE-COLLAPSE-01-v3-交付.md`。
