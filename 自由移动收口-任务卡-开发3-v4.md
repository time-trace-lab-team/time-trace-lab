# 任务卡 · 生命周期 Phase 2 前置与收尾 v4（开发三）

> 任务 ID：**BUG-002-LIFECYCLE-Phase2-PREP** ＋ **DEV3-v4-收尾**（开发三 v4）
> 板块：**开发三**（`mechanism/**`、`level/**`、`ui/**`、`persistence/**` + 测试、`docs/development/开发3/**`）
> 状态：**已发卡，可开工**
> 基线：`origin/develop` = `442b895`（Phase 1 已随 PR #64 合入）
> 日期：2026-09-12

## 一、最高优先：Phase 2 前置结论（Phase 2 的唯一阻塞）

PM 侧 app 收尾已完成并开 **PR #66**：`Level01Assembly` 自持注册表与总线、窄端口注入、`cleanup()` 不再触碰全局单例、
四个 app 集成测试移除全局 `clear()`；新增 `Level01AssemblyLifecycleTest` 三条；`app/**` 内
`DockingPlateRegistry.getInstance()` / `EventDispatcher.getInstance()` **零命中**；clean 全量 **350/0/0**。

因此 Phase 2 只剩一件事等你：**给出结论并写进交付文档** ——

> `DockingPlate.Snapshot`、`MechanismSnapshot`（`snapshot/**`）、`AutoDockSnapshotPort`
> 是否**隐式依赖兼容单例**？若依赖，列出依赖点与 Phase 2 需要一并改造的范围（或在 Phase 1 里补掉）。

结论到位后 PM 立刻发 Phase 2 开工令（删兼容单例 + 6 个旧构造器）。**未经开工令不要编码 Phase 2。**

## 二、Phase 1 收尾回填（文档）

1. `L-4b`（`level/**` 清 `defaultExit`）PM 已核实**删净**（`git grep defaultExit src/main/.../level` 为空）——
   请在交付文档回填条目与数字；
2. `E-2` 的 **`Door` 计数口径**：残影占板与玩家占板是否等价、`Door` 是否只认“两块板都被占”而不区分占用者身份 ——
   确认已写进 `docs/development/开发3/开发三-稳定ID与排序-autoDock规格冻结.md`（若只在代码里请补文档）；
3. `RayManager` 删除与「注册表与事件总线的生命周期」冻结节已在 #64 交付 —— 在交付文档里给出节号与结论摘要。

## 三、EXPAND-MAP-VIEW-02（B 卡）：**未立项**

窗口/视口（A 卡）已立项并分配给开发一 + PM。地图扩展**尚未立项**：需要项目方确认“玩法空间不足”并给出具体体验理由；
若立项：优先 **B2（14×10 / 16×11，保留拓扑与解法）**，B1（20×12、重做第一关）需接受第一关重做 + 到达刻重算 +
全部关卡测试重跑。**到达刻重算必须与 B 同卡**，禁止先改地图后补；轮长若需调整，须由 PM 先改 README §六 表格（12–25 秒内）。

## 四、边界

- 允许：`mechanism/**`、`level/**`、`ui/**`、`persistence/**`、`audio/**`、对应测试、`docs/development/开发3/**`。
- 禁止：`core/**`、`entity/**`、`app/**`、`render/**`、`replay/**`、`snapshot/**`、`pom.xml`、`src/main/resources/**`。
- **`app/**` 属 PM**：Phase 1 的 app 接线已由 PM 完成，Phase 2 若需 app 配合（删除旧构造器后的调用点），
  由 PM 在同一批处理，你先给清单。

## 五、验收

1. Phase 2 前置结论（含证据/依赖点清单）交付；
2. Phase 1 收尾回填（L-4b / Door 口径 / 生命周期冻结节）在交付文档可查；
3. `.\mvnw.cmd -o clean test` 退出码 0，任何单测 ≤ 1s；
4. 未获批前不实施 Phase 2、不触碰 app/**。
