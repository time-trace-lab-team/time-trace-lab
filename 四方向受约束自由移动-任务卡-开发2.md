# 任务卡 · 移动模型变更的录制/回放配套（开发二）【最新版 · 待执行】

> 任务 ID：**R2.7-IDLE-FRAMES**
> 所属板块：**开发二**（replay / snapshot + 测试）
> 状态：**待执行**（依赖开发一先落地 `MovementState.IDLE`）
> 依据：`README.md`（已修订：§三 四方向受约束移动、§九 时间回放方案）、`R5-开工前裁决.md`
> 上游卡：开发一 `C-PLAYER-MOVE-01`（提交 `35a4442` 的 README 变更）
> 日期：2026-09-10

---

## 一、背景

README §三 已由「恒速巡行」改为**四方向受约束移动**：按住走、松开停。因此**普通廊道也会出现静止帧**（`MovementState.IDLE`），README §九 已同步为「松开全部方向键后角色静止并**逐刻写入静止帧**，`DOCKED` 状态同样写入静止帧，重新移动后继续记录」。

开发二侧核实结论：`replay/**` 只在 `PlayerFrame.movementState` **存储**该枚举，**没有任何按值分支**，因此新增 `IDLE` **不需要改逻辑**；本卡职责是**确认并锁定确定性**，而不是新增功能。

## 二、目标

1. 确认 `MovementState.IDLE` 帧被正常记录与回放，**不需要**特殊分支、**不得**引入第二套时钟或按移动状态改变帧索引。
2. 用测试锁定：静止段仍**逐 tick 写帧**、帧索引严格 `== tick`、`0..D-1` 无缺/重帧。
3. 残影回放仍是**位置快照逐刻索引**，`IDLE` 段与 `DOCKED` 段一致复现。

## 三、允许 / 禁止修改路径

- **允许**：`src/main/java/org/example/timeloop/replay/**`、`snapshot/**`；对应测试。
- **禁止**：`pom.xml`、`app/**`、`core/**`、`entity/**`、`render/**`、`mechanism/**`、`level/**`、`ui/**`、`persistence/**`、`audio/**`、`src/main/resources/**`。
- **前置**：`MovementState.IDLE` 由开发一在 `core/MovementState` 新增（本卡不得自行添加）。

## 四、实施要点

1. **不改回放模型**：`EchoState.frameAt(roundTick)` / `ReplayPort` 保持"直接索引记录帧"，不得按 `movementState` 分支或跳过帧。
2. **静止段逐 tick 写帧**：`RecordingSession.recordFrame` 对 `IDLE` 帧与 `CRUISING` 帧一视同仁；`DOCKED` 帧行为不变。
3. **不新增边界语义**：`IDLE` 不产生事件（不是 `autoDock` 占用，不发 `OCCUPANCY_RELEASED`）；事件仍只在停驻/离开/释放边沿产生。
4. **轮末事务不变**：`IDLE` 段照常计入 `durationTicks`，满长封装、轮末生成残影的逻辑不动。

## 五、测试（必须新增/调整）

1. `IDLE` 帧被记录：连续若干 `IDLE` 帧进入缓冲，`size()` 与 `frameAt()` 连续无空档；
2. 帧索引严格 `== tick`：混合序列 `CRUISING → IDLE → DOCKED → IDLE → CRUISING` 全程成立；
3. 残影逐刻一致：同一混合序列录制→下一轮 `EchoState.frameAt(t)` 与原帧**逐 tick 相等**；
4. `IDLE` 不产生 `TimelineEvent`（事件集合不含额外项）；
5. 满长/丢弃边界不回归（`D` 帧封装一次、未满丢弃）。

## 六、验收条件

1. 全量 `mvn test` 退出码 0；除开发一重写的两条移动测试外，其余不回归。
2. `replay/**`、`snapshot/**` 内**无**按 `MovementState` 值的分支（评审可查）。
3. 静止段的回放结果与录制逐 tick 一致，且不含独立计时器/帧跳过。
4. 事件语义不变：`IDLE` 不发事件。

## 七、交叉依赖与顺序

- **必须先等**开发一在 `core/MovementState` 加入 `IDLE`，本卡才能编译新测试；在此之前只做分析与用例设计，不写代码。
- 本卡**不阻塞**开发一；开发一可在 `IDLE` 落地后独立跑其卡内测试。

## 八、遗留（按既有裁决执行，不并入本卡）

- **R5-A「第一关快照基础」**：按 `docs/decisions/R5-开工前裁决.md` 裁决 1/2/3 执行（含 typed restore port 签名提案，需 PM + 使用方确认后落地）。
- 裁决 4（`tryLeave` 放宽）由**开发三**主责、开发一配合，**开发二只按事件最终 `tick` 回放**。

## 九、验证命令

```powershell
.\mvnw.cmd test
.\mvnw.cmd "-Dtest=TimelineRecordingTest,EchoStateTest,RecordingSessionTest" test
```

---

*本卡只动 `replay/**`、`snapshot/**` 与测试；发现需要改 `MovementState`（core）或其他板块 → 停止上报。*
