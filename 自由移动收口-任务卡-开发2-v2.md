# 任务卡 · 自由移动收口 v2（开发二）

> 任务 ID：**X-MOVE-COLLAPSE-01-DEV2-v2**（替代早前 `自由移动收口-任务卡-开发2.md`；其 R-1…R-4 已交付）
> 板块：**开发二**（`replay/**`、`snapshot/**` + 测试）
> 状态：**已发卡，可开工**
> 基线：`origin/develop` 现 `202e7ad`；你的交付在 **`origin/codex/dev2-x-move-collapse`（`0d28063`，R1–R4）**
> 集成状态：PR #54 / #55 已开（PM），与你的分支无文件冲突（你只动 `replay/**`）
> 日期：2026-09-10

## 一、当前已完成（复核结论，勿重做）

| 子项 | 内容 | PM 复核 |
| --- | --- | --- |
| R-1 | 移动脚本逐刻保真（`MovementScriptFidelityTest`） | ✅ 已交付（含 IDLE/DOCKED 段） |
| R-2 | 事件录制闭环（`EventRoundTripTest`） | ✅ 已交付；PM 侧 app 已开始 `recordEvent`（`7c3de3e`） |
| R-3 | 轮边界契约（`RoundBoundaryContractTest` + `RecordingSession`/`RoundClock` javadoc） | ✅ 与 PM 的「轮末停在 READY、调用方负责 `transition(PLAYING)`」实现一致 |
| R-4 | 寿命/淘汰/满长边界回归 | ✅ |

**注意**：该分支**尚未并入 develop**（`git merge-base --is-ancestor origin/codex/dev2-x-move-collapse origin/develop` 为假）。

## 二、本卡任务

### A. 交付并入（最高优先）

1. 用 `codex/dev2-x-move-collapse` 开 PR → `develop`（标题 `X-MOVE-COLLAPSE-01 R1-R4 录制/回放与轮边界`）；
2. PR 描述里写明：`replay/**` 的行为改动**只有 javadoc**（`RecordingSession`/`RoundClock` 的轮末停靠契约），
   其余为新增测试；并附 `Tests run / Failures / Errors` 与最慢单测耗时；
3. 若 develop 已并入 PR #54/#55，请把 develop 并入你的分支后再跑一次全量，确认无冲突。

### B. E-3：残影回放 actor 归属的契约注记（配合 `X-MOVE-COLLAPSE-01-ECHO-ACTOR`）

背景（PM 实测，真 bug）：`AutoDockService.requireActor` 强制 actor 只能是 `player` 或 `echo_<N>`
（且 `N == sourceRound`），而 `DockingPlate.onEvent(ECHO_DISAPPEARED)` **只在占用者等于 `"echo_" + sourceRound` 时**才释放
→ 现在 app 回放传的是录制时的 `"player"`，导致**残影消失后驻留板永久占用**。

要求（只改 `replay/**` 的契约与测试，**不改写行为**）：

1. 在 `EchoState.eventsAt(long)` 的 javadoc 明确：**回放写入机关时的 actor 归属由调用方改写为 `echo_<sourceRound>`**；
   `replay/**` 不负责改写，也不得把 `player` 当作残影；
2. 新增/补充测试：一段录制的 `DOCK_ENTERED` 事件经 `EchoState.eventsAt(t)` 取出后，
   **其 `event.actorId()` 仍是录制值**（契约不改写），并断言调用方改写后 `sourceRound` 与 `echo_<N>` 一致；
3. 若你认为需要，加一条「残影消失后其占板语义由调用方释放」的注释级说明（不引入 replay 侧机关状态）。

### C. （可选，需先给结论）事件 `sourceRound` 语义

现状：活玩家事件的 `sourceRound` 恒为 app 的常量 `0`（`PLAYER_SOURCE_ROUND`）。有了 B 的 actor 改写后，
占用侧已不会冲突（`player` ≠ `echo_N`）。若你认为回放语义需要「事件带当轮轮次」，请先给一页结论
（改动面、兼容性、对既有残影数据的影响），由 PM 决定是否单独发卡；本卡**不做**该改动。

## 三、允许 / 禁止

- 允许：`replay/**`、`snapshot/**`、对应测试、`docs/development/开发2/**`。
- 禁止：`app/**`、`core/**`、`entity/**`、`mechanism/**`、`level/**`、`render/**`、`ui/**`、`pom.xml`、`src/main/resources/**`。
- 发现 app 侧调用缺失 → 写进交接清单，**不要动 `app/**`**。

## 四、依赖与顺序

- A 可立即做；B 可立即做（与 E-1 的 app 改写并行，互不阻塞）；
- C 仅出结论，不改代码。

## 五、交付

PR 链接 + 提交哈希 + `.\mvnw.cmd -o test` 数字 + 最慢单测耗时 + B 的契约条目落在哪个 javadoc/测试，
写进 `docs/development/开发2/X-MOVE-COLLAPSE-01-v2-交付.md`。
