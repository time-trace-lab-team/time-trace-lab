# 任务卡 · 第一关「门与终点合并」· 渲染外观（开发一）

> 任务 ID：**L01-GATE-MERGE-DEV1**
> 主责：**开发一**（`render/**` + 其测试）｜ 依据：`L01-门与终点合并-PM裁决.md`
> 基线：`origin/develop` = `3e0778d`（PM clean 复跑 381/0/0）
> 状态：**开工**（可与开发二并行；闸门节点坐标由开发三给出后再做目视对齐）
> 日期：2026-09-13

## 〇、PM 已确认的交付契约（2026-09-14，取自你的 render 对接卡）

**结论：PM 审核通过，无需改动。** 与 PM 裁决 A′ 完全一致，逐条确认如下：

| 契约 | 确认 |
| --- | --- |
| `RenderViews.MechanismKind` 新增 `SWITCH`；`RenderViews.Mechanism` 记录结构**不变** | ✅ 与裁决 §五 一致（优先用 `kind` 表达，不动结构） |
| `SWITCH` + `active=false` = 未触发的弹起踏板 | ✅ |
| `SWITCH` + `active=true` = **本轮已锁存的压下踏板**，**不表示当前是否有人站在上面** | ✅ 与 D1/D4 一致；`active` 由 app 侧传 `rightPlate.isLatched()`（PM 已在开发三卡 §2.3.1 固定该读接口名） |
| `EXIT` + `active=false/true` = 关闭的实体闸门 / 打开的门框（保留既有 `E` 提示） | ✅ 与裁决 §二 A′ 第 2 条一致；`active` 现在承载 `exit.isDoorUnlocked()`（现状已如此） |
| `DOOR` 绘制分支**保留**，第一关不再投影该条目 | ✅ 与裁决 §五「保留 `MechanismKind.DOOR`」一致 |
| 开发一**不得**改 `app/**`；PM 不改 `Door`/`ExitTerminal` 契约 | ✅ 边界无冲突 |

**两点补充（不影响开工）**：

1. 你的卡 §2 里 PM 的投影依赖 `rightPlate` 的**锁存状态**，该读接口此前不存在 —— PM 已在
   `L01-门与终点合并-任务卡-开发3.md` §2.3.1 固定为 **`DockingPlate.isLatched()`**（仅 `role=switch` 置位、
   离开不清零、`reset()` 清零）。**你不需要调用它**（PM 投影时传入 `active`），列在此仅为让你知道 `active=true`
   的语义来源；
2. 卡内编号 **缺 §3**（从 §2 直接跳到 §4）—— 不影响执行，仅记录，避免以后引用时找不到。

## 一、目标

玩家在画面上看到的是**一个闸门终点 + 一个开关**，而不是「两块一样的板 + 一个远处的门 + 一个终点」。

## 二、交付物

1. **`RenderViews.MechanismKind` 新增 `SWITCH`**（PM 已批准），javadoc 必须写明：
   「`SWITCH` 是 `dock_plate` 的**表现变体**（关卡数据 `role=switch`），不是独立机关类型；
   README §五 / §七 的机关范围未变更。」
2. **开关外观**：按键/踏板形态，用 `Mechanism.active` 区分 **已开启（锁存 ON）/ 未开启（OFF）** 两态，
   视觉上与驻留板明确不同（玩家不应再把它读成「另一块板」）。
   **注意语义**：开关**踩上即开启、离开不关闭**（本轮内锁存，见 PM 裁决 §十 D1/D4）——
   因此「按下」姿态不应绑定在「有人站在上面」上，而应表达**持续开启**（例如压下后保持在下位）。
3. **闸门终点的开关态**：`MechanismKind.EXIT` 现在会承载「闸门是否已解锁」的 `active` 信号
   （来源 `exit.isDoorUnlocked()`）——请确保：
   - 未解锁时表现为**关闭的闸门**（有实体感、明确不可进入）；
   - 解锁后表现为**打开**（可辨认的通行状态），并按既有规则绘制交互范围提示。
4. **`MechanismKind.DOOR` 保留**（第二关起仍需要），第一关不再产出该条目——**不要删除门图层的绘制分支**。
5. **测试**：`src/test/java/.../render/**` 补
   - `SWITCH` 与 `PLATE` 外观可区分（**锁存 ON / OFF** 两态渲染差异断言，沿用你既有的图层变换测试范式）；
   - 闸门 `active=true/false` 两态的渲染差异；
   - 不依赖具体像素值，断言结构/颜色通道差异即可。

## 三、允许 / 禁止路径

**允许**：`src/main/java/org/example/timeloop/render/**`、`src/test/java/org/example/timeloop/render/**`、
`docs/development/开发1/**`。

**禁止**：`app/**`（PM）、`level/**`、`mechanism/**`、`snapshot/**`、`replay/**`、`core/**`、`pom.xml`、`README.md`。
**不要**改 `RenderViews.Mechanism` 的记录结构（PM 裁决 §五：优先用 `kind` 表达；若确实无法表达，
停手回报并附理由，走小提案）。

## 四、验收

1. 开关两态 + 闸门两态共 4 种外观可在测试中断言；
2. **目视**：默认窗口下，**开关与左驻留板一眼可区分**；闸门关闭 / 打开状态可辨认；
3. `.\mvnw.cmd -o clean test` 退出码 0、**任何单测 ≤ 1 s**、数字回填（基线 381）；
4. `git diff --check` 干净。

## 五、证据与回传

```
branch: codex/l01-gate-merge-dev1
SHA:    <40 位>
新增外观: SWITCH(按下/弹起)、EXIT(关闭/打开) —— 断言测试名
截图:    关闭态 / 打开态 / 开关激活态（可选，PM 会补目标机截图）
clean test: ? / ? / ? / ? ；最慢单测 <名称> <秒>
```

**交付以远端提交为唯一凭据**；禁止 `git add -A`，精确白名单提交。

## 六、停止条件

1. 发现必须改 `RenderViews.Mechanism` 结构或 `app` 侧构造才能表达外观 → 停手回报 PM；
2. 需要改 `level/**` 数据（例如 `role` 字段没到位）→ 停手回报，由开发三补；
3. 单测 > 1 s 且无法解释 → 停手回报。
