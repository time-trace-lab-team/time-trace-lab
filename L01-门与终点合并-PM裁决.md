# PM 裁决 · 第一关「门与终点合并」范围与公共接口

> 任务 ID：**TASK-PM-L01-GATE-MERGE**
> 拟稿：**郎森（成员 E / 测试）** ｜ 裁决：**PM（成员 A / 架构与集成）**
> 裁决日期：2026-09-13 ｜ 基线：`origin/develop` = **`3e0778d`**（PM clean 复跑 **381 / 0 / 0**）
> 状态：**已裁决，可开工**（拆卡见 §七）
> 关联（本裁决**不覆盖**，另行受理）：`BUG-005` 交互缓冲未接线、`REQ-001` 结算界面缺失

## 一、PM 复核：卡内事实核对（行号为 PM 实测，替换卡内旧行号）

| 卡内论断 | PM 实测 | 结论 |
| --- | --- | --- |
| 门有双重职责 | ①物理阻挡：`app/Level01Assembly.java:431-437` `isPassable()` 比对 `door.getPosition()`；②解锁信号：`mechanism/Door.java:78-87` `updateDoorState()` → `doorUnlocked` 事件 | ✅ 成立 |
| 直接删除会立刻崩溃 | `Level01Assembly.java:129` `levelData.getDoors().get(0)`；`ExitTerminal.java:39-44,54-55` 强制 `associatedDoorId` 且必须是 `door` 前缀机制 ID | ✅ 成立 |
| 完整耦合 11 处 | PM 扫到 **16 处引用**（`Level01Assembly` 13 行 + `ExitTerminal` 3 处），比卡内更多（卡内漏了 `renderViews` 中的门图层状态与 `restart()`/`onRoundEnd()` 两处 `reset` 之外的门态投影） | ✅ 结论不变：**不能直接删除** |
| 行号 | 卡内 `117`→实际 `129`；`196`→`213`；`308-309`→`343-346`；`332`→`372`；`393-399`→`431-437`；`464`→`502`；`481`→`533`；`ExitTerminal 43-44/80-85/97-101`→`39-44/85-90/108-113` | ⚠️ 已订正（卡内行号基于更早的工作副本） |
| `ExitTerminal` javadoc 里的坐标 | `DEFAULT_INTERACT_RADIUS` 注释仍写「右驻留板 (8,5) 与出口终端 (9,5)」，实测已是 (18,8)/(19,8) | ⚠️ **过期注释，随本批订正** |

**级别数据实测**（`level/Level01Footsteps.java:1170-1211`）：

| 物件 | 格子 | 世界坐标 | 备注 |
| --- | --- | --- | --- |
| 左驻留板 `L01_plate_left` | (4,6) | (216, 312) | `autoDock=true` |
| 右驻留板 `L01_plate_right` | (18,8) | (888, 408) | `autoDock=true` |
| 终点 `L01_exit_00` | (19,8) | (936, 408) | 与右板相距 48 < 72（1.5 格内可交互） |
| 门 `L01_door_01` | (18,13) | (888, 648) | `requiredPlateIds = {left, right}`；源码注释自称「不在任何必经路上」 |

**可用的新闸门节点**（PM 实测路径节点表）：开关 (18,8) 周围 1.5 格内的既有节点有
`c17_r8 / c18_r7 / c18_r9 / c20_r8`（48）与 `c17_r7 / c19_r7 / c17_r9 / c19_r9`（67.9）。
→ 卡内 §6 的约束**可满足**，开发三有多个候选，不需要新造节点。

## 二、裁决 1：决策选项 —— 采纳 **A′**（= A 的玩家可见结果 + B 的实现路径）

| 选项 | 裁决 |
| --- | --- |
| A（门不再作为独立机关、`ExitTerminal` 改为接收条件提供者） | **不采纳其代码形态**。理由：①会重开刚冻结的 R5-B 契约（`ExitTerminal.Snapshot(mechanismId, doorUnlocked, triggered)`，`docs/decisions/R5-B-聚合接口冻结-PM裁决.md` §二 #3）→ 连锁到聚合器、快照、`StableIdValidator` 前缀规则四处；②本周期目标是**可读性修复**，不是架构清理；③A 并不能删除 `Door` 类（README §五 保留「门」作为第二关起的机关），只是把它从第一关挪走条件逻辑 |
| B（保留 `Door` 但隐形、仍派发解锁事件） | **不完整采纳**。玩家可见结果确实变好，但门会变成真正的「隐形中间层」——这正是测试反对的点 |
| **A′（采纳）** | **门 = 闸门本体**：`Door` 与 `ExitTerminal` **使用同一世界坐标**，玩家看到的是一个闸门（关闭 = 走不进去 + 显示未解锁；满足两条件 = 开门 + 可按 `E` 结算）。门不再有第二个门体，也不再是隐形层；**代码契约不变** |

**A′ 的具体形态**：

1. `Door` 的位置改为**新闸门节点**（≠ 原终点 (19,8)），`ExitTerminal` 使用**同一坐标**；
   两者同格，渲染为一个闸门终点。
2. `renderViews()` 在第一关**不再产出 `MechanismKind.DOOR` 条目**；闸门外观由 EXIT 按
   `exit.isDoorUnlocked()`（即 `Mechanism.active`）表现。`MechanismKind.DOOR` 枚举**保留**（第二关起仍用）。
3. 门的物理阻挡**保留**且语义变好：锁着时挡住**闸门自己那一格**（可见、合理），不再是一堵看不见的墙。
4. `requiredPlateIds` 仍为 `{L01_plate_left, L01_plate_right}`（右板改为开关后 ID 不变，见 §三）。

## 三、裁决 2：右驻留板改为「开关」——**批准，且不新增机关类型**

- 「开关」实现为 **`dock_plate` 的表现变体**（关卡数据 `properties.put("role", "switch")`），
  **不新增 `mechanism` 类型、不新增事件类型**；README §五 与 §七 的冻结范围**不动**。
- 语义：**占用即激活、离开即失活、轮末 `reset`、不锁存**（与 `autoDock` 完全一致）。
  → 这是**刻意**的：若开关锁存，第二轮只需玩家单独操作，残影就不再是必需，第一关「双来源因果链」的教学价值会丢失。
- 机制 ID 保持 `L01_plate_right`（`Door` 的 `StableIdValidator` 要求 `plate` 前缀；改 ID 会无谓地牵连快照与测试）。
- 外观与文案（开关图标、按下/弹起状态、HUD 文案）由开发一/开发三负责，player 看到的不是「板」。

## 四、裁决 3：几何硬约束（**全部为硬性，PM 会写进可执行测试**）

新闸门节点（`Door` 与 `ExitTerminal` 同坐标）必须同时满足：

1. **≠ 原终点** (19,8) = (936, 408)（需求明确要求）；
2. 落在**既有路径节点**上（`AutoDockService` 与路径图要求机关中心 = 节点中心）；
3. **与开关中心（888, 408）距离 ≤ 1.5 × tileSize = 72** —— 否则玩家必须离开开关才能按 `E`，
   离开即失活 → 条件不满足 → **关卡无解**；
4. **不得与开关同格** —— 闸门锁着时挡住自己那一格，玩家将无法站上开关 → 无解；
5. **不得是「出生点 → 左驻留板」或「出生点 → 开关」的唯一通路节点**（锁着时不得堵死通路）；
6. 第 1 轮可规划路线内可达。

**加一条可测不变量**：`distance(gate, switch) ∈ (0, 72]` 且 `gate` 不是上述两条路线上的割点。
开发三必须给出「选了哪个节点、为什么满足 5/6」的一行证据。

## 五、裁决 4：公共接口变更 —— 逐条处置

| 卡内提案 | 裁决 |
| --- | --- |
| `ExitTerminal` 构造去掉 `associatedDoorId` | **驳回**（见 §二；无必要，且重开 R5-B 冻结） |
| `ExitTerminal` 解锁来源改为监听新条件 | **驳回**（继续监听 `DOOR_UNLOCKED`，由 `Door` 派发） |
| `Level01Assembly.objectiveView()` 的 `doorUnlocked` 语义 | **保留字段**，语义明确为「闸门是否已解锁」；HUD 文案层表达「开关」措辞（`ui/**`，开发三） |
| `RenderViews.MechanismKind` 是否保留 `DOOR` | **保留**（第二关起使用）；**批准新增 `MechanismKind.SWITCH`**，并在枚举 javadoc 写明「`SWITCH` 是 `dock_plate` 的表现变体，不是独立机关类型」 |
| `RenderViews.Mechanism` 是否新增外观字段 | **暂不新增**：优先用 `kind = SWITCH` 表达，避免动冻结面；若开发一证明无法用 `kind` 表达，再走小提案 |

## 六、裁决 5：README 同步时机

README §三（通关链路）、§六（第一关示例：由「双驻留板门」改为「左驻留板 + 开关 → 同格闸门终点」）、
§十六 相关条目，**随实现 PR 一起落地**（由 PM 修改），**不提前写不存在的行为**。
§五 / §七 **不改**（无新机关类型）。

## 七、拆卡（单板块，禁止同批跨模块）

1. `L01-门与终点合并-任务卡-开发3.md` —— 关卡数据 + 机制侧注释订正 + 关卡侧可解性测试 + HUD 文案；
2. `L01-门与终点合并-任务卡-开发1.md` —— 渲染：`MechanismKind.SWITCH` 外观与闸门开关态表现；
3. `L01-门与终点合并-任务卡-开发2.md` —— 快照/恢复：确认新条件组合在**既有冻结契约**内可恢复，补 2 条测试；
4. `L01-门与终点合并-任务卡-PM接入.md` —— app 接线（渲染列表、`objectiveView`、清理、测试）+ README 同步 + 端到端目视验收。

依赖顺序：**开发三先定节点并给出约束证据** → 开发一/二并行 → PM 接入 → 测试人工验收。

## 八、验收（整批）

1. 玩家可见：地图上只有**一个**闸门终点，开关旁就是它；门锁着时**走不进那一格**；两条件满足即开门；
2. **开关离开即失活**（非锁存）：第一轮踩上开关再离开，闸门立刻回到关闭态；
3. **第一轮 → 第二轮**：第一轮玩家占左板（残影录制）→ 第二轮残影占左板 + 玩家踩开关 → 开门 → 按 `E` 结算；
4. 三名以上成员试玩，**3 分钟内能口述通关链路**（这是本次变更的唯一目的，必须实测）；
5. 关卡侧可解性测试 + app 端到端测试更新并通过；
6. `.\mvnw.cmd -o clean test` 退出码 0、**任何单测 ≤ 1 s**、数字回填（基线 381）；
7. `git diff --check` 干净；所有交付以远端提交为凭据。

## 九、边界与不做

- 本批**只动第一关**；不得顺手重构其它机关、不得改 `Door`/`DockingPlate` 的公共契约；
- `BUG-005`（交互缓冲未接线）、`REQ-001`（结算界面缺失）**另行受理**，但建议与本批并入同一里程碑验收
  （都属「第一关体验闭环」）；
- 若实现过程中发现必须改 `ExitTerminal` 契约才能达成验收，**停手回报 PM 重裁**，不要自行扩面。
