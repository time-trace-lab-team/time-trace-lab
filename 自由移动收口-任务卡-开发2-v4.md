# 任务卡 · 回放收口与 R5-B 启动 v4（开发二）

> 任务 ID：**REPLAY-EVICTION-DISPATCH-CONTRACT** ＋ **R5-B-AGGREGATION**（开发二 v4）
> 板块：**开发二**（`replay/**`、`snapshot/**` + 测试、`docs/development/开发2/**`）
> 状态：**已发卡，可开工**
> 基线：`origin/develop` = `442b895`（你的 R1–R4、E-3、E-4 均已随 PR #61/#62 合入）
> 日期：2026-09-12

## 一、A 部分（可立即做）：残影淘汰派发的调用契约

**PM 实测**：`GameEvent.echoDisappeared(...)` 在 `src/main` 里**仍无任何派发点**（只有工厂方法），
因此 `DockingPlate.onEvent(ECHO_DISAPPEARED)` 的释放分支仍是死代码。第一关 `L=1` + 轮末 `plate.reset()` 掩盖了它，
**L≥2 的关卡（第三至五关）之前必须可用**。

分工按既有裁决：**`replay/**` 只暴露数据，派发由 app 负责**（保持分层，`replay/**` 不得 import `mechanism/event`）。

请交付（**不写代码也行，先给契约**；若需新增访问器再实现）：

1. **一行调用样例**：app 在轮末如何拿到本轮的淘汰列表（用你 E-4 落地的 API —— 是 `addOnRoundEnd` 返回列表，
   还是 `evictedSince(...)` 之类只读访问器？给出确切签名与返回值类型）；
2. **tick 归属**：淘汰事件应记在**哪一个逻辑刻**（轮末边界刻 = 刚结束那轮的最后一刻？还是新轮第 0 刻？）——
   PM 会按此在 app 里派发，口径必须与 `TimelineEvent.tick` 的既有约定一致；
3. **容量淘汰与寿命淘汰**是否都出现在该列表里（`CAPACITY = 2` 与 `L` 两条路径）；
4. 若现有 API 不足以表达（例如列表被内部清空、或调用一次即失效），先出提案再由 PM 批准。

## 二、B 部分（可选，一页结论）：事件 `sourceRound` 语义

活玩家事件 `sourceRound` 恒为 app 的常量 `0`（现已在 E-1 后用 `echo_<N>` 区分占用，功能上不再冲突）。
若你认为回放语义需要「事件带当轮轮次」，给一页方案（改动面、对既有残影数据的兼容性、迁移步骤）；
**本卡不改该行为**，PM 视结论决定是否单独发卡。

## 三、C 部分（下一阶段主线）：R5-B 聚合与快照

按你自己的交接文档（`docs/development/开发2/R5-B-P-MOVE-ALIGN-开发二交接文档.md`）的 Gate B/C：

1. 冻结 **R5-B 聚合接口**（多机关快照 → 一轮事务内的原子 restore）；
2. 与开发三的 per-mechanism 快照/恢复（`MechanismSnapshot`、`AutoDockSnapshotPort`、射线/共振/中继）对接；
3. **typed restore port** 的签名提案需 PM + 使用方（开发三）确认后再落地；
4. 注意与 `BUG-002-LIFECYCLE` Phase 2 的交叉：注册表/总线改为按装配注入后，聚合快照**不得隐式依赖单例**。

## 四、说明：EXPAND-MAP-VIEW 与本模块无关

窗口/视口改造（`EXPAND-MAP-VIEW-01`）不改世界尺寸、不改轮长、不改帧数，`replay/**` **无需变更**；
只有渲染的投影变换变化，`PlayerFrame` 内容与索引语义不变。

## 五、允许 / 禁止

- 允许：`replay/**`、`snapshot/**`、对应测试、`docs/development/开发2/**`。
- 禁止：`app/**`、`core/**`、`entity/**`、`mechanism/**`、`level/**`、`render/**`、`ui/**`、
  `pom.xml`、`src/main/resources/**`；`replay/**` 不得 import `mechanism/**`。

## 六、验收

1. A 部分契约（签名 + tick 口径 + 两条淘汰路径）写进交付文档，并给 app 一行调用样例；
2. 若新增访问器：`EchoQueueTest` 覆盖「寿命淘汰」「容量淘汰」两条并保证既有用例不回归；
3. C 部分：R5-B 聚合接口提案（含 typed restore port 签名）提交 PM 与开发三评审；
4. `.\mvnw.cmd -o clean test` 退出码 0，任何单测 ≤ 1s，数字与基线哈希回填。

---

## 七、PM 裁决（2026-09-13）：A 部分结案，改为 app 侧 diff，**不采纳 `lastEvictedEchoes()` 提案**

开发二 A 部分交付（契约四问 + `lastEvictedEchoes()` 提案）已复核，采纳其中三条、否决一条：

| 项 | 裁决 |
| --- | --- |
| ② tick 归属 | **确认**：淘汰刻 = 轮末边界刻 = `durationTicks - 1`（= `onRoundEnd` 内 `completeNormalRound` **之前**的 `clock.roundTick()`），与 `TimelineEvent.tick` 既有约定一致 |
| ③ 两条淘汰路径 | **确认你的澄清**：返回列表只来自**寿命淘汰**（`nextRound - sourceRound > L`）；容量是防御断言（`IllegalStateException`），不产生淘汰返回。请把这句话正式写进 `EchoQueue` 的 javadoc（替换易误读的“容量淘汰”表述） |
| ④ API 缺口 | **不采纳 `lastEvictedEchoes()`**：它会在 `replay/**` 引入“只在下一次轮末事务前有效”的**粘性状态**，还需要在 `restartFromFirstRound` / `failFinalRound` / `completeGoal` 三处补清空点，收益不抵成本。改为 **app 侧轮末 diff**（PM 实现）：`onRoundEnd()` 内在事务前取 `echoQueue.activeEchoes(clock.currentRound())` 的 sourceRound 集合，事务后取新轮集合，**差集即本轮被淘汰的残影** → app 用边界刻派发。**零公共 API 变更、无粘性状态、无需跨模块排序**；E-4 已落地的 `addOnRoundEnd` 返回值保留备用（不计为死代码） |
| B 部分 `sourceRound` | **结案、不发卡**：L=2 时两个在场残影已由 `echo_1` / `echo_2` 区分，无需给活玩家事件打轮次；保持“活玩家 `sourceRound = 0`” |

### 7.1 ⚠️ 样例里必须修正的一处（PR #66 之后）

你给的调用样例用的是 `EventDispatcher.getInstance().dispatch(...)` —— **现在这样派发不会到达任何机关**：
`BUG-002-LIFECYCLE Phase 1` 之后，板/门/出口只注册在**装配自己的** `GameEventBus` 实例上（PR #66）。
正确写法：

```java
// app/Level01Assembly.onRoundEnd()：completeNormalRound 之前先取边界刻
long boundaryTick = clock.roundTick();          // == durationTicks - 1
Set<Integer> beforeRounds = echoQueue.activeEchoes(clock.currentRound()).stream()
        .map(EchoState::sourceRound).collect(Collectors.toSet());
recording.completeNormalRound(() -> dockController.reset(AutoDockResetReason.ROUND_END, boundaryTick));
Set<Integer> afterRounds = echoQueue.activeEchoes(clock.currentRound()).stream()
        .map(EchoState::sourceRound).collect(Collectors.toSet());
for (int gone : beforeRounds) {
    if (!afterRounds.contains(gone)) {
        eventBus.dispatch(GameEvent.echoDisappeared("echo_" + gone, boundaryTick, gone));  // eventBus = 装配实例
    }
}
```

### 7.2 分工与验收

- **开发二**：本卡 A 部分**结案**（结论已采纳，无需代码变更）；只剩把 ③ 的澄清写进 `EchoQueue` javadoc；
- **PM（我）**：实现上面的 app 侧 diff + 派发，并补一条 app 级契约测试：用**包内可见**的 bus 访问器捕获
  `ECHO_DISAPPEARED`，断言「第 1 轮残影在第 2→3 轮边界被淘汰时恰好派发一次，`sourceRound = 1`、
  `tick = durationTicks - 1`」，且后续轮次不重复派发；
- **残留（另开）**：`restartFromFirstRound` 会清空全部残影，此时也应释放它们占住的板（`OCCUPANCY_RELEASED` 口径）
  —— 第一关用不到，登记为 L≥2 关卡前的待办；
- 本卡 C 部分（R5-B 聚合）继续按原计划推进。

---

## 八、B 部分结案（PM，2026-09-13）

开发二 B 部分（`sourceRound` 语义一页结论）已提交，**PM 采纳并正式结案：不单独发卡**。

**采纳的结论**：`GameEvent.sourceRound` 维持**二值语义** —— `0` = 活玩家（`player`）、`N ≥ 1` = 第 N 轮残影
（`echo_<N>`）；占用/释放判定一律看 **actorId**，`sourceRound` 只是残影侧的轮次标签。
把活玩家改成「当轮轮次」**无功能收益**（不改变任何占用判定，却让“活玩家”失去稳定锚点），
且改动落在 `app/**` + `mechanism/**`（跨板块破坏性），而 `replay/**` 无需变动 → **不改**。

**PM 追加一条不变量（请机制侧补一行 javadoc）**：`GameEvent.sourceRound` 的注释里写明
「**0 = 活玩家（不得替换为当轮轮次）；N ≥ 1 = 第 N 轮残影，且必须与 actorId `echo_<N>` 一致**」——
`GameEvent` 位于 `mechanism/event/**`（开发三路径），已并入开发三 Phase 2 的文档小项，避免将来被“顺手修正”。


