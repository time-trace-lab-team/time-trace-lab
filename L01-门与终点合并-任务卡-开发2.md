# 任务卡 · 第一关「门与终点合并」· 快照与恢复（开发二）

> 任务 ID：**L01-GATE-MERGE-DEV2**
> 主责：**开发二**（`snapshot/**`、`replay/**` 的只读核对 + 其测试）｜ 依据：`L01-门与终点合并-PM裁决.md`
> 基线：`origin/develop` = `3e0778d`（PM clean 复跑 381/0/0）
> 状态：**开工**（与开发一并行；**不需要新字段**，见 §一）
> 日期：2026-09-13

## 一、本卡为什么存在（范围很小，别扩面）

闸门与终点合并、右板改为开关之后，**状态组合**变了（同一格上「门是否解锁」与「终点是否可结算」必须一致，
开关的占用同时驱动「板占用」与「门的解锁条件」）。这些状态**都已在既有冻结契约内**：

- 开关占用 → `AutoDockStateSnapshot(List<DockSnapshot(mechanismId, occupancy, reentryBlockedAtTick)>)`（R5-B §二 #4）
- 门 → `Door.Snapshot(mechanismId, state)`（#2）
- 终点 → `ExitTerminal.Snapshot(mechanismId, doorUnlocked, triggered)`（#3）

→ **不需要新字段、不需要改 `R5-B` 冻结**。本卡要做的是**证明恢复路径不会出现不一致状态**，
并在必要时补测试（不是补接口）。

## 二、交付物

1. **一致性核对（书面结论）**：逐一回答——
   - 恢复顺序中 `Door` 与 `ExitTerminal` 的恢复先后是否会造成「终点已解锁但门仍 `LOCKED`」（或反之）的中间态？
     若有，说明聚合器 `apply` 内的顺序是否已消除该中间态，并给出代码位置；
   - 开关（`plate` 变体）的占用恢复后，门的 `state` 是否会被**重新计算**？如果会，是否需要重算而不是照抄快照
     （照抄会与占用事实矛盾）？给出你的判断与依据。
2. **测试（`src/test/java/.../snapshot/**`）**：
   - `restoreUnlockedGateStillAllowsExitInteraction`：恢复到「门已解锁 + 终点未触发」的快照后，
     `exit.interact(...)` 返回 `true`；
   - `switchReleaseReLocksGateAfterRestore`：恢复到「开关被占、门解锁」后，让开关释放，
     门回到 `LOCKED` 且终点不再可结算（**非锁存语义**在恢复路径上同样成立）；
   - 若 §二.1 发现需要重算：补一条「照抄快照会导致矛盾状态 → 聚合器必须重算」的反例测试。
3. 若结论是「现有实现已正确」，也必须把 §二.1 的两问写成文档结论（**不允许只写「测试通过」**）。

## 三、允许 / 禁止路径

**允许**：`src/test/java/org/example/timeloop/snapshot/**`、`docs/development/开发2/**`。
若确有必要，`src/main/java/org/example/timeloop/snapshot/**`（**仅**在聚合器内调整恢复顺序，不得新增字段）。

**禁止**：`app/**`（PM）、`mechanism/**`（开发三）、`level/**`、`render/**`、`replay/**`、`pom.xml`、`README.md`。
**不得**改动 `R5-B` 冻结的签名或字段语义（任何增减 = 停手回报）。

## 四、验收

1. 两条测试通过（或「实现已正确」的文档结论 + 反例测试）；
2. `.\mvnw.cmd -o clean test` 退出码 0、**任何单测 ≤ 1 s**、数字回填（基线 381）；
3. `git diff --check` 干净；
4. 与 R5-B 聚合器卡（`R5-B-聚合器实现-任务卡-开发二.md`）**不冲突**：若两张卡都要动 `snapshot/**`，
   以本卡为**前置**（先证明一致性，再在聚合器卡里做实现）。

## 五、证据与回传

```
branch: codex/l01-gate-merge-dev2
SHA:    <40 位>
一致性结论: Door/Exit 恢复顺序 → <…>；门态是否重算 → <…>
测试: <测试名 1>、<测试名 2>
clean test: ? / ? / ? / ? ；最慢单测 <名称> <秒>
```

**交付以远端提交为唯一凭据**；禁止 `git add -A`，精确白名单提交。

## 六、停止条件

1. 结论是「必须新增/修改冻结字段」才能保证一致 → 停手回报 PM（重开冻结需 PM 裁决）；
2. 发现 `mechanism/**` 侧行为缺陷（例如门不解锁）→ 停手回报，转开发三；
3. 单测 > 1 s 且无法解释 → 停手回报。
