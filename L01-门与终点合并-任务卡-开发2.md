# 任务卡 · 第一关「门与终点合并」· 快照与恢复（开发二）

> 任务 ID：**L01-GATE-MERGE-DEV2**
> 主责：**开发二**（`snapshot/**` + 其测试）｜ 依据：`L01-门与终点合并-PM裁决.md`（含 §十/§十一 追加裁决）
> 基线：`origin/develop` = `3e0778d`（PM clean 复跑 381/0/0）
> 状态：**待 PM 冻结锁存字段后开工**（前置：`L01-GATE-MERGE-DEV3` §2.3 字段提案 → PM 冻结）
> 日期：2026-09-13（2026-09-14 修订：锁存开关引入一个**已批准的最小字段增补**）

## 一、本卡范围（比第一版大一点，但只涉及一个字段）

开关**锁存**（踩上即 ON、离开不关闭、轮末 `reset()`）引入了**新状态**，它不在 R5-B 已冻结的字段内：

- 开关占用 → `AutoDockStateSnapshot(List<DockSnapshot(mechanismId, occupancy, reentryBlockedAtTick)>)`（R5-B §二 #4）
- 门 → `Door.Snapshot(mechanismId, state)`（#2）
- 终点 → `ExitTerminal.Snapshot(mechanismId, doorUnlocked, triggered)`（#3）

**PM 冻结（2026-09-14，按开发三实际交付改判）**：字段**不在** `AutoDockStateSnapshot.DockSnapshot`，
而在 **`DockingPlate.StateSnapshot` 的第 5 分量 `latched`**（`DockingPlate.Snapshot` 增 `default boolean isLatched()`）：

| 项 | 冻结值 |
| --- | --- |
| 字段 | `boolean latched`（`mechanism/DockingPlate.StateSnapshot`） |
| `reset()` | `false`（OFF） |
| `restore()` | 取快照值；**无锁存位的旧 4 参快照等价 `false`** |
| 不变量 | `latched ⟹ latching`；与 `state` 独立（`(UNOCCUPIED, latched=true)` 是常态）；本轮单调 `false → true`；与 `reentryBlockedAtTick` 无耦合 |

**理由（采纳开发三）**：锁存是**机关侧状态**，autoDock 侧继续只表达「真实占用 + 防重入」，**避免第二个真相源**。
`Door` 与 `DockingPlateRegistry` **零改动**：开关的 `isOccupied()` 语义已扩展为「占用 ∨ 已锁存」。

**你（开发二）现在必须做的**：把 `MechanismSnapshot` 往返改为使用 **5 参构造器并透传 `isLatched()`**——
否则「已触发但人已离开」的开关在快照往返后会**丢成 OFF**。
除这一处外，不得再改冻结面。

## 二、交付物

1. **按 PM 冻结后的字段实现覆盖**：
   - `validate` 阶段校验：`latched=true` 时该机制必须是 `latching` 的板（`role=switch`），
     不允许「非开关机关带锁存位」；
   - `apply` 阶段覆盖该字段；`reset(ROUND_END/FULL_RESTART/SCENE_EXIT)` 路径下开关回到 `OFF`。
2. **一致性核对（书面结论，必须回答）**：
   - `Door` 与 `ExitTerminal` 的恢复先后是否会造成「终点已解锁但门仍 `LOCKED`」的中间态？若有，说明聚合器
     如何消除，并给出代码位置；
   - 恢复到「开关锁存 ON、门 `UNLOCKED`」后，门态是否会被**重算**（照抄快照 vs 由占用重算）？给出判断与依据；
     **注意**：锁存 ON 时占用可能已为空（玩家已离开开关），此时「重算」会错误地把门锁回去 —— 必须避免。
3. **测试（`src/test/java/.../snapshot/**`）**：
   - `restoreLatchedSwitchKeepsGateUnlocked`：恢复到「开关锁存 ON（占用为空）+ 左板被残影占用 + 门解锁 +
     终点未触发」后，`exit.interact(...)` 返回 `true`；
   - `roundEndResetClearsLatchedSwitch`：`ROUND_END` / `FULL_RESTART` 后开关回到 `OFF`、门回到 `LOCKED`；
   - `restoringNonSwitchLatchedFlagIsRejected`：对非开关机关设 `latched=true` 的快照，`validate` 必须拒绝
     且**世界零变化**（沿用半恢复防护范式）；
   - 若 §二.2 的结论是「会重算」，补一条反例测试证明照抄快照会产生矛盾状态。
4. 与 `R5-B-聚合器实现-任务卡-开发二.md` 的关系：**本卡是它的前置**（先证明一致性语义），
   之后在聚合器卡里完成 `validate/apply` 全量实现。

## 三、允许 / 禁止路径

**允许**：`src/main/java/org/example/timeloop/snapshot/**`（**仅**为覆盖已冻结的锁存字段）、
`src/test/java/org/example/timeloop/snapshot/**`、`docs/development/开发2/**`。

**禁止**：`app/**`（PM）、`mechanism/**`（开发三：锁存状态归它）、`level/**`、`render/**`、`replay/**`、
`core/**`、`pom.xml`、`README.md`；不得自行新增/改名字段（字段以 PM 冻结为准）。

## 四、验收

1. 三条（或四条）测试通过；§二.2 两个问题有成文结论；
2. `.\mvnw.cmd -o clean test` 退出码 0、**任何单测 ≤ 1 s**、数字回填（基线 381）；
3. `git diff --check` 干净。

## 五、证据与回传

```
branch: codex/l01-gate-merge-dev2
SHA:    <40 位>
冻结字段: <PM 冻结后的字段名与语义>
一致性结论: Door/Exit 顺序 → <…>；门态重算 → <…>
测试: <测试名逐条>
clean test: ? / ? / ? / ? ；最慢单测 <名称> <秒>
```

**交付以远端提交为唯一凭据**；禁止 `git add -A`，精确白名单提交。

## 六、停止条件

1. 字段未冻结（开发三提案未落地 / PM 未冻结）→ **等**，不要先写实现；
2. 结论是「必须改动已有冻结字段的语义」→ 停手回报 PM；
3. 发现 `mechanism/**` 侧锁存行为缺陷（例如离开即关闭）→ 停手回报，转开发三；
4. 单测 > 1 s 且无法解释 → 停手回报。
