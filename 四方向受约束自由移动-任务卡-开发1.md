# 任务卡 · 四方向受约束自由移动（开发一）【最新版 · 待执行】

> 任务 ID：**C-PLAYER-MOVE-01**
> 所属板块：**开发一**（core / entity / render + 对应测试）
> 状态：**待执行**（前置已全部解除，可直接开工）
> 依据：`README.md`（已修订：§一/§二/§三/§四/§六/§七/§九/§十一/§十三/§十五/§十六/§十七/附录A）、`开发1-基础引擎与玩家-技术指南.md`
> 日期：2026-09-10

---

## 一、已批准的变更决定（前置已解除）

| 决定 | 结果 |
| --- | --- |
| **README 修订** | ✅ 已完成并提交：`35a4442 docs(readme): 需求变更——恒速巡行改为四方向受约束移动`（§三 小节已改名，并留「需求变更记录」） |
| **约束模型** | ✅ **选项 2**：约束在**正交路径中心线**，但移动**由玩家驱动**（保留 `LevelGeometry` / 路径图 / autoDock 节点语义） |
| **静止状态取值** | ✅ **新增 `core/MovementState.IDLE`**（语义清晰，不与 `DOCKED` 混用） |
| **同刻按住相反方向键** | ✅ **停止**（角色静止并记 `IDLE`；不取"末位胜出"） |

---

## 二、实测问题与代码归因（本卡要消除的 4 条）

| # | 现象 | 现实现归因 |
| --- | --- | --- |
| 1 | 角色**自动移动** | `entity/PatrolController.advance()` 每刻无条件前进 `config.baseSpeed()` |
| 2 | 按方向键**有延迟** | 输入只进单槽队列，**到节点锁定区（≈7.2）才提交** |
| 3 | 拐角**不操作也自动寻路** | `core/path/PathExitSelector` 五级优先级自动决策 |
| 4 | 按一个方向**一直走到无路** | ①+③ 叠加，直到 `defaultExit`/死路自动返回 |

---

## 三、目标行为（最终规则）

1. **按住方向键才移动**：按住即以固定 `baseSpeed` 沿该方向推进；**松开全部方向键立即停止**（`MovementState.IDLE`，保留位置与朝向）。
2. **转向同刻生效**：任意时刻改按另一方向立即改向，**不需要到路口**、不需要排队。
3. **不自动寻路**：到达路口/拐角只有玩家输入才转向；无输入即静止。删除五级优先级、`defaultExit` 自动行进、死路自动返回。
4. **遇阻停下**：前方为内墙、边界墙或关闭门时**停下**（`IDLE`）等待改向；不得顶墙停留抖动、自动绕行或自动掉头。
5. **同刻相反方向 → 停止**（保持方向不变或维持上一有效朝向，由实现择一并写测试）。
6. **受约束**：角色始终吸附在路径中心线上，不得脱离路径或穿墙/穿门。
7. **玩家主动转向只接受直行或 90°**；不允许原地掉头。

## 四、必须保留（不得回归）

| 保留项 | 落点 |
| --- | --- |
| 固定轮长 + 唯一读秒 + 自动轮次切换 | `replay/RoundClock`、`RecordingSession`（不动） |
| **位置快照残影**（每 tick 一帧、逐刻索引回放、帧索引==tick） | `replay/PlayerFrame`、`TimelineRecording`、`EchoState`（不动） |
| **autoDock 自动驻留** | 进入区域 → `DOCKED` + 激活 + 清旧输入 + 快照按住键；**新方向边沿**才离开并恢复速度（`entity/C3DockController` 不动） |
| E 交互缓冲（6–10 tick） | `entity/C3DockController`（不动） |
| 相位下潜 `Space`（30/45 tick） | C4（本卡不实现，只确保不回归） |
| 渲染只读、坐标变换 | `render/**`（仅跟随新位置语义，不得用视觉补偿玩法坐标） |

## 五、允许 / 禁止修改路径

- **允许**：`src/main/java/org/example/timeloop/core/**`、`entity/**`、`render/**`；对应 `src/test/java/.../{core,entity,render}/**`；`docs/development/开发1/**`。
- **禁止**：`pom.xml`、`app/**`、`replay/**`、`snapshot/**`、`mechanism/**`、`level/**`、`ui/**`、`persistence/**`、`audio/**`、`src/main/resources/**`。
- 需要上述任一或公共契约变更 → **停止并上报**（见 §九）。

## 六、实施要点

1. **`core/MovementState`**：新增 `IDLE`（放在现有 `CRUISING/SLOWED/DOCKED` 之后，不改既有常量语义）。
2. **`entity/PatrolController`**：
   - `advance(...)` 改为接收本刻**方向输入**（如 `Direction` 或 `Optional<Direction>`）；无输入 → 不推进位置，返回 `IDLE` 帧。
   - 按住方向 → 以 `baseSpeed` 沿该方向推进；路径吸附保留（仅用于消除浮点抖动，不用于选出口）。
   - 删除"到节点自动决策出口"的调用；改为**目标方向合法性判定**（不可通行则忽略输入并保持 `IDLE`）。
   - 到达节点中心时的吸附与转向提交：仅当玩家输入方向在该节点合法时执行。
3. **`core/path/PathExitSelector`**：由"五级自动选路"改为**纯判定**（给定当前节点/朝向/请求方向 → 是否可通行）；移除 `defaultExit` 自动行进与死路自动返回的**选择**语义（`defaultExit` 字段可保留但不再驱动自动移动）。
4. **`entity/C3DockController`**：不动；仅在集成处确保"进入 autoDock 时不因旧按键离开"仍成立。
5. **输入语义**：确认 `core/input/InputIntent` 的 `held` 足以表达"按住走、松开停"；同刻相反方向判为停止；若需补字段，先出提案。
6. **渲染**：`render/**` 只跟随；`IDLE` 帧按静止姿态绘制。

## 七、测试（必须重写 + 新增）

**必须重写**（现断言的是被废止的自动巡行）：
- `entity/PatrolControllerStraightLineTest`：原「无输入巡行 60 刻到 (0,120)」→ 改为「无输入静止不动」。
- `entity/PatrolControllerNodeBehaviorTest`：原「自动选路/直行/死路返回」→ 改为「无输入不转向、遇阻停下」。

**必须新增**：
1. 按住方向移动 / 松开立即停止（`IDLE`）；
2. 同刻改向立即生效（≤1 tick，无延迟）；
3. 到达路口无输入**不自动转向**；
4. 前方内墙 / 边界墙 / **关闭门** / 死路 → 停下（`IDLE`），不绕行不掉头；
5. 同刻按住相反方向 → 停止；
6. 主动掉头被拒绝；
7. autoDock：进入即停驻+激活；按住旧键不离开；新边沿离开并恢复速度；
8. **残影逐刻一致**：同一输入序列录制→下一轮回放，位置/停驻段逐 tick 相同；
9. `IDLE` 段仍逐 tick 写入 `PlayerFrame`（不产生空档）。

## 八、验收条件

1. 无输入 → 完全静止；按住 → 匀速移动；松开 → 立即停止。
2. 方向输入同刻生效，无可感知延迟。
3. 路口/拐角不自动转向；无输入时遇墙/门/死路停下。
4. 按住一个方向只在按键期间移动，不会自动持续。
5. 内墙、边界墙、关闭门不可穿；角色不脱离路径中心线。
6. autoDock 规则与旧版一致（进入停驻、新边沿离开、占用释放）。
7. 固定轮长/读秒/轮次切换、位置快照残影逐刻回放**不回归**（原 243 条测试中除被重写的两条外全绿）。
8. 纯逻辑测试不初始化 JavaFX，`mvn test` 退出码 0。

## 九、跨模块停止条件

- 需改 `app/**`（键位适配/装配）→ 停止，报 PM 开集成任务。
- 需改 `level/**`（瓦片/路径/`LevelGeometry`）→ 停止，报 PM 找开发三。
- 需改 `replay/**`（帧字段/回放语义）→ 停止，报 PM 找开发二。
- 需新增除 `MovementState.IDLE` 以外的公共枚举/契约 → 先出提案，PM 批准后实施。

## 十、验证命令

```powershell
.\mvnw.cmd test
.\mvnw.cmd javafx:run      # 目标机手工验收：按住走 / 松开停 / 不自动寻路
```

## 十一、影响面与风险

| 风险 | 说明 | 缓解 |
| --- | --- | --- |
| 关卡平衡变化 | 原关卡按恒速巡行设计，自由移动改变可达刻 | 第一关先验；**第二关射线到达刻公式须重算**（PM 排期） |
| 残影一致性 | 移动模型改变 | 位置快照与逐 tick 记录不变；回归测试锁定 |
| 开发三侧（试用/中继停驻） | autoDock 语义不变 | 本卡不动 `mechanism/**` |
| 工作量 | 移动/输入/碰撞重写 + 两条测试重写 | 选项 2 复用 path graph，改动集中在 `entity/` |

---

*本卡为 PM 签发的最终版；开发一按本卡执行，遇 §九 任一即停止上报。*
