# 任务卡 · 自由移动最终验收（测试 / QA）

> 任务 ID：**X-MOVE-COLLAPSE-01-FINAL-ACCEPT**
> 主责：**测试（QA）** ｜ 配合：PM（集成与证据）、开发一（core/entity）、开发三（mechanism/level）
> 状态：**已发卡**，但**有前置**（见 §一），前置未满足前只做准备、不起验收结论
> 日期：2026-09-10

## 一、前置（缺一不可，PM 会在满足时通知）

1. **PR #54**（CORE-3 + APP-1 成对）已合并 `develop`；
2. **PR #55**（ENT-2a + ENT-2b 成对）已合并 `develop`；
3. `X-MOVE-COLLAPSE-01-ECHO-ACTOR`（残影回放 actor 归属）已合并 —— **玩法正确性**，建议纳入本次门禁
   （不修则「残影消失后驻留板永久占用」）；
4. `X-MOVE-COLLAPSE-01-APP-2`（出口宽容半径接线）已合并 —— 不修则「隔空按 E 通关」；
5. 拉取最新 `develop` 后工作树干净：`git status --short --branch` 无未跟踪的源码改动。

## 二、验收矩阵（全部要有可复现证据）

### A. README §十六「四方向移动原型门禁」8 条

逐条给结论 + 证据（操作序列或测试名），重点：
1. 无输入完全静止；按住以固定 `baseSpeed` 移动；松开**同刻**停止；
2. 输入同刻生效、无排队；转向只在**路径节点中心**提交，段中间按垂直方向不转向；
3. 到路口/拐角**不自动寻路**；遇内墙、边界墙或关闭门停下等待改向；**真死路允许原路返回**；任何节点都不得把玩家锁死；
4. 隐藏完整网格后，五名成员 3 分钟内理解「按住走、松开停」与路口菱形，≥4 人认为可预测；
5. 进入驻留板清空旧输入并自动停下；按住到达前的旧键不离开；新按一次方向才出发；
6. `Space` 后相位期间仍以当前速度沿当前方向前进、能穿过射线带不减速；
7. 五人 25 次试玩无时滞通过率 ≥ 80%；
8. `E` 输入缓冲让正常经过终端稳定交互；同一输入序列录制回放后，停驻/离开、相位表现与时滞后的到达时刻一致。

### B. `测试口径更新-C-PLAYER-MOVE-系列.md` §三 的 13 条回归

无输入静止 / 按住走 / 松开同刻停 / 同刻相反方向停 / 前进+垂直不冻住 / 垂直意图在节点中心提交转向 /
真死路可掉头 / 普通直廊节点掉头被拒 / **出生点按反方向不崩** / 连续直行穿 ≥3 节点不挂死 /
驻留板走到机关中心才停驻且离开释放占用 / 残影逐刻复现 / `IDLE` 段仍每刻写帧。

### C. 第一关端到端（自由移动版）

- 至少两轮：第 1 轮玩家占一块驻留板并停驻 → 第 2 轮**残影占住该板**、玩家占另一块 → 门解锁 → 按 `E` 结算；
- 证据可用开发三 `Level01TwoRoundSimulationTest`（`52af111`，headless 模拟）+ app 侧集成测试；
- 明确记录：**单轮不可能同时占两块板**（离开即释放）是设计意图，不是缺陷。

### D. 工程门禁

- `.\mvnw.cmd -o test`：退出码、`Tests run / Failures / Errors`、墙钟耗时；
- **任何单测不得超过 1 秒**（死循环/挂死防线，超时即判回归）；
- `grep` 验收：`case DIR_UP` 仅 `core/input/LogicalKey.java`；`core|entity|app` 无 `defaultExit` 代码引用；
  `turnLockDistance`、`pendingTurn` 归零；`MovementState.SLOWED` 有生产者。

## 三、交付格式

一份《自由移动最终验收报告》，含：结论（通过/不通过）、A/B/C/D 四节逐条结论与证据、
失败项的**责任板块 + 最小复现**、以及是否放行进入批量关卡制作的建议。

## 四、边界

- 测试不改任何 `src/main/**`；发现缺陷写成回填条目（责任方 + 复现 + 期望/实际）；
- 跨模块问题交 PM 裁决，不要自行在两个板块之间“折中”。
