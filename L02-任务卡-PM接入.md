# 任务卡 · 第二关「装配泛化 + 选关入口」（PM / 集成）

> 任务 ID：**L02-A-PM**
> 主责：**PM**（`app/**`、`README.md`、`docs/**`）｜ 依据：`L02-门房与双残影-PM裁决.md`
> 基线：`origin/develop` = **`f8f414c`**（clean 复跑 410 / 0 / 0）
> 状态：**待开发三交出 L2 关卡数据与几何后开工**（可先做 §一.1 的泛化重构）
> 日期：2026-09-14

## 一、交付物

1. **装配泛化**（当前 `Level01Assembly` 与 `TimeTraceLabApplication` 都把第一关写死）：
   - 抽出 `LevelAssembly` 接口（`tick / renderViews / hudContext / objectiveView / pathNodeMarkers / phase /
     start / restart / isFinalPhase / cleanup`），`Level01Assembly` 实现之（**行为与既有测试不得变化**）；
   - 新增 `Level02Assembly`（或参数化装配），持有 L2 的三块板、**两个门**、出口、射线（L2-B）；
   - 装配工厂 `LevelFactory`（或等价物）：按关卡 ID 构建关卡数据 + 装配，供 `app` 选择。
2. **多门阻挡**：`isPassable()` 现在只查一个 `door` → 改为**遍历本关所有门**（任一未解锁且挡在该节点即不可通行），
   并为「两个门互不影响」补一条 app 集成测试。
3. **最小选关入口**：README §四 的 `MENU → LEVEL_SELECT → PLAYING` 目前尚未实现。
   本批只做**最小可玩入口**：主菜单/选关里能选第一关或第二关（键位与视觉从简），
   **不做**存档、设置、关卡进度落盘（§十六 #6 属独立批次）；把"完整菜单体验"记为待办卡。
4. **L2 端到端测试**（`src/test/java/.../app/**`）：
   - 三轮官方链（裁决 §三）在**真实装配**下通关；门外板离开后房门回锁；R3 两条残影同时压板 → 闸门解锁 → `E` 结算；
   - R4 边界：`E1` 消散 + 占用释放，无崩溃、无残留状态；
   - 相位下潜（L2-B 落地后）：未下潜命中只减速、下潜穿过不减速。
5. **README / 文档同步**（与实现同批）：
   - §五 机关表：驻留板与普通门「使用关卡」加**第二关**；
   - §六 第二关行：描述改为「短射线走廊 + 门房链」，参数 `20 s / L=2 / maxRounds=4`；
   - §十三 时间线：第 3 天改为「L2-A 门房链竖切片」，射线走廊（L2-B）排到第 4 天前段；
   - §十六：新增一条「L2 双残影因果链」验收项；
   - `docs/decisions/**` 归档本裁决。

## 二、允许 / 禁止路径

**允许**：`src/main/java/org/example/timeloop/app/**`、`src/test/java/org/example/timeloop/app/**`、
`README.md`、`docs/**`。

**禁止**：`render/**`、`entity/**`、`core/**`、`level/**`、`mechanism/**`、`replay/**`、`snapshot/**`、`ui/**`、`pom.xml`；
发现问题转对应单板块卡，不在别人路径上顺手改。

## 三、验收

1. L1 既有测试**全部不变更地通过**（装配泛化是纯重构：不得为了 L2 改 L1 行为）；
2. L2 端到端三轮通关；两个门互不影响；R4 边界无残留；
3. `.\mvnw.cmd -o clean test` 退出码 0、**任何单测稳定 ≤ 1 s**、数字回填（基线 410）；
4. JavaFX 人工验收：能选到第二关；房门开/关、三块板占用、闸门开/关、两条残影代际可辨认（截图存档）；
5. README §五/§六/§十三/§十六 与实现一致；`git diff --check` 干净。

## 四、证据与回传

```
branch:  codex/l02-a-pm
SHA:     <40 位>
L1 回归:  <测试数字，须与 410 基线可比>
L2 E2E:  <用例名 + 三轮刻表>
选关入口: <如何进入第二关>
人工验收: <逐条结论 + 截图文件名>
README:  §五 / §六 / §十三 / §十六 改动摘要
```

## 五、停止条件

1. 装配泛化**要求改 L1 行为或 `core/**` 契约** → 停手回报，先出重构小提案；
2. 开发三的几何证据不满足裁决 §三 的 5 条硬约束 → 停手回报，退回关卡侧；
3. L2 端到端测试暴露「无解」（例如 R2 玩家来不及进房）→ 停手回报，这是关卡侧问题；
4. 单测稳定 > 1 s 且无法解释 → 停手回报。
