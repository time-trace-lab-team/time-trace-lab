# 任务卡 · 第二关「低姿穿行 + 门房」（开发三）

> 任务 ID：**L02-A-DEV3**（门房链）/ **L02-B-DEV3**（射线段，后置）
> 主责：**开发三**（`mechanism/**`、`level/**`、`ui/**`）｜ 依据：`L02-门房与双残影-PM裁决.md`
> 基线：`origin/develop` = **`f8f414c`**（clean 复跑 410 / 0 / 0）
> 状态：**L2-A 开工**；**L2-B 等 L2-A 合入**（裁决 §五）
> 日期：2026-09-14

## 一、目标（L2-A）

给第二关造出「**门 + 三块压力板 + 双残影**」的因果链房间：门由门外板控制，
终点闸门由内板 + 主驻留板**同时**占用解锁，官方解三轮成链（裁决 §三）。

## 二、交付物（L2-A）

1. **关卡数据 `level/Level02Corridor.java`**（新，命名可与你既有风格统一）：
   - 三块 `dock_plate`（`autoDock=true`）：`L02_plate_door`（门外）、`L02_plate_inner`（房内）、
     `L02_plate_main`（房外主任务板）；
   - **两个门**：`L02_door_room`（`requiredPlateIds={L02_plate_door}`，锁着时挡房间入口节点）、
     `L02_door_exit`（`requiredPlateIds={L02_plate_inner, L02_plate_main}`，**与出口同格**，同 L1 的合并做法）；
   - 出口 `L02_exit_00`，`associatedDoorId = L02_door_exit`；
   - 参数按裁决 §四：`durationTicks = 1200`、`maxRounds = 4`、`echoLifeL = 2`；
   - 出生点、路径图、内墙与房间几何自定，但必须满足裁决 **§十一（追加裁决，取代原 §三）** 的约束集合：
     **1′（锁门只挡房间）／2（闸门与内板 > 72）／3′（窗口时长 ≥ 房门内侧邻点→内板走行刻 + 余量）／
     5（射线不覆盖驻留点，L2-B）／6（进房即承诺，HUD 提示）／7（门外板与主驻留板不得相邻，建议 ≥ 3 格）**；
     **房间不设旁路**（房门是唯一入口，这是本关教学点）。
   - **前置证据（PM 已审：裁决 §十三，有条件批准）**：时间表数字已全部复核通过，但**必须改一处几何** ——
     **R2 的进房路线不得经过门外板**（否则 E1 与玩家在刻 216 争抢门外板，写入顺序不可控，最坏情况房门同刻回锁 → R2 无解）。
     改法：门外板留在第 6 列（R1 路线），R2 改走**另一列**（如第 7 列）从**南侧**进入房门；
     R2 到**房门南邻点**须**早于窗口开始**（可等待，不追赶）；跨门刻与窗口起止**各留 ≥ 30 刻余量**；
     改格数后**重算并回传**受影响的刻表行。
2. **几何与可解性测试**（`src/test/java/.../level/Level02*.java`）：
   - 上述约束**逐条断言**（尤其：删房门节点后 `spawn→门外板` / `spawn→主驻留板` / `spawn→闸门` 仍连通；
     闸门与内板 > 72；窗口时长 ≥ 走行刻）；
   - **三轮链 headless 模拟**（照裁决 §十一.3 官方解，**注意 R1 不进房**）：
     R1 门外板窗口 + 主驻留板 → R2 借窗口进房 + 内板驻留到轮末 → R3 `E1`（门窗口 + 主板）+ `E2`（内板）
     → 内板与主驻留板同刻被占 → 闸门解锁 → 出口 `interact()` 为 `true`；
   - **反例**：①只压内板或只压主驻留板 → 闸门不解锁；②**R1 单人无法穿过房门**（写成显式测试：
     单人踩门外板后离开 → 房门同刻回锁 → 房门格不可通行）；③故意压短窗口 → R2 进不了房（证明"窗口不足即无解"）；
   - **裁决 §十三.3 的三条补充测试（必做）**：
     `r2EntryRouteDoesNotIntersectOuterPlate`（R2 路线与门外板节点不相交）／
     `sameTickPlateContentionOlderEchoWins`（R3 刻 216 附近 E1/E2 争抢门外板 → **较旧者胜**，E2 位置回放不受影响）／
     `r2PassesDoorOnlyInsideWindow`（R2 跨门刻 ∈ `[a+30, b−30]`；并明确记录"玩家经过门外板是否停驻"的实际行为，
     两种分支都必须有确定结论）；
   - **房门回锁**：离开门外板后房门立刻回锁（门外板**不锁存**，与 L1 开关的锁存语义相反，务必写清测试名以免后人混淆）；
   - **进房即承诺**：R2 玩家进房后直到轮末无法离开，且不崩溃、不残留状态。
3. **HUD / 文案（`ui/**`）**：
   - 第三轮场景下，HUD 需能表达「两块板分别由哪一代残影压住」（README §三 的残影代际：由新到旧）；
   - **必须新增两句提示**：R1 提示「门外板要踩得够久，为下一轮留门」；R2 进房后提示「需驻留到本轮结束」（约束 6）；
   - 字段扩展由你决定，但**不得**破坏既有 `ObjectiveViewModel` 的构造调用方
     （PM 侧 app 会同步；如需改构造，先在卡内写明新签名）。
4. **规格文档**：`docs/development/开发3/**` 追加「第二关判定链与参数」一节（含官方解、约束集合、**时间表结论**）。

## 三、交付物（L2-B，等 L2-A 合入后再做）

1. 在 L2 关卡数据里加入**一段短射线走廊**（一束低风险时滞射线），位置使玩家**去程必须处理它**；
2. **把 `mechanism/ray/Ray.java` 接成注入端口**（`GameEventBus`，只读共享 `roundTick`）：
   **不得**使用 `DockingPlateRegistry.getInstance()` / `EventDispatcher.getInstance()` 兼容单例；
3. 射线**不需要快照端口**（无持久状态，R5-B §10.2 结论保留）→ 不要在聚合器里加具体类旁路；
4. 射线周期与预警公式按 README §三；相位下潜 `PHASED` 与减速 `SLOWED` 复用现有实现，**不新增机制类型**；
5. 测试：未下潜命中只写减速不死亡；下潜期间穿过不减速；L2 正式解法相位输入 ≤ 2 次（§十六 #11）。

## 四、允许 / 禁止路径

**允许**：`src/main/java/org/example/timeloop/{level,mechanism,ui}/**`、
`src/test/java/org/example/timeloop/{level,mechanism,ui}/**`、`docs/development/开发3/**`。

**禁止**：`app/**`（PM）、`render/**`（开发一）、`snapshot/**`、`replay/**`、`core/**`、`pom.xml`、`README.md`；
**不得**新增机关类型/事件类型；**不得**修改 `Door`/`ExitTerminal`/`DockingPlate` 的**公共契约**
（L2 只需多实例，不需要改签名）。

## 五、验收

1. 5 条硬约束逐条有证据；三轮链模拟 + 反例 + 回锁测试全部通过；
2. `.\mvnw.cmd -o clean test` 退出码 0、**任何单测稳定 ≤ 1 s**、数字回填（基线 410）；
3. `git diff --check` 干净；
4. 交付文档回传：分支 / SHA / L2 几何事实（房门与各板坐标、距离、通路长度）/ 三轮刻表。

## 六、证据与回传

```
branch: codex/l02-a-dev3
SHA:    <40 位>
关卡参数: durationTicks=1200 / maxRounds=4 / echoLifeL=2
硬约束 1–5: <逐条一行证据>
三轮刻表: R1 <关键刻> / R2 <关键刻> / R3 <关键刻>
clean test: ? / ? / ? / ? ；最慢单测 <名称> <秒>
```

**交付以远端提交为唯一凭据**；禁止 `git add -A`，精确白名单提交。

## 七、停止条件

1. 需要改 `Door`/`ExitTerminal`/`LevelData` 的公共契约才能实现 → 停手回报 PM（本批按"零契约变更"发卡）；
2. 5 条硬约束**无法同时满足**（例如房门必然是割点）→ 停手回报 PM 重裁几何，不要自行放宽；
3. L2-B 发现必须给射线加状态/端口 → 停手回报（R5-B 结论要重开，属契约变更）；
4. 单测稳定 > 1 s 且无法解释 → 停手回报。
