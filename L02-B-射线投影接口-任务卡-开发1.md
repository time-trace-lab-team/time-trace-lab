# 任务卡 · L02-B 射线只读投影接口 `RayBeam` / `RayVisualState`（开发一实现）

> 任务 ID：**L02-B-RAY-PROJECTION-IFACE**
> 主责（实现 `render/**`）：**开发一** ｜ 消费/投影（`app/**`）：**PM** ｜ 权威状态来源：**开发三**（`mechanism/ray/**`，只读）
> 依据：开发三「需要 PM 补充的最小接口」请求 + `L02-门房与双残影-PM裁决.md` §五/§六（L2-B）
> 基线：`origin/develop` = **`31920dc`**（PM clean 复跑 **454 / 0 / 0**）
> 状态：**PM 已批准并冻结形状**；开发一实现后，PM 接投影、开发一接 B1 渲染
> 日期：2026-09-14

## 一、PM 复核：**批准**，且**不需要改 `mechanism/**`**

已核 `mechanism/ray/Ray.java` 的公开面**已经够用**：`getId()`、`getStart()`、`getEnd()`（世界坐标 `Vector2D`）、
`getState()`（`Ray.State { OFF, WARNING, ACTIVE }`），`RayFactory.buildFrom/updateAll` 也在。
⇒ 本批**只加一个只读 DTO 与 `Frame` 的一个分量**，`mechanism/**` 零改动。

## 二、冻结接口（**逐字实现，不得增删字段**）

```java
/** 射线的一个只读渲染投影。坐标一律为【世界坐标】，由图层自行套 WorldTransform。 */
public record RayBeam(String id,
                      double startX, double startY,
                      double endX, double endY,
                      RayVisualState state) {
    public RayBeam {
        // id 非空非空白；四个坐标必须有限；state 非 null；否则抛异常（构造即校验）
    }
}

/**
 * 射线的可视状态。与 {@code mechanism/ray/Ray.State} 一一对应，
 * 但 render 层【不得】依赖 mechanism（分层）；映射由 app 侧完成。
 */
public enum RayVisualState { OFF, WARNING, ACTIVE }
```

`RenderViews.Frame` 增加**第 4 个分量**：

```java
public record Frame(Player player, List<Mechanism> mechanisms, List<EchoTrail> echoes,
                    List<RayBeam> rays) {
    // 紧凑构造器：rays 非 null + List.copyOf
    // 兼容重载（必须保留，避免改动既有调用点）：
    public Frame(Player player, List<Mechanism> mechanisms, List<EchoTrail> echoes) {
        this(player, mechanisms, echoes, List.of());
    }
}
```

**硬性要求**：

1. `rays` **按 `id` 升序**存放（快照/离屏回归需要确定性顺序）；
2. **L1 必须恒为空列表**（L1 无射线）⇒ L1 的画面与既有测试**零变化**；
3. `Frame.empty()` 的签名**不变**，内部传 `List.of()`；
4. 兼容重载**必须保留**（现有多处 `new RenderViews.Frame(player, mechanisms, echoes)` 调用点不得被迫修改）；
5. 坐标为**世界坐标**；不得在 `render/**` 里做屏幕偏移（投影由 `WorldTransform` 负责）。

## 三、分工

| 方 | 交付 |
| --- | --- |
| **开发一**（本卡主体） | `render/RenderViews.java`：`RayBeam` + `RayVisualState` + `Frame` 第 4 分量 + 兼容重载 + `empty()` 更新；`render/**` 测试：空列表默认、`List.copyOf` 不可变、按 id 排序、`RayVisualState` 三态在图层上**可区分**（离屏回归，不要断言具体像素值） |
| **PM** | `app/**` 权威投影：`RayFactory.updateAll(rays, roundTick)` → 逐条 `Ray` 映射为 `RayBeam`（**穷尽 switch 三个状态**，不写 `default`，将来 mechanism 加状态要编译期暴露）→ 放进 `Frame.rays` |
| **开发三** | 无需改动（`Ray` 公开面已够）；B1 渲染按本卡接口对接 |

## 四、允许 / 禁止路径

**开发一**：允许 `src/main/java/org/example/timeloop/render/**`、`src/test/java/org/example/timeloop/render/**`、
`docs/development/开发1/**`；**禁止** `app/**`（PM）、`mechanism/**`、`level/**`、`ui/**`、`replay/**`、`snapshot/**`、`pom.xml`、`README.md`。

## 五、验收

1. `RayBeam` / `RayVisualState` / `Frame` 第 4 分量按 §二 逐字实现；兼容重载与 `empty()` 到位；
2. 既有调用点**一处都不用改**（L1 与既有 render 测试零改动即通过）；
3. 新测试：排序、不可变、三态可辨；
4. `.\mvnw.cmd -o clean test` 退出码 0、数字**不回退**（基线 **454**）、`git diff --check` 干净；
5. 回传：分支 / SHA / 测试数字 / `RayBeam` 与 `Frame` 的最终签名。

## 六、停止条件

1. 发现必须改 `mechanism/**`（例如 `Ray` 没暴露某字段）→ 停手回报 PM，**不要**为渲染去改机关；
2. 发现必须改既有 `Frame` 调用点才能编译 → 说明兼容重载没做对，按 §二.4 修正，**不要**去改 `app/**`；
3. 单测中位数相对基线恶化 > 50% → 停手回报（口径见裁决 §十二.2：≥3 次复跑取中位数，且不得与 JavaFX 实跑并行）。
