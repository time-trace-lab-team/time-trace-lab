# 任务卡 · 第一关「门与终点合并」· 关卡数据与可解性（开发三）

> 任务 ID：**L01-GATE-MERGE-DEV3**
> 主责：**开发三**（`level/**`、`mechanism/**` 注释订正、`ui/**` 文案）｜ 依据：`L01-门与终点合并-PM裁决.md`
> 基线：`origin/develop` = `3e0778d`（PM clean 复跑 381/0/0）
> 状态：**开工**（本卡是整批的前置：先定节点，开发一/二再并行）
> 日期：2026-09-13

## 一、目标

把第一关的「两块驻留板 → 远处门 → 终点」改成**一个闸门终点**：闸门与终点同格，旁边是开关。

## 二、交付物

1. **新闸门节点**：`Level01Footsteps.buildDoors()` 的门位置与 `buildEntities()` 的 `L01_exit_00` 位置
   改为**同一个新节点坐标**，并同时满足 PM 裁决 §四 的 6 条硬约束：
   ①≠ (19,8)；②是既有路径节点；③与开关中心 (888,408) 距离 **∈ (0, 72]**；④**不与开关同格**；
   ⑤不是「出生点 → 左板」「出生点 → 开关」的唯一通路（割点）；⑥第 1 轮可规划路线内可达。
   - 候选（PM 实测）：`c17_r8 / c18_r7 / c18_r9 / c20_r8`（48）、`c17_r7 / c19_r7 / c17_r9 / c19_r9`（67.9）；
   - **交付文档里必须写**：选了哪个节点 + 一条一句的 ⑤⑥ 证据（例如路径图上把该节点删掉后
     `出生点 → 左板`/`出生点 → 开关` 仍连通）。
2. **右板改为开关**：`L01_plate_right`（cells (18,8)）加 `putProp("role", "switch")`；
   **ID 不变**、`autoDock` 不变、`Door.requiredPlateIds` 保持 `{L01_plate_left, L01_plate_right}`。
   **不新增机关类型、不新增事件类型**（PM 裁决 §三）。
3. **订正过期注释**：`mechanism/ExitTerminal.java` 中 `DEFAULT_INTERACT_RADIUS` 的 javadoc
   仍写「右驻留板 (8,5) 与出口终端 (9,5)」→ 改为与当前数据一致（并说明该常量只是兼容下限，
   第一关实际用 `interactRadiusForTileSize`）。
4. **关卡侧测试**（`src/test/java/.../level/**`）：
   - 新不变量测试：`0 < distance(gate, switch) <= 1.5 * tileSize`、`gate != 原终点`、`gate` 是路径节点；
   - 两轮通关模拟（更新既有 `Level01TwoRoundSimulationTest` / `Level01CausalChainTest`）：
     第一轮玩家占左板 → 第二轮残影占左板 + 玩家踩开关 → 门 `UNLOCKED` → 终点 `interact()` 返回 true；
   - **反例测试**：开关离开后门回到 `LOCKED`（证明**不锁存**）。
5. **HUD / 文案**（`ui/**`）：把右板相关文案从「右驻留板」改为「开关」（`ObjectiveViewModel` 字段不变）。

## 三、允许 / 禁止路径

**允许**：`src/main/java/org/example/timeloop/level/**`、`src/main/java/org/example/timeloop/ui/**`、
`src/main/java/org/example/timeloop/mechanism/ExitTerminal.java`（**仅**注释）、
`src/test/java/org/example/timeloop/{level,ui}/**`、`docs/development/开发3/**`。

**禁止**：`app/**`（PM）、`render/**`（开发一）、`snapshot/**`、`replay/**`、`core/**`、
`mechanism/**` 的**行为**改动（除上述注释）、`pom.xml`、`README.md`。

## 四、验收

1. 6 条几何约束**逐条有证据**（测试或文档中的数据 + 图论说明）；
2. 两轮通关模拟通过；反例（不锁存）通过；
3. `.\mvnw.cmd -o clean test` 退出码 0、**任何单测 ≤ 1 s**、数字回填（基线 381）；
4. `git diff --check` 干净。

## 五、证据与回传

```
branch: codex/l01-gate-merge-dev3
SHA:    <40 位>
闸门节点: L01_node_<…> (col,row) = (x,y)；与开关距离 = <…> ≤ 72
割点证据: <一行说明>
clean test: ? / ? / ? / ? ；最慢单测 <名称> <秒>
```

**交付以远端提交为唯一凭据**；禁止 `git add -A`，精确白名单提交。

## 六、停止条件

1. 找不到同时满足 6 条约束的既有节点 → 停手回报 PM（不要新造节点、不要改约束）；
2. 发现必须改 `Door`/`ExitTerminal`/`DockingPlate` 的**行为**才能达成 → 停手回报（本批明令不动公共契约）；
3. 单测 > 1 s 且无法解释 → 停手回报。
