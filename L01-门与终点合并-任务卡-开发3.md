# 任务卡 · 第一关「门与终点合并」· 关卡数据与可解性（开发三）

> 任务 ID：**L01-GATE-MERGE-DEV3**
> 主责：**开发三**（`level/**`、`mechanism/**`、`ui/**`）｜ 依据：`L01-门与终点合并-PM裁决.md`（含 §十/§十一 追加裁决）
> 基线：`origin/develop` = `3e0778d`（PM clean 复跑 381/0/0）
> 状态：**开工**（本卡是整批前置：先定节点与锁存字段，开发一/二再并行）
> 日期：2026-09-13（2026-09-14 按补充卡 §3.6 修订）

## 一、目标

把第一关「两块驻留板 → 远处门 → 终点」改成**一个闸门终点 + 一个锁存开关**：
闸门与终点同格、闸门旁就是开关；开关踩上即开启并保持到本轮结束。

## 二、交付物

### 2.1 新闸门节点（`Level01Footsteps.buildDoors()` + `buildEntities()`）

门位置与 `L01_exit_00` 位置改为**同一个新节点坐标**，满足 PM 裁决 §四 修订后的不变量集合：

| # | 约束 | 判据 |
| --- | --- | --- |
| 1 | ≠ 原终点 (19,8) | 硬 |
| 2 | 是既有路径节点 | 硬 |
| 3 | **≠ 开关格 (18,8)** | 硬（闸门锁着时挡住自己那一格，同格会让人无法站上开关） |
| 4 | **与左驻留板中心 (216,312) 距离 > 1.5 × 48 = 72** | **硬（防「占着左板直接按 E」的单轮通关）** |
| 5 | 不是「出生点 → 左板」「出生点 → 开关」的割点 | 硬 |
| 6 | 第 1 轮可规划路线内可达 | 硬 |
| 7 | 与开关距离（可读性建议） | 软：推荐 ≤ 2 格 |

候选（PM 实测既有节点）：`c17_r8 / c18_r7 / c18_r9 / c20_r8`（距开关 48）、
`c17_r7 / c19_r7 / c17_r9 / c19_r9`（距开关 67.9）。
**交付文档必须写**：选了哪个节点 + 约束 4/5/6 各一行证据（约束 5 用「删掉该节点后两条路线仍连通」证明）。

### 2.2 开关：右驻留板变体 + **本轮内锁存**

- 关卡数据：`L01_plate_right`（(18,8)）加 `putProp("role", "switch")`；**ID 不变**、`autoDock` 不变；
  `Door.requiredPlateIds` 保持 `{L01_plate_left, L01_plate_right}`；
- **锁存语义**（PM 裁决 §十，D1①/D2①/D3/D4）：
  - **踩上（停驻）即触发** → 状态 `ON`；
  - **离开不关闭**，保持 `ON` 直到**本轮结束**；
  - **轮末 `reset()`、FULL_RESTART / 场景退出必须复位为 `OFF`**；
  - **单向**：本轮内触发后不可关闭、不可再次切换；
  - **残影可以触发**（复用 `DOCK_ENTERED`，不新增事件类型）；
  - 实现位置建议：`DockingPlate` 的变体状态（`role=switch` 时启用锁存位），**不要**新建机关类；
- **不得**新增机关类型、不得新增事件类型（README §五/§七 不变）。

### 2.3 快照字段提案（**开发二的前置，必须先给**）

锁存是**新状态**，不在 R5-B 冻结字段内（PM 裁决 §十一）。本卡必须产出字段提案：

- 建议：`DockSnapshot` 增加 `boolean latched`（或论证「可由 `occupancy` 派生且不破坏 `isOccupied` 语义」）；
- 必须写清：①字段名与类型；②`reset()` 时的取值；③与 `occupancy`/`reentryBlockedAtTick` 的交叉不变量；
  ④为何这是**最小**增补。
- **PM 冻结后**开发二才实现；本卡先给提案，不得自行改 `snapshot/**`。

### 2.3.1 三方共用读接口（**PM 指定命名，必须照此实现**）

开发一 render 对接卡（2026-09-14）确认 `RenderViews.Mechanism` 结构不变，开关的 `active` 由 **app 侧**投影：

```java
// Level01Assembly.renderViews()（PM 侧；本卡只需提供被调用的读方法）
new RenderViews.Mechanism(rightPlate.getId(), x, y, RenderViews.MechanismKind.SWITCH,
        rightPlate.isLatched());
```

因此本卡必须提供：

```java
/** 本轮锁存状态：仅 role=switch 的板会置位；普通驻留板恒为 false。reset() 必须清零。 */
public boolean isLatched();
```

- 语义：踩上（停驻）即置 `true`；玩家/残影**离开不清零**；`reset()` 清零；
- **禁止**用 `isOccupied()` 顶替（否则开关离开即弹起，违反 D1/D4）；
- 名称固定 `isLatched()`（注意 `ResonanceStateMachine` 已有同名方法，语义完全不同 → javadoc 必须写清「本类的锁存只到轮末、且与占用无关」）；
- 这是本卡对 app / render 的**唯一**新增读接口；除它以外不要扩大 `DockingPlate` 的公开面。


### 2.4 订正过期注释

`mechanism/ExitTerminal.java` 的 `DEFAULT_INTERACT_RADIUS` javadoc 仍写「右驻留板 (8,5) 与出口终端 (9,5)」，
与当前数据 (18,8)/(19,8) 不符 → 按实际订正（并说明该常量只是兼容下限）。

### 2.5 测试（`src/test/java/.../level/**`、`mechanism/**` 归属开发三）

1. **几何不变量测试**：上表 1–6 条逐条断言（含 `distance(gate, 左板) > 72`）；
2. **锁存语义测试**：踩上 → `ON`；离开 → **仍 `ON`**；`reset()` → `OFF`；重复踩不改变状态；
3. **两轮通关模拟**（更新 `Level01TwoRoundSimulationTest` / `Level01CausalChainTest`）：
   第一轮玩家踩开关并占左板 → 第二轮残影压左板 + 玩家到闸门按 `E` → 结算成功；
4. **反例测试（防单轮通关）**：第一轮玩家占左板后，验证「在左板上无法结算」（闸门不在交互半径内）；
5. **残影触发测试**：残影踩开关同样置 `ON`。

### 2.6 HUD / 文案（`ui/**`）

右板文案改为「开关」，并体现「已开启 / 未开启」；`ObjectiveViewModel` 字段**不变**（结构冻结）。

## 三、允许 / 禁止路径

**允许**：`src/main/java/org/example/timeloop/level/**`、`src/main/java/org/example/timeloop/ui/**`、
`src/main/java/org/example/timeloop/mechanism/DockingPlate.java`（**仅**为 `role=switch` 的锁存状态）、
`src/main/java/org/example/timeloop/mechanism/ExitTerminal.java`（**仅**注释）、
`src/test/java/org/example/timeloop/{level,mechanism,ui}/**`、`docs/development/开发3/**`。

**禁止**：`app/**`（PM）、`render/**`（开发一）、`snapshot/**`（开发二）、`replay/**`、`core/**`、
`pom.xml`、`README.md`；不得改 `Door`/`ExitTerminal` 的**行为**。

## 四、验收

1. 约束 1–6 逐条有断言或证据；候选节点的选择理由成文；
2. 锁存语义 4 条测试 + 两轮通关 + 反例 + 残影触发全部通过；
3. 字段提案成文（PM 才能冻结）；
4. `.\mvnw.cmd -o clean test` 退出码 0、**任何单测 ≤ 1 s**、数字回填（基线 381）；
5. `git diff --check` 干净。

## 五、证据与回传

```
branch: codex/l01-gate-merge-dev3
SHA:    <40 位>
闸门节点: L01_node_<…> (col,row)=(x,y)；与开关距离 <…>；与左板距离 <…> (>72)
割点证据: <一行>
字段提案: <字段名/类型/reset 取值/不变量>
clean test: ? / ? / ? / ? ；最慢单测 <名称> <秒>
```

**交付以远端提交为唯一凭据**；禁止 `git add -A`，精确白名单提交。

## 六、停止条件

1. 找不到同时满足约束 1–6 的既有节点 → 停手回报 PM（不得新造节点、不得放宽约束）；
2. 锁存实现必须改 `Door`/`ExitTerminal` 行为或新增事件类型 → 停手回报；
3. 字段提案需要改动**已有**冻结字段语义（不是新增）→ 停手回报，PM 重裁；
4. 单测 > 1 s 且无法解释 → 停手回报。
