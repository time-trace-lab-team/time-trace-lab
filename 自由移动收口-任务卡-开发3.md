# 任务卡 · 自由移动收口（开发三）

> 任务 ID：**X-MOVE-COLLAPSE-01-DEV3**（父卡 `跨模块契约卡-四方向移动收口-X-MOVE-COLLAPSE-01.md`）
> 所属板块：**开发三**（`mechanism/**`、`level/**`、`ui/**`、`persistence/**` + 对应测试）
> 状态：**可开工**，其中 **L-1 为最高优先**（它卡住开发一的 `ENT-2b`，也卡住第一关双板门闭环）
> 目标：机关/关卡侧与「玩家驱动四方向移动」对齐——停驻离开刻真正按 README 释放、第一关在自由移动下可通关、
> 交互半径与文案不再假设自动巡行。

---

## 〇、开工前必读：交接文档与 origin 不一致（PM 已核对，2026-09-10）

`docs/development/开发3/P-MOVE-ALIGN-继续开发交接文档.md` §5.1 声称以下三项**已完成**，
但在 `origin/develop`（`202e7ad`，含 `8140bdb`）上复核**均不成立**：

| 交接文档声称 | origin 实测 |
| --- | --- |
| `AutoDockOccupancyPort.tryLeave` 已支持合法方向同刻释放（位置可在区域内） | ❌ `AutoDockService.java:177` 仍是 `return result(state, AutoDockResult.Status.NOT_OUTSIDE_REGION, tick);` |
| `LevelGeometry` 不再维护默认出口映射，`Level01Footsteps`/`Level03Footsteps` 已移除旧默认出口配置 | ❌ `LevelGeometryImpl.java:14/32/36/114/191` 仍有 `defaultExitMap`；`level/model/PathNode.java:17` 仍有 `defaultExit`；`LevelGeometry.getDefaultExit` 仍存在 |
| 已删除无引用的 `mechanism/ray/PhaseManager` | ❌ `origin/develop:src/main/java/org/example/timeloop/mechanism/ray/PhaseManager.java` 仍在 |

复核命令（可复现）：

```powershell
git grep -n "NOT_OUTSIDE_REGION" origin/develop -- src/main
git grep -n "defaultExit" origin/develop -- src/main/java/org/example/timeloop/level
git grep -ln "PhaseManager" origin/develop -- src/main
```

**结论**：这些改动看起来只存在于开发三**本地未推送的工作树**（交接文档也提到工作树里有不可清理的既有改动，
且四张 `P-MOVE-ALIGN-前置条件卡-*.md` 在仓库里根本不存在）。因此：

1. **第一步不是写代码，而是 `git push` 你的开发三分支**（含那四张前置条件卡文档），
   由 PM 核对 diff 后决定是合并还是按本卡重做；
2. 在推送之前，本卡 L-1 / L-4 按 **origin 现状**执行（即：确实需要做）；
3. 若本地改动与 origin 冲突（例如 `Level01Footsteps` 的节点数据），以 origin 为基线重做，不要 `reset --hard` 丢证据。

---

## 一、任务清单

### L-1 · 【最高优先】`tryLeave` 放宽为「合法出口同刻释放」（ENT-2a）

- 现状（`develop` `8140bdb` 实测，`mechanism/autodock/AutoDockService.tryLeave`）：

  ```java
  if (!isOwner(state, actorId, sourceRound))            return NOT_OCCUPANT;
  if (exitDirection == null
      || !state.definition.legalExitDirections().contains(exitDirection))
                                                        return INVALID_EXIT_DIRECTION;
  if (state.definition.region().contains(worldPosition)) return NOT_OUTSIDE_REGION;   // ← 要放宽
  clearOccupancy(state);
  state.reentryBlockedAtTick = tick;
  return LEFT;
  ```

- 目标语义（`R5-开工前裁决.md` 裁决 4，README §三「离开驻留型机关时…对应占用同时释放」）：
  **占用者按下合法出口 → 同刻 `LEFT` 并清占用，不要求位置出界**；
  `NOT_OCCUPANT` / `INVALID_EXIT_DIRECTION` / `UNKNOWN_DOCK` 语义保持不变；`NOT_OUTSIDE_REGION` 从正常路径消失
  （保留枚举常量以便兼容，或在文档里标注废弃）。
- 必须补的测试（`AutoDockServiceTest`）：
  ① 区域内 + 合法出口 → `LEFT` 且 `occupancy` 清空、`reentryBlockedAtTick == tick`；
  ② 区域内 + 非法出口 → `INVALID_EXIT_DIRECTION`，占用**不变**；
  ③ 非占用者 → `NOT_OCCUPANT`；
  ④ 释放后同 tick 禁止重入（`SAME_TICK_REENTRY_BLOCKED` 仍成立）。
- 依赖：开发一 `ENT-2b` 在本项进 develop 之前不得开工（否则事件与占用双源不一致）；PM 会在本项合并后通知开发一。
- 交付：合并后请通知 PM，PM 负责 app 侧 `mirrorDockEvents` 与残影回放的 tick 归属校验。

### L-2 · 第一关在自由移动下的通关核查（关卡侧）

- 用冻结的移动参数（`baseSpeed=2`、`tileSize=48`、节点中心吸附、真死路允许掉头）走一遍完整链路：
  出生点 → 分叉 → 左驻留板（停留）→ 离开释放 → 回分叉 → 右驻留板（停留）→ 门开 → 出口终端结算；
- 目标：确认关卡数据（`Level01Footsteps` 的 `allowDirs`/节点布局/门与驻留板位置）**不依赖**已废止的
  「自动选路/默认出口/死路自动返回」；凡有隐性依赖，在本卡范围内修正 `level/**` 数据；
- 交付物：**固定步长模拟或录制证据**（按键脚本 + 到达刻表），写进交付文档；若发现关卡数据需改，附改动前后对比；
- 备注：单轮内无法同时占两块板（离开即释放），所以第 1 轮只能为第 2 轮铺一块板——这是设计意图，
  验收时请按「两轮」构造脚本，不要把它当成缺陷。

### L-3 · 出口终端交互半径（消除 app 的"隔空按 E"）

- 现状：`ExitTerminal.interact(long tick, int sourceRound)` **没有位置参数**，app 只能无条件结算
  （`Level01Assembly.interactIfRequested` 无距离判定）；README §三 要求 `E` 交互使用**宽容半径 + 6–10 tick 输入缓冲**。
- 请二选一并写进交付文档（PM 需确认签名）：
  **A（推荐）**：机制侧提供纯判定 API，例如 `boolean isInInteractRange(Vector2D worldPosition)`（半径常量来自关卡数据或机制配置），
  app 判定通过后再调用 `interact`；
  **B**：`interact(long tick, int sourceRound, Vector2D worldPosition)` 内部做半径判定并在超距时返回 `false`。
- **半径下限（硬约束，否则第一关会变成无解）**：`level/Level01CausalChainTest` 记录的第一关解法是
  「上一轮残影占左板 + 当前玩家占右板 → 门解锁 → **玩家在右板上触发出口**」，
  而右驻留板 (8,5) 与出口终端 (9,5) 相距恰好 1 格 = `tileSize` = 48 世界单位。
  因此半径必须 **≥ 1 格（48）**，建议 1.5 格（72）留出容差；小于 48 会让玩家必须离开右板才能按 E，
  而离开右板会释放占用、门重新锁上（`Door` **不闩锁**：非全占即 `LOCKED`）→ 关卡不可通关。
  请在交付文档里写明所选半径及其与 `tileSize` 的关系，并补一条「从右驻留板中心可按 E 结算」的测试。
- 半径判定必须与残影无关（残影不结算出口），且不引入第二套计时器。

### L-4 · `defaultExit` 与文案清理（LEVEL-0）

- `level.model.PathNode.defaultExit` 与 `LevelGeometry.getDefaultExit` **只保留编写/校验用途**，不得驱动移动；
- `Level01Footsteps` / `Level03Footsteps` 逐个节点核查：路径节点只表达**可通行性与中心线**；
  若 `defaultExit` 已无编写价值，直接删除并同步 `LevelGeometryImpl` 的校验；
- 教程/关卡内文案删除「松键仍巡行 / 自动寻路 / 提前排队」类表述（README §三 已改）；
- 与开发一 `CORE-3`（删 core `PathNode.defaultExit`）的先后：**你只负责 level 侧**；core 侧删除时
  `app/PathGraphBridge` 会红，由 PM 补 `APP-1`，不需要你处理。

### L-5 · 登记但**本卡不执行**：第二关/第五关到达刻重算

- 到达刻只能在 `CORE-2`（速度/`SLOWED`）与 `ENT-2` 落地后，用**固定步长模拟 + 冻结按键脚本**定稿，
  禁止用估算公式回填（README §九）。本卡只登记风险，等 PM 单独发卡。

---

## 二、允许 / 禁止修改路径

- 允许：`mechanism/**`、`level/**`、`ui/**`、`persistence/**`、`audio/**`、对应测试、`docs/development/开发3/**`。
- 禁止：`core/**`、`entity/**`、`app/**`、`replay/**`、`snapshot/**`、`render/**`、`pom.xml`、`src/main/resources/**`。
- 需要改公共签名（L-1 的语义放宽、L-3 的 A/B 选择）→ 先出提案，由 PM 与开发一（消费者）确认。

## 三、验收

1. L-1：`AutoDockServiceTest` 四条新用例通过，`grep -rn "NOT_OUTSIDE_REGION" src/main` 只出现在枚举定义或已标注废弃处；
2. L-2：交付文档含第一关两轮通关的脚本与到达刻表，且门/出口时序自洽；
3. L-3：给出半径 API（A 或 B）与数值，并有一条「超距按 E 不结算」的测试；
4. L-4：`grep -rn "defaultExit" src/main/java/org/example/timeloop/level` 的用途只余编写/校验（或已删净）；
5. `.\mvnw.cmd -o test` 退出码 0，任何单测 ≤ 1 秒。

## 四、依赖与顺序

- **L-1 立即做**（解锁开发一 ENT-2b 与第一关双板闭环）；
- L-2 可与 L-1 并行；L-3 需 PM 确认 A/B；L-4 独立；
- L-5 等 `CORE-2` + `ENT-2`。
