# 三岗前置任务卡（2026-09-10）

> 由项目经理（PM）整理，依据三份岗位交接文档 + 开发指南 + 当前 develop（`181e816`，135 测试全绿）核实。
> 用途：厘清三个开发各自的下一步、缺口前置、由谁补、验收标准，避免继续互相等。

---

## 一、三岗进度与前置缺口核对

### 1.1 开发一（core / entity / render）

| 模块 | 状态 |
| --- | --- |
| C0 固定步长时钟 | ✅ 完成 |
| C1 tick 调度与循环接入 | ✅ 完成 |
| C2 恒速巡行与路径图灰盒 | ✅ 完成（`core/path`、`entity/PatrolController`，已合入 develop） |
| **C3 方向输入 / E 缓冲 / autoDock** | ⬜ **未开工** |

**C3 前置缺口**（逐项核对，已有部分已解除）：

| 前置 | 状态 | 说明 |
| --- | --- | --- |
| `LevelGeometry` + tileSize=48 | ✅ 已就绪 | 开发三已交付，`Level01Footsteps` 的 `TILE_SIZE` 已是 48.0（开发一交接文档写的"仍 64.0"已过时） |
| `TimelineEvent` 正式类型 + 稳定排序键 | ❌ **缺失** | 全仓无此源文件；属开发二职责，见卡 #1 |
| autoDock 区域查询 / 合法出口 / 占用规则 | ❌ 未正式冻结 | 需开发三提供只读查询规格，见卡 #3 |
| `InputIntent` / `DockEvent` 公共契约裁决 | ❌ 待 PM | 见卡 #2 |
| C3 单模块任务卡 | ❌ 待 PM | 见卡 #2 |

### 1.2 开发二（replay / snapshot）

| 模块 | 状态 |
| --- | --- |
| R0–R2（TickContext / RoundClock / PlayerFrame / TimelineRecording） | ✅ 完成，已合入 |
| R3 单残影 + 轮末事务（EchoState / RecordingSession） | ✅ 纯逻辑完成，已合入 |
| R4 双残影 + 整轮寿命（EchoLifetime / EchoQueue） | ✅ 纯逻辑完成，已合入 |
| **R3 接线 / R5 快照事务 / R6 RenderSnapshot** | ⬜ 未开始 |
| **`TimelineEvent`（开发二自己的契约，未交付）** | ❌ 缺失 |

**前置缺口**：

| 前置 | 状态 | 说明 |
| --- | --- | --- |
| `TimelineEvent` | ❌ 自欠 | 指南 §2.3 写明此类型**由开发二定义**、开发一/三产生 —— 不是等别人，见卡 #1 |
| `MechanismSnapshot` | 🟡 已在 develop | 开发三越界所写、只覆盖 3 机关、依赖 mechanism 包，需评审归属，见卡 #3 |
| 稳定机制 ID | ❌ 待开发三 | 事件排序键需要，见卡 #3 |

### 1.3 开发三（mechanism / level / ui）

| 模块 | 状态 |
| --- | --- |
| P1 事件 + 基础机关 | ✅ 完成 |
| P2 统一射线 + 相位 | ✅ 完成 |
| P3 第 3 关 + 寿命教学 | ✅ 完成 |
| LevelGeometry / MechanismSnapshot | ✅ 已交付 |
| **P4 共振 + 第 4/5 关** | ⬜ 未开始 |

**P4 前置缺口**：

| 前置 | 状态 | 说明 |
| --- | --- | --- |
| 开发一 C4（`resonanceAura` 相交判定） | ❌ 未做 | 共振几何判定 |
| 开发一 C5（Canvas 渲染） | ❌ 未做 | 残影/能量线画面 |
| 开发二 `sourceRound` | ✅ 已就绪 | `EchoLifetime.getSourceRound()` |
| 开发二 R6 `RenderSnapshot` 机关状态 | ❌ 未做 | 共振状态显示 |

**开发三自身遗留（卡 #3 必须先处理）**：

1. `replay/EchoLifetimeManager.java` 已进 develop，仍自建 `currentRound++` 计数器（第二套时钟）+ 全局单例；与开发二的 `RecordingSession` + `EchoQueue` 是**两套并行系统**。
2. 开发三交接文档有**大量过时/错误信息**（见 §1.4），需修正。
3. 越权的 `app/Level01Integration`、`app/Level02Integration`、`TimeTraceLabApplication` 改动需清理/还原。

### 1.4 交接文档中发现的问题（PM 需知）

| 文档 | 问题 |
| --- | --- |
| 开发三交接文档 §3.4 / §5.1 | 仍写"等待开发二 R1（TickContext）"—— R1 早已完成并合入，**信息过时** |
| 开发三交接文档 §2.3 / §10 | 称 `EchoLifetime`"供开发二 R4 使用"—— 开发二已有官方版同名类，开发三这版应废弃；"这不是越权而是接口提供"的说法需 PM 裁决 |
| 开发一交接文档 §6.3 | 写"tileSize 仍 64.0"—— 已修复为 48.0，过时 |

---

## 二、三张前置任务卡

### 卡 #1 · 开发二：交付正式 `TimelineEvent` 类型与稳定排序键

| 项 | 内容 |
| --- | --- |
| 任务 ID | R2.5（临时编号，PM 确认） |
| 所属板块 | 开发二（replay） |
| 目标 | 在 `replay/` 定义并测试正式 `TimelineEvent`（不可变 record）与稳定 `Comparator`，解除开发一 C3 的阻塞 |
| 允许修改 | `src/main/java/org/example/timeloop/replay/**`、对应测试 |
| 禁止修改 | `pom.xml`、`app`、`core`、`entity`、`render`、`mechanism`、`level`、`ui`、`persistence`、`audio`、`resources` |
| 必须字段 | `tick`、稳定 actor ID、`sourceRound`、稳定 mechanism ID、事件类型、离开方向/原因、排序键 |
| 至少事件类型 | `DOCK_ENTERED` / `DOCK_LEFT` / `OCCUPANCY_RELEASED`（开发一 C3 所需的边沿事件） |
| 排序规则 | 同 tick：先类别优先级，再稳定 ID（确定性，不依赖集合迭代顺序） |
| 依赖 | 开发三提供稳定 mechanism ID 规则（见卡 #3 第 2 条） |
| 验收 | ① `TimelineEvent` + `Comparator` 落地并带单测；② 排序确定性测试（同输入同顺序）；③ 与开发一确认事件在 `PlayerKinematics` 之前采集、在 `advance()` 前记录 |
| 停止条件 | 缺稳定 ID 规则时只输出字段提案，不虚构 ID |

### 卡 #2 · 开发一：C3 前置核对与 PM 契约裁决（当前只核对，不写码）

| 项 | 内容 |
| --- | --- |
| 任务 ID | C3-门禁核对 |
| 所属板块 | 开发一（core / entity / render），门禁由 PM 牵头 |
| 目标 | 由 PM 签发的 C3 任务卡 + 冻结 `InputIntent` / `DockEvent` 契约，使开发一获得开工许可 |
| 允许修改 | 无代码改动；只产出接口需求清单、核对报告 |
| 禁止修改 | 全部代码（C3 未获准前不写 C3 代码） |
| 开发一需输出 | ① 对 `InputIntent`（逻辑方向、pressed/released/held、E/Space 边沿、WASD↔方向键同义）的字段提案；② 对 `DockEvent` 的事件字段与排序键提案 |
| PM 需裁决 | `InputIntent` 归属、JavaFX `KeyEvent`→`InputIntent` 适配位置（开发一默认不改 `app/`）、同 tick 进入又按键 / 双 autoDock 重叠 / 失焦漏 release 的唯一规则、C1 装配责任 |
| 依赖 | 卡 #1（TimelineEvent）、卡 #3 第 2 条（autoDock 查询 + 稳定 ID） |
| 验收 | PM 签发 C3 任务卡后，开发一方可从 `develop` 建 `codex/dev1-c3` 开工 |

### 卡 #3 · 开发三：清理越权遗留 + 修正陈旧文档 + 冻结稳定 ID 与 autoDock 查询

| 项 | 内容 |
| --- | --- |
| 任务 ID | 开发三-收尾门禁 |
| 所属板块 | 开发三（mechanism / level / ui），跨模块 ID 冻结需 PM 参与 |
| 目标 | 清自身遗留、修正文档、提供开发一/二所需的最小只读查询与稳定 ID 规则 |
| 允许修改 | `mechanism/**`、`level/**`、`ui/**`、`docs/development/开发3/**` |
| 禁止修改 | `replay/**`、`snapshot/**`、`app/**`、`core/**`、`entity/**`、`render/**`、`pom.xml` |
| 必做三项 | ① 废弃 `replay/EchoLifetimeManager.java`（第二套时钟 + 单例），寿命/淘汰改用开发二的 `RecordingSession` + `EchoQueue`（EchoLifetime 官方版已在 develop）；② 修正交接文档过时信息（"开发二 R1 未完成"等）；③ 清理/还原 `app/` 越权文件 |
| 需冻结的规格 | autoDock 区域只读查询（稳定 mechanism ID、区域形状/边界、停驻中心、合法出口、占用释放规则）；全项目稳定 mechanism/actor ID 规则（供卡 #1 排序键） |
| 可并行 | P4 共振状态机（DORMANT→ARMED→LATCHED）与第 4/5 关数据，不依赖开发一 C4/开发二 R6 的部分先做 |
| 验收 | ① 全仓不再有第二套寿命系统（删 Manager 后 135 测试仍绿）；② 交接文档无过时断言；③ autoDock 查询 + 稳定 ID 规格书面冻结 |

---

## 三、关键结论（一句话版）

1. **真正的单点阻塞是 `TimelineEvent`**：开发二欠着没交付，同时卡住开发一 C3。→ 先做卡 #1。
2. **开发三的交接文档严重过时**，会误导协作。→ 卡 #3 第 ② 条必须尽快。
3. **开发三的 `EchoLifetimeManager` 是第二套时钟**，已在 develop 上，拖越久越多人依赖。→ 卡 #3 第 ① 条最优先。
4. **开发一 C3 的多数前置其实已就绪**（LevelGeometry、tileSize 都好了），只等 TimelineEvent + PM 签卡。

*执行顺序建议：卡 #3（开发三清理 Manager + 冻结 ID）→ 卡 #1（开发二交付 TimelineEvent）→ 卡 #2（PM 签 C3 卡）→ 三岗各自进入 C3 / R5 / P4。*
