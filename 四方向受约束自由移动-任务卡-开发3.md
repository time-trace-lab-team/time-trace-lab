# 任务卡 · 关卡/几何/机制随移动模型更新（开发三）【最新版 · 待执行】

> 任务 ID：**P-MOVE-ALIGN**
> 所属板块：**开发三**（mechanism / level / ui + 测试）
> 状态：**待执行**（可与开发一并行，第二关到达刻需等开发一移动参数稳定）
> 依据：`README.md`（已修订：§三 四方向受约束移动、§六 关卡规划、§十六 验收）、`docs/decisions/C4-前置跨界裁决.md`、`docs/decisions/R5-开工前裁决.md`
> 日期：2026-09-10

---

## 一、背景

README §三 由「恒速巡行 + 路口五级自动出口决策」改为**玩家驱动的四方向受约束移动**。核实到开发三侧有**直接依赖旧模型**的地方：`defaultExit` 在 `PathNode`、`LevelGeometry.getDefaultExit`、`LevelGeometryImpl`（构造校验 + 映射）中被使用——它原本只服务于"自动走默认出口"，新模型下**不再参与自动移动**。

## 二、目标

让关卡几何、机关与教学文案在新移动模型下**自洽**，并落地已有裁决的遗留项。

## 三、允许 / 禁止修改路径

- **允许**：`src/main/java/org/example/timeloop/mechanism/**`、`level/**`、`ui/**`；对应测试；`docs/development/开发3/**`；`src/main/resources/levels/**`。
- **禁止**：`pom.xml`、`app/**`、`core/**`、`entity/**`、`render/**`、`replay/**`、`snapshot/**`、`persistence/**`、`audio/**`。
- 需要改 `core/entity`（如移动/枚举）→ **停止上报**（属开发一）。

## 四、实施要点

### 4.1 废弃 `defaultExit`（服务旧自动选路）
- `level/model/PathNode`：`defaultExit` 字段与 `getDefaultExit/setDefaultExit` 标记 `@Deprecated` 并**从自动移动语义中解绑**（可保留字段兼容旧数据，但不再被玩法依赖）。
- `level/LevelGeometry.getDefaultExit(String)`：标记废弃，或改为返回空/移除；同步 `LevelGeometryImpl` 的 `defaultExitMap` 与校验分支。
- **不得**删除"可通行"语义：`PathNode.allowDirs` 与 `LevelGeometryImpl` 的 **1-tile 邻接校验保留**（它现在只用于"该方向是否可走"）。
- `LevelDataValidator`：同步调整对 `defaultExit` 的断言（如有）。

### 4.2 关卡数据与教学
- 核查 `Level01Footsteps` / `Level03Footsteps`：路径节点仅用于**可通行性与中心线**；不再需要"默认出口/死路自动返回"相关配置。
- 凡引用"松键仍巡行 / 自动寻路 / 提前排队"的**关卡内文案与教程步骤**按 README 修订更新。

### 4.3 第二关到达刻重算（**关键**）
移动模型改变 → 移动时间改变 → 既有公式的输入变化：
- `noLagArrivalTick + interactionBufferTicks <= durationTicks - successMarginTicks`
- `laggedArrivalTick > durationTicks`
- 第五关：`abs(arrivalTickA - arrivalTickB) > resonanceWindowTicks`、`abs((arrivalTickA + delayTicks) - arrivalTickB) <= resonanceWindowTicks`
必须用**固定步长模拟**重算并更新关卡参数（L02 数据尚无，先在卡内给出计算结果与目标参数）。

### 4.4 既有裁决的遗留项（本卡可一并清理）
| 项 | 裁决 | 动作 |
| --- | --- | --- |
| `mechanism/ray/PhaseManager` | C4 裁决 1 | **删除**（零引用死代码；相位归开发一 `entity/PlayerPhaseController`） |
| `AutoDockOccupancyPort.tryLeave` | R5 裁决 4 | 放宽 `NOT_OUTSIDE_REGION`：占用者同刻按下合法出口即可释放（开发一配合在离开刻调用） |
| P2/P4 快照端口 | R5 裁决 3 | 提供射线/共振/中继/核心的**纯状态快照与 restore**，解锁 R5-B |

## 五、测试

1. 关卡加载校验：节点 ID 唯一、1-tile 邻接待节点、机关引用合法；`defaultExit` 缺失**不再报错**；
2. 可通行性：给定节点与方向，`getValidExits` 结果与几何一致；
3. 到达刻：第二关两条到达刻条件由固定步长模拟断言（数值随参数更新）；
4. `tryLeave` 新语义：占用者同刻按合法出口 → 立即 `LEFT` 并释放（不要求位置出界）；
5. 删除 `PhaseManager` 后全量测试仍绿。

## 六、验收条件

1. `new LevelGeometryImpl(Level01Footsteps.build())` 仍可构造；关卡校验不因 `defaultExit` 缺失失败。
2. 全量 `mvn test` 退出码 0；无残留引用旧自动选路的关卡配置。
3. 第二关到达刻公式用**模拟值**给出并更新参数文档。
4. `mechanism/ray/PhaseManager` 已删除且无引用。
5. 教程/文案不再出现"松键仍巡行/自动寻路"。

## 七、交叉依赖与顺序

- **4.1/4.2**（几何与文案）可**立即开始**，不依赖开发一。
- **4.3**（到达刻）需开发一 `C-PLAYER-MOVE-01` 的移动参数（`baseSpeed`/转向规则）稳定后再定稿。
- **4.4 `tryLeave`** 需与开发一联调：开发三放宽端口 → 开发一在 `C3DockController` 于离开刻调用。
- **4.4 快照端口**接口签名需 PM 批准（R5 裁决 2 的 typed restore port 相关）。

## 八、跨模块停止条件

- 需改 `core/**`（`MovementState.IDLE`、`Direction`）→ 停止，报开发一。
- 需改 `entity/**`（移动/`C3DockController`）→ 停止，报开发一。
- 需改 `replay/**`（事件/帧契约）→ 停止，报开发二。
- 需改 `app/**`（装配）→ 停止，报 PM 开集成卡。

## 九、验证命令

```powershell
.\mvnw.cmd test
.\mvnw.cmd "-Dtest=LevelDataValidatorTest,AutoDockServiceTest,StableMechanismIdTest" test
```

---

*本卡只动 `mechanism/**`、`level/**`、`ui/**`、levels 资源与开发三文档；发现需改其他板块 → 停止上报。*
