# 任务卡 · 自由移动收口 v2（开发三）

> 任务 ID：**X-MOVE-COLLAPSE-01-DEV3-v2**（替代早前 `自由移动收口-任务卡-开发3.md`；其 L-1/ENT-2a 已交付并配对验证）
> 板块：**开发三**（`mechanism/**`、`level/**`、`ui/**`、`persistence/**` + 测试）
> 状态：**已发卡，可开工**
> 基线：`origin/develop` 现 `202e7ad`；你的分支：`feature/content-x-move-collapse-01-dev3`（`7bed288`）、
> `-dev3-l2`（`52af111`）、`-dev3-l3`（`cf4714f`）、`-dev3-l4a`（`9a51132`）
> 集成状态：ENT-2a 已被 PM cherry-pick 进 **PR #55**（`cd776b9`），配对 ENT-2b 后 **289/0/0**
> 日期：2026-09-10

## 一、当前已完成（复核结论，勿重做）

| 子项 | 提交 | PM 复核 |
| --- | --- | --- |
| L-1 / ENT-2a `tryLeave` 同刻释放 | `86553c9` | ✅ 已进 PR #55；`NOT_OUTSIDE_REGION` 保留 `@Deprecated`，签名未变 |
| L-3 出口宽容半径（方案 A，1.5 格） | `cf4714f` | ✅ 分支干净（`ExitTerminal` +48、测试 +89）；**待合入**，合入后 PM 接 APP-2 |
| L-2 第一关两轮通关模拟 | `52af111` | ✅ 纯 `level/**` 测试（+479），**待合入** |
| L-4a 第一关不再声明默认出口 | `9a51132` | ✅ 分支干净（`Level01Footsteps` -3/+1），**待合入** |

## 二、本卡任务

### A. 三支各自开 PR（最高优先，互不合并）

1. `-dev3-l3` → `develop`（标题 `X-MOVE-COLLAPSE-01 L-3 出口宽容交互半径`）；
2. `-dev3-l2` → `develop`（标题 `... L-2 第一关两轮自由移动通关模拟`）；
3. `-dev3-l4a` → `develop`（标题 `... L-4a 第一关不再声明默认出口`）。
   每个 PR 附 `Tests run / Failures / Errors` 与最慢单测耗时；**不要**把三支并成一个大 PR（粒度裁决 §9.3）。
4. 说明：L-3 合入后 PM 会接 `X-MOVE-COLLAPSE-01-APP-2`（app 侧 `isInInteractRange` 判定）；
   L-4a 与 L-2 不阻塞任何人。

### B. E-2：残影 actor 归属的机制侧确认与补测（配合 `X-MOVE-COLLAPSE-01-ECHO-ACTOR`）

背景（PM 实测，真 bug）：`DockingPlate.onEvent(ECHO_DISAPPEARED)` 只在 `occupantId.equals("echo_" + sourceRound)`
时才释放；而回放侧现在传录制时的 `"player"` → **残影消失后驻留板永久占用**。

要求：

1. 在机制规格文档（`docs/development/开发3/开发三-稳定ID与排序-autoDock规格冻结.md`）确认并写明：
   `echo_<sourceRound>`（1 起算）是**冻结约定**，`AutoDockService.requireActor` 与
   `DockingPlate.onEvent(ECHO_DISAPPEARED)` 都依赖它；
2. 补机制侧测试：`tryEnter("echo_1", 1, t)` → 派发 `ECHO_DISAPPEARED(sourceRound = 1)` → **板被释放**；
   再补一条「`ECHO_DISAPPEARED.sourceRound` 与非 `echo_<该轮>` 占用者不匹配时不得误释放」；
3. 明确残影占板不参与 `Door` 的计数口径（若参与，说明与玩家占板是否等价）。

### C. `mechanism/ray/PhaseManager` 删除（C4 裁决 1，逾期项）

**证据**：`git grep -l "class PhaseManager" origin/develop -- src/main` 仍然命中
`mechanism/ray/PhaseManager.java` —— 你的 P-MOVE-ALIGN 卡 §4.4 列为「删除（零引用死代码）」但尚未落地，
且没有任何已推送分支删了它（相位归开发一 `entity`）。

要求：确认零引用后删除，并保证全量绿；如发现仍有引用，先出结论给 PM，不要顺手改 `entity/**`。

### D. L-4b：`defaultExit` 数据侧清理（**必须等 PR #54 合并后**）

顺序硬约束：`CORE-3`（core 删字段）→ `APP-1`（`app/PathGraphBridge` 去转换）→ **L-4b**
（`level.model.PathNode.defaultExit` + `LevelGeometry.getDefaultExit` 清理）。
PR #54 未合并前开工会让 `PathGraphBridge` 编译断。PM 会在合并后主动通知。

### E. L-5（仅登记，不执行）

第二关 / 第五关到达刻重算：等 `SpeedModifierPort` 的**射线写入端**（`mechanism/ray` 尚未实现）与 ENT-2 稳定后，
由 PM 单独发卡。禁止用估算公式回填（README §九）。

## 三、允许 / 禁止

- 允许：`mechanism/**`、`level/**`、`ui/**`、`persistence/**`、`audio/**`、对应测试、`docs/development/开发3/**`。
- 禁止：`core/**`、`entity/**`、`app/**`、`render/**`、`replay/**`、`snapshot/**`、`pom.xml`、`src/main/resources/**`。
- 公共签名/语义变更（如 B 的 `ECHO_DISAPPEARED` 校验）先出提案，PM + 使用方（开发一/开发二）确认。

## 四、依赖与顺序

1. A（三支 PR）可立即做；
2. B 可立即做；
3. C 可立即做（与本轮其他项无耦合）；
4. **D 等 PR #54 合并**；
5. E 只登记。

## 五、交付

三个 PR 链接 + 各自提交哈希与测试数字；B 的规格文档改动与测试名；C 的删除提交；
写进 `docs/development/开发3/X-MOVE-COLLAPSE-01-v2-交付.md`。
