# 任务卡 · 基线慢测试收口（开发三）· v2

> 任务 ID：**TECH-DEBT-SNAPSHOT-RUNTIME-v2**
> 主责：**开发三**（`src/test/java/org/example/timeloop/level/**` **本来就是你的路径**）
> 依据：`L02-门房与双残影-PM裁决.md` **§十二.2**（0B 裁决）
> 基线：`origin/develop` = **`ac8a490`**（PM 复跑 412 / 0 / 0）
> 状态：**开工**（低优先，可与 L2-A 并行；不阻塞 L2-A 合入）
> 日期：2026-09-14

## 一、为什么会有这张卡（与旧卡的区别）

- 旧卡 `TECH-DEBT-SNAPSHOT-RUNTIME-任务卡-开发一.md` 已**撤回**：当时 PM 把「与 JavaFX 实跑并行」测出的
  `1.247 s` 误判成回归，且**归属给错了人**（该用例在 `level/**`，不是开发一的路径）。
- 本卡是**换了归属与口径**后的重发依据：同一用例三方实测分别为 **PM 0.865–0.998 s**、**开发三 0.877 s**、
  **开发一 2.956–3.027 s** —— 跨机/跨状态差 **3 倍以上**，说明绝对阈值不可执行，需要一个确定的收口动作。

## 二、交付物（二选一，PM 接受任一种，但必须有可复核证据）

### 路线 1：把它压到同机中位数 ≤ 1 s（首选）

`org.example.timeloop.level.Level01MapRenderSnapshotTest`（28×16 全图快照）。允许手段：

- 减少重复渲染：同一刻**只渲染一次**，多处断言共用同一张图；不要为每个断言重跑 `renderFrame`；
- 降采样渲染尺寸：用 `WorldTransform.fit(worldW, worldH, 较小视口, …)` 渲染到较小画布（如 960×540），
  **不**按 1:1 世界像素（1344×768）渲染；
- 若原先逐刻快照：改为**关键刻 + 结构断言**，并在测试注释里写明「为什么抽样仍能发现回归」。

**不允许**：把像素/结构断言降级为「渲染不抛异常」；删断言换速度一律打回。

### 路线 2：提交成本拆解（若确认无法在不降覆盖的前提下压缩）

给出实测拆解（渲染尺寸、层数、每层耗时占比、渲染次数），并说明「超时防线如何仍然有效」
（例如该用例单独设断言级超时，或标记为慢测并在 CI 门禁里单列）。

## 三、测量口径（**必须照此报数**，出自裁决 §十二.2）

1. 测量时**不得有 JavaFX 实跑、构建或其它重负载并行**；
2. 报 **≥ 3 次复跑**的数值与**中位数**；
3. 只有**中位数连续 3 次 > 1 s** 才算回归，单次抖动不算。

## 四、允许 / 禁止路径

**允许**：`src/test/java/org/example/timeloop/level/**`；如确有必要可动 `level/**` 生产代码的**只读投影/快照入口**
（不得改变关卡几何、判定链、参数语义）。

**禁止**：`app/**`、`render/**`、`replay/**`、`snapshot/**`、`mechanism/**`、`ui/**`、`pom.xml`、`README.md`。
**注意**：L2-A 正在改 `level/Level02Corridor.java`（新文件）—— 本卡只碰 L1 的快照测试与其直接依赖，避免与自己的 L2-A 分支抢同一文件。

## 五、验收

1. 同机 **3 次复跑中位数 ≤ 1 s**，或提交路线 2 的成本拆解；
2. `.\mvnw.cmd -o clean test` 退出码 0，数字**不回退**（当前基线 412），断言强度有说明（改前断言什么 → 改后断言什么）；
3. `git diff --check` 干净。

## 六、证据与回传

```
branch: codex/l1-snapshot-runtime-dev3
SHA:    <40 位>
机器/状态: <是否无并发实跑> ｜ 复跑: <s1/s2/s3> 中位数 <s>
改前: <基线数值与来源>   改后: <数值>
断言强度: <改前 → 改后，一句话>
clean test: ? / ? / ? / ?
```

**交付以远端提交为唯一凭据**；禁止 `git add -A`，精确白名单提交。

## 七、停止条件

1. 压缩必须动 `app/**` / `render/**` / `pom.xml` → 停手回报 PM；
2. 发现真正的性能缺陷在生产渲染层（例如某层每帧 O(格数²)）→ 停手回报，**单开性能卡**，
   不要在这张"门禁卡"里顺手做性能重构；
3. 无法在保持断言强度的前提下压缩 → 走路线 2，不要硬压。
