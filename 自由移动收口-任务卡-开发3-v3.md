# 任务卡 · 自由移动收口 v3（开发三）

> 任务 ID：**X-MOVE-COLLAPSE-01-DEV3-v3**（替代 v2 卡；A（三支 PR）与 ENT-2a 已完成并合并）
> 板块：**开发三**（`mechanism/**`、`level/**`、`ui/**`、`persistence/**` + 测试、`docs/development/开发3/**`）
> 状态：**已发卡，可开工**
> 当前基线：`origin/develop` = `6dc5cad`（313/0/0，c3-ctor 修复已随 **#56** 合入）
> PM 侧在途：**PR #57**（APP-2 出口宽容半径 + ECHO-ACTOR E-1；你 L-3 的 `cf4714f` 已被消费）
> 日期：2026-09-11

## 一、C · 删除 `mechanism/ray/PhaseManager`（逾期项，最优先）

**证据**：`git grep -l "class PhaseManager" origin/develop -- src/main` 仍命中
`mechanism/ray/PhaseManager.java`；你的 P-MOVE-ALIGN 卡 §4.4（C4 裁决 1）列为「删除（零引用死代码）」但未落地。

要求：确认零引用后删除，全量绿；**不要**顺手改 `entity/**`（相位实现归开发一）。

## 二、B · E-2 残影 actor 归属的机制侧确认与补测

背景（PM 实测，真 bug 已由 app 侧 E-1 修复）：`AutoDockService.requireActor` 强制 actor ∈ {`player`, `echo_<N>`}，
且 `DockingPlate.onEvent(ECHO_DISAPPEARED)` **只在占用者等于 `echo_<sourceRound>` 时**释放。

要求：

1. 在 `docs/development/开发3/开发三-稳定ID与排序-autoDock规格冻结.md` 确认并写明 `echo_<sourceRound>`（1 起算）
   是**冻结约定**，两处实现都依赖它；
2. 补机制侧测试：① `tryEnter("echo_1", 1, t)` → 派发 `ECHO_DISAPPEARED(sourceRound = 1)` → **板被释放**；
   ② `ECHO_DISAPPEARED.sourceRound` 与占用者 `echo_<N>` 不匹配时**不得误释放**；
3. 明确 **`Door` 的计数口径**：残影占板与玩家占板是否等价、`Door` 是否只认「两块板都被占」而不管占用者身份；
   若有差异，写进规格文档（第一关解法叙述依赖这个口径）。

## 三、D · L-4b 数据侧清理（PM 已批准开工，见父卡 §12.4）

前置已满足（CORE-3 + APP-1 随 #54 进 develop）。范围：

- `level.model.PathNode` 的 `defaultExit` 字段（含构造与校验）；
- `LevelGeometry.getDefaultExit` 与 `LevelGeometryImpl` 的 `defaultExitMap`；
- `Level01Footsteps` 已在 L-4a 去掉声明，无需再动。

**不要**改 `app/**`（`PathGraphBridge` 已不再使用默认出口）。完成后全量绿 + 数字回填交付文档。

## 四、E · L-5 保持登记态

第二关 / 第五关到达刻重算，等 `SpeedModifierPort` 的**射线写入端**（`mechanism/ray` 尚未实现）与 ENT-2 稳定后，
由 PM 单独发卡；**禁止用估算公式回填**（README §九）。

## 五、流程提醒

- 你不合并 PR、不推 `develop`/`main`；PR 由轮值 PM 合；
- 请求合并前先把最新 `develop` 同步进自己的分支并跑全量（PR 描述附基线哈希与数字）——
  `#51` 与 `#54` 合并后才暴露的 `PathNode` 构造断点就是这么来的（现已由 #56 修复）；
- 每支独立 PR：C、B、D **不要并成一个大 PR**。

## 六、允许 / 禁止

- 允许：`mechanism/**`、`level/**`、`ui/**`、`persistence/**`、`audio/**`、对应测试、`docs/development/开发3/**`。
- 禁止：`core/**`、`entity/**`、`app/**`、`render/**`、`replay/**`、`snapshot/**`、`pom.xml`、`src/main/resources/**`。
- 公共签名/语义变更（如 B 的 `ECHO_DISAPPEARED` 校验）先出提案，PM + 使用方确认。

## 七、交付

三支 PR 链接 + 各自哈希与数字；B 的规格文档改动与测试名；C 的删除提交；
写进 `docs/development/开发3/X-MOVE-COLLAPSE-01-v3-交付.md`。

---

## 八、L-4b 开工通知（PM 正式发出，2026-09-11）

**批准开工。** 基线：`origin/develop` 当前 **`b76c14c`**（PM 实测 **318/0/0**，墙钟 7.5s）。

### 8.1 已核（PM 复跑）

- C（删 `mechanism/ray/PhaseManager`）**已随 #59 合并**，文件已消失 ✅；
- B（E-2 机制侧）**已随 #58 合并** ✅；
- 你的前置复核成立：`entity/**` 无 `getDefaultExit`；`app/PathGraphBridge` 只剩一行说明注释。

### 8.2 影响面（PM 已扫全仓，照此删即可，无遗漏）

生产代码（仅 `level/**`）：

- `level/model/PathNode`：`defaultExit` 字段、四参构造器、`getDefaultExit()`、构造校验；
- `LevelGeometry.getDefaultExit(String)`（接口方法）；
- `LevelGeometryImpl`：`defaultExitMap` 字段、构造填充（L32/L35-36）、`validateNodes` 校验（L112-116）、`getDefaultExit`（L190-191）。

测试（必须同步）：

- `LevelGeometryImplTest:29` `assertNull(geometry.getDefaultExit("L01_node_fork"), ...)` → 随接口删除；
- `LevelGeometryImplTest:81` `assertNull(fork.getDefaultExit(), ...)` → 随模型删除；
- `Level01TwoRoundSimulationTest` 只在**注释**里提到 `defaultExit`（L61-62），无需改动。

其它 `new PathNode(...)` 调用点**无四参残留**：`Level01Footsteps`（3 参，L-4a 已改）、`LevelDataValidatorTest:121`（3 参）
→ 删除四参构造器是安全的。

### 8.3 验收

1. `git grep -rn "defaultExit\|DefaultExit" src/main/java/org/example/timeloop/level` → **空**；
2. `git grep -rn "getDefaultExit" src` → **空**（含测试）；
3. `.\mvnw.cmd -o test` 退出码 0、**任何单测 ≤ 1s**、数字回填交付文档；
4. 禁止路径未触碰：`core/**`、`entity/**`、`app/**`、`render/**`、`replay/**`、`snapshot/**`、`pom.xml`、`src/main/resources/**`。

### 8.4 流程

提交后 **push 分支**（授权推送），PR 由你或 PM 在 GitHub 创建；**合并由 PM 执行**；
请求合并前先把最新 `develop` 同步进分支并跑一次全量（附基线哈希与数字）。

