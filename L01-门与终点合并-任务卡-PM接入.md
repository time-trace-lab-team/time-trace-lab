# 任务卡 · 第一关「门与终点合并」· app 接入与文档（PM / 集成）

> 任务 ID：**L01-GATE-MERGE-PM**
> 主责：**PM**（`app/**`、`README.md`、`docs/**`）｜ 依据：`L01-门与终点合并-PM裁决.md`
> 基线：`origin/develop` = `3e0778d`（PM clean 复跑 381/0/0）
> 状态：**待开发三给出闸门节点后开工**（前置：`L01-GATE-MERGE-DEV3`）
> 日期：2026-09-13

## 一、交付物

1. **渲染列表（`Level01Assembly.renderViews()`）**：
   - 开关对应的 `RenderViews.Mechanism` 用 `MechanismKind.SWITCH`（依赖开发一的枚举）；
   - **移除**第一关的 `MechanismKind.DOOR` 条目（门不再有独立门体；闸门外观由 EXIT 的 `active` 表达）。
2. **`objectiveView()`**：字段不变；确认 `doorUnlocked` 语义为「闸门是否已解锁」，右板字段语义为「开关是否被激活」
   （必要时更新 javadoc，不动 `ObjectiveViewModel` 结构 —— 结构属 `ui/**`，开发三）。
3. **`isPassable()`**：保留门的阻挡（现在挡的是**闸门自己那一格**，可见合理），
   但必须确认闸门节点**不是**出生点 → 左板 / 出生点 → 开关的割点（依赖开发三的证据 + 一条 app 侧集成测试）。
4. **app 测试更新**：
   - `Level01AssemblyLevel01FlowTest`（端到端通关）改为新链路：第一轮玩家踩开关并占左板（开关注为锁存 ON）→
     第二轮残影压左板 + 玩家到闸门按 `E` → 结算；
   - 与几何坐标相关的测试（`Level01AssemblyTest`、`Level01AssemblyRoundLoopTest`、`Level01AssemblyMovementTest`）
     按新节点坐标更新；
   - 新增两条集成测试：①「闸门锁着时不可进入闸门节点，解锁后可通行」；②「**第一轮占着左板无法结算**」
     （防单轮通关 —— 闸门与左板距离 > 72 的直接验证）；
   - 新增一条：轮末 `onRoundEnd()` 后开关回到 OFF、闸门回到 `LOCKED`。
5. **README 同步**（与实现同批）：
   - §三：通关链路表述（两条件 → 闸门开启 → 按 `E` 结算）；
   - §六：第一关示例由「双驻留板门」改为「左驻留板 + 开关 → 同格闸门终点」；
   - §十六：相关验收条目同步（若与 §十三 的移动口径冲突，一并订正）；
   - §五 / §七 **不改**（无新机关类型）。
6. **端到端目视验收**（目标机）：
   - 只有**一个**闸门终点，开关就在旁边；门锁着走不进那一格；两条件满足即开门并按 `E` 结算；
   - **开关锁存**：踩上后离开仍保持开启，直到本轮结束；轮末/重开后回到未开启；
   - **第一轮无法单轮通关**（占着左板时闸门不在交互半径内）；
   - 三张窗口截图（默认 / 更宽 / 更高）存档。

## 二、允许 / 禁止路径

**允许**：`src/main/java/org/example/timeloop/app/**`、`src/test/java/org/example/timeloop/app/**`、
`README.md`、`docs/**`。

**禁止**：`render/**`、`level/**`、`mechanism/**`、`snapshot/**`、`replay/**`、`core/**`、`ui/**`、`pom.xml`。
发现问题转对应单板块卡，**不在别人的路径上顺手改**。

## 三、验收

1. 端到端测试：新链路两轮通关；闸门锁/开两态通行性测试通过；
2. `.\mvnw.cmd -o clean test` 退出码 0、**任何单测 ≤ 1 s**、数字回填（基线 381）；
3. 目视验收 6 条（§一.6）全部通过，并有截图；
4. README §三/§六/§十六 与实现一致；`git diff --check` 干净。

## 四、证据与回传

```
branch:  codex/l01-gate-merge-pm
SHA:     <40 位>
clean test: ? / ? / ? / ?
闸门节点: <col,row>；割点检查: <结论>
目视:    6 条逐条结论；截图文件名
README:  §三 / §六 / §十六 改动摘要
```

## 五、停止条件

1. 开发三的节点证据不满足 PM 裁决 §四 的 6 条约束 → 停手回报，不发接入批；
2. 端到端测试暴露「无解」（例如离开开关才能按 `E`）→ 停手回报，这是关卡侧问题，转开发三；
3. 单测 > 1 s 且无法解释 → 停手回报。
