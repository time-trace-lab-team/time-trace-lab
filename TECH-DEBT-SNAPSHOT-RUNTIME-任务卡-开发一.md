# 任务卡 · 快照测试耗时收口（开发一）

> 任务 ID：**TECH-DEBT-SNAPSHOT-RUNTIME**
> 主责：**开发一**（`render/**` 及其测试）｜ 配合：PM（门禁口径）
> 基线：`origin/develop` = **`1aa58ab`**（PM clean 复跑 **381 / 0 / 0**）
> 状态：**~~开工~~ → 已撤回（2026-09-14，见 §〇）**
> 日期：2026-09-13

## 〇、**本卡已撤回（2026-09-14，PM 更正）**

**撤回理由**：PM 复核发现原判据是**被污染的测量**。当时 PM 正并行运行 JavaFX 实跑（`mvnw javafx:run`），
把并发负载算进了单测耗时。在**无并发实跑**的干净环境下（`develop @ d7463f8`），同一测试连测 3 次：

```
run 1: 0.903 s   run 2: 0.865 s   run 3: 0.871 s      ← 均 < 1 s，不构成回归
（原误报：1.247 s，来自与 JavaFX 实跑并行的测量）
```

→ **不需要开发一为耗时做任何改动**，`Level01MapRenderSnapshotTest` 保持现状。

**保留的流程口径（写入测试纪律，全组遵守）**：

1. 单测耗时必须在「**无 JavaFX 实跑、无其它重负载并行**」的机器状态下测量，
   否则会把并行负载误判成性能回归（本次即为此例，PM 自查自纠）；
2. 报「单测 > 1 s 回归」必须附：命令、机器状态（是否有实跑/构建并行）、**至少 3 次复跑**数值；
3. 只有**稳定** ≥ 1 s 才算回归。

以下为原卡内容，**仅作记录**，不再要求开发一执行。

## 一、问题（PM 实测，可在你的机器复现）

`EXPAND-MAP-VIEW-01` 配对合入后，PM 在 `develop` 上跑 `.\mvnw.cmd -o clean test`，
单测耗时排行第一是：

```
Level01MapRenderSnapshotTest   1.247 s   ← 超过「任何单测 ≤ 1 s」的口径
第二名（app 侧）                0.147 s
```

1.247 s 本身不是死循环，但它**顶掉了门禁**：一旦以后再慢一倍，就无法区分「快照渲染成本」与「真的挂死」。
本卡目标：**在不降低断言语义的前提下把它压回 ≤ 1 s**，或给出**可复核的成本论证**并由 PM 决定是否豁免。

## 二、交付物

1. **优化**（优先）——任选或组合，只要断言强度不降：
   - 用 `WorldTransform.fit(worldW, worldH, 较小视口W, 较小视口H)` 把快照渲染到较小画布（例如 960×540），
     **不**按 1:1 世界像素（1344×768）渲染；
   - 减少重复渲染次数：同一刻不要为多个断言重复 `renderFrame`；能一次渲染多处读取就一次；
   - 只在必要刻抽样（若原先逐刻快照，改为关键刻 + 结构断言），并在测试注释里写明**为何抽样仍能发现回归**。
2. **给出改前/改后数字**：同一命令、同一机器、`-o clean test` 下的该测试耗时（各测 3 次取中位数更好）。
3. **附前 5 慢测试耗时表**（证明没有把瓶颈挪到别处）。
4. 若你判断**无法在不降低覆盖的前提下压缩**，不要硬压：改为提交**成本论证**
   （渲染尺寸、层数、次数、每层耗时占比的实测拆解）+「超时防线如何仍然有效」的方案（例如单独给该测试
   设置断言级超时/专门的慢测标签），由 PM 裁决豁免或拆分。

## 三、边界

- **允许**：`src/test/java/org/example/timeloop/render/**`；如确有必要，可调整 `render/**` 生产代码里的
  **纯绘制性能**（不得改变投影语义、不得改 `WorldTransform` 的公开契约）。
- **禁止**：`app/**`（PM）、`core/**`、`replay/**`、`mechanism/**`、`level/**`、`pom.xml`、`README.md`。
- **不得**为降耗时删掉真实断言（例如把「像素比对」降级为「渲染不抛异常」）；这类改动一律打回。

## 四、验收

1. `.\mvnw.cmd -o clean test` 退出码 0，全量数字**不得下降**（当前 381；若因抽样减少用例请逐条说明）；
2. 该测试耗时 **≤ 1 s**（或提交成本论证 + 豁免申请）；
3. 断言强度有明确说明：改前断言什么、改后断言什么、为什么等价或有意识地取舍；
4. `git diff --check` 干净。

## 五、证据与回传格式

```
branch: codex/snapshot-runtime-dev1
SHA:    <40 位>
改前: Level01MapRenderSnapshotTest 1.247 s（PM 基线 1aa58ab）
改后: <秒> ×3 次
前 5 慢: <名称 秒> / <…>
clean test: Tests run / Failures / Errors / Skipped = ? / ? / ? / ?
断言强度: <改前 → 改后，一句话>
```

**交付以远端提交为唯一凭据**；禁止 `git add -A`，一律精确白名单提交。

## 六、停止条件

1. 压缩必须动 `app/**` 或 `pom.xml` → 停手回报 PM；
2. 压缩会让断言强于/弱于现状的取舍无法自证 → 停手回报，改交成本论证；
3. 发现真正的性能缺陷在生产代码（例如某层每帧 O(格数²)）→ 停手回报，单开性能卡，避免与门禁卡混做。
