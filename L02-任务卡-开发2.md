# 任务卡 · 第二关「双残影首次真跑」（开发二）

> 任务 ID：**L02-A-DEV2**（+ L2-B 的快照核对项）
> 主责：**开发二**（`replay/**`、`snapshot/**` + 其测试）｜ 依据：`L02-门房与双残影-PM裁决.md`
> 基线：`origin/develop` = **`f8f414c`**（clean 复跑 410 / 0 / 0）
> 状态：**开工**
> 日期：2026-09-14

## 一、本卡的真正风险：**L=2 从未真正跑过**

`EchoQueue` 已经有 `CAPACITY = 2`、寿命淘汰、`activeEchoes(round)`，但**至今只有一个关卡（L1，L=1）在跑**——
也就是说「**两条残影在同一轮里同时回放、同刻各自产生机关事件**」这条路径**从未被真实执行过**。
L2 是它的第一次实战：R3 需要 `E1` 与 `E2` 同时压住**不同**的两块板。

## 二、交付物

1. **双残影同刻回放的正确性**（`replay/**`）：
   - 同一 `roundTick` 上两个残影各自写入的机关事件必须**都生效且互不覆盖**（同一块板被两个来源争抢时，
     按现有 actor/`sourceRound` 规则确定胜者，并把规则写进 javadoc + 测试）；
   - 两条轨迹的**位置快照回放**互不干扰（`frameAt` 各自独立，不得共享缓冲）；
   - 事件顺序：同一刻多个残影事件必须有**确定性顺序**（不得依赖 `HashMap` 遍历顺序）；
   - 测试：`twoEchoesReplayTheSameTickIntoDifferentPlatesDeterministically`（同脚本重复跑 ≥ 3 次结果一致）、
     `twoEchoesCompetingForOnePlateHaveDeterministicWinner`、`echoTrailsAreIndependentPerSourceRound`。
2. **寿命与淘汰（L=2, maxRounds=4）**：
   - R3 时活跃残影 = `{E1, E2}`；R4 时 `E1` 已消散（寿命淘汰），**必须派发 `ECHO_DISAPPEARED`**（沿用既有契约与边界刻）；
   - 淘汰后：该残影占用的机关必须**释放**（L1 已修的 `ECHO-ACTOR` 闭环要覆盖 L2 的多板场景）；
   - **锁存开关语义不得被淘汰路径清掉**（L2 的门外板**不锁存**，但 L1 的开关锁存必须仍然只由 `reset()` 清）；
   - 测试：`firstEchoExpiresAtRoundFourAndReleasesItsPlates`、`echoEvictionDoesNotClearLatchState`。
3. **快照/恢复覆盖 L2 形状**（`snapshot/**`）：
   - 聚合器必须覆盖 **3 块板 + 2 个门 + 1 个出口**（多门是第一次出现）；
   - 门与门之间相互独立（一个解锁不影响另一个）；恢复到「房门解锁 + 闸门未解锁」的中间态后，行为与恢复前一致；
   - **锁存字段**（R5-B §七 冻结增补）在 L2 里没有使用者（门外板不锁存），但恢复路径必须**不破坏**它
     （L1 仍然依赖）；测试：`restoringLevelTwoMidStateKeepsRoomsDoorUnlockedAndGateLocked`。
4. **与 `R5-B-聚合器实现-任务卡-开发二.md` 的关系**：本卡是该卡在 L2 形状上的**前置验证**；
   聚合器全量实现仍按那张卡做，字段以 `docs/decisions/R5-B-聚合接口冻结-PM裁决.md` §七 为准。

## 三、允许 / 禁止路径

**允许**：`src/main/java/org/example/timeloop/{replay,snapshot}/**`、`src/test/java/org/example/timeloop/{replay,snapshot}/**`、
`docs/development/开发2/**`。

**禁止**：`app/**`（PM）、`render/**`、`mechanism/**`（开发三）、`level/**`、`core/**`、`pom.xml`、`README.md`；
**不得**扩容 `EchoQueue.CAPACITY`（仍为 2）；**不得**改 R5-B 冻结契约（要改先提提案）。

## 四、验收

1. 上述测试全部通过；**同刻双残影**用例重复 3 次结果一致；
2. `.\mvnw.cmd -o clean test` 退出码 0、**任何单测稳定 ≤ 1 s**、数字回填（基线 410）；
3. `git diff --check` 干净。

## 五、证据与回传

```
branch: codex/l02-a-dev2
SHA:    <40 位>
同刻双残影: <用例名 + 3 次一致结论>
淘汰时刻: E1 在 R4 消散的边界刻 = <tick>；释放的板 = <…>
多门快照: <用例名>
clean test: ? / ? / ? / ? ；最慢单测 <名称> <秒>
```

**交付以远端提交为唯一凭据**；禁止 `git add -A`，精确白名单提交。

## 六、停止条件

1. 同刻双残影出现**非确定性**（顺序不稳定）→ 停手回报 PM：这是设计级问题，不要用"测试里排序"掩盖；
2. 发现必须在 `mechanism/**` 侧加"谁先谁后"的状态才能确定胜者 → 停手回报，转开发三；
3. 需要改 R5-B 冻结契约 → 停手回报（走新提案）；
4. 单测稳定 > 1 s 且无法解释 → 停手回报。
