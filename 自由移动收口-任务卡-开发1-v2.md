# 任务卡 · 自由移动收口 v2（开发一）

> 任务 ID：**X-MOVE-COLLAPSE-01-DEV1-v2**（替代早前 `自由移动收口-任务卡-开发1.md`；其 CORE-1/2/3、ENT-1/3、ENT-2b 均已交付并验证）
> 板块：**开发一**（`core/**`、`entity/**`、`render/**` + 测试）
> 状态：**已发卡，可开工**
> 基线：`origin/develop` 现 `202e7ad`；你的交付在 `origin/codex/dev1-next-module`（`582594f`）与 `codex/ent2b-paired`（`d440354`）
> 集成状态：PM 已开 **PR #54**（CORE-3+APP-1，288/0/0）与 **PR #55**（ENT-2a+2b，289/0/0），均 clean
> 日期：2026-09-10

## 一、当前已完成（复核结论，勿重做）

| 子项 | 提交 | PM 复核 |
| --- | --- | --- |
| CORE-1 方向输入投影 | `c3b8464` | ✅ `case DIR_UP` 只剩 `LogicalKey` 一处；`heldDirections()` 不可变 |
| CORE-2 速度冻结 + 减速端口 | `f92cec9` | ⚠️ **实现正确但零测试**（见 §二 A） |
| ENT-1 单槽意图归位 | `ac9637f` | ✅ app 侧 `pendingTurn` 已删除，行为等价 |
| ENT-3 轮初复位 | `b171b0c` | ✅ PM 已在 `start()`/`onRoundEnd()` 各调用一次并跑通 |
| CORE-3 自动选路清零 | `82fe73d` | ✅ `core|entity|app` 无 `defaultExit` 代码引用 |
| ENT-2b 离开刻释放 | `d440354` | ✅ 配对后 289/0/0，场景 A–D 全绿 |

## 二、本卡任务

### A. CORE-2 补测（缺口，最高优先）

**证据**：`git grep -n "SpeedModifierPort\|speedMultiplier" <你的分支> -- src/test` **为空** —— 端口没有任何测试，
父卡 §一 CORE-2 的验收（「乘子 0.5 → 每刻位移 1.0 + `SLOWED`」）**未满足**。

要求新增（`entity/PatrolController*Test` 或新建 `PatrolControllerSpeedModifierTest`）：

1. 乘子 `0.5`：按住方向 → 每刻位移 = `baseSpeed × 0.5`；`movementState == SLOWED`；
2. 乘子恢复 `1.0` → 位移恢复 `baseSpeed`、`movementState == CRUISING`；
3. 段中间与节点中心两种位置都按乘子推进（节点中心提交转向后仍用当前乘子）；
4. `resetTo(...)` 之后仍按当前乘子推进（复位不清速度修正）；
5. 无输入 → `IDLE`（乘子不影响静止语义）。

交付方式：直接补进 `origin/codex/dev1-next-module`（PR #54 会带上），或另开小 PR；两者都行，**但不要 rebase 已推送的提交**。

### B. R-1 渲染：静止 / 减速必须可辨识（README §三「地图逻辑与视觉表现」）

**现状证据**：`src/main/java/org/example/timeloop/render/**` 内 `git grep "movementState|IDLE|SLOWED"` **零命中** ——
`PlayerLayer` 只按 `direction` 与 `phased` 绘制，`RenderViews.Player.movementState` 是**死字段**。
于是「按住走 / 松开停」在画面上**只有位移差别**，射线减速（`SLOWED`）更是完全看不出来。

要求（不改玩法坐标、纯只读渲染）：

1. `IDLE`（静止）与 `CRUISING` 有可辨识的姿态/形状差异（不靠颜色单独承载）；
2. `SLOWED` 有可辨识的减速反馈（例如拖尾/描边/步频），且不引入第二套计时器；
3. `RenderViews` 契约不变（若必须扩展字段，先出提案给 PM）；
4. 测试不依赖 JavaFX Toolkit：用纯投影/变换断言（同 `WorldTransformTest` 风格）。

### C. R-2 渲染：可转向位置提示（README §三 + §十六 门禁第 4 条）

**现状证据**：`render/**` 只有 `MechanismLayer` 画了「出口中心菱形」，**没有**路径节点/路口提示。

要求：

1. 路径节点中心绘制 6–10 像素菱形/短刻痕；玩家接近时提亮；
2. **不画全屏网格**（README 明确禁止棋盘格）；
3. 提示数据只能来自路径图/关卡几何，不得反推玩法状态；
4. 补一条纯函数测试（给定节点与玩家距离 → 是否绘制/亮度档位），避免只靠肉眼。

## 三、允许 / 禁止

- 允许：`core/**`、`entity/**`、`render/**`（**仅 §二 B/C 的只读渲染**）、对应测试、`docs/development/开发1/**`。
- 禁止：`app/**`、`level/**`、`mechanism/**`、`replay/**`、`snapshot/**`、`ui/**`、`pom.xml`、`src/main/resources/**`。
- 公共签名变更（`RenderViews` 字段、任何 core/entity 公开 API）先出提案，PM + 使用方确认后再动。

## 四、依赖与顺序

- **A 可立即做**，不依赖任何人；建议尽快并入 PR #54，避免“验收项缺失”带进 develop；
- **B/C 可立即做**，与 ECHO-ACTOR、APP-2、L-3 等无耦合；
- `mechanism/ray` 射线实现落地后，PM 会通知你做「`SpeedModifierPort` 端到端联调」（本卡只做 §二 A 的单元级覆盖）。

## 五、交付

分支 + 提交哈希 + `.\mvnw.cmd -o test` 的 `Tests run / Failures / Errors` + 渲染改动的**截图或像素级断言证据**，
写进 `docs/development/开发1/X-MOVE-COLLAPSE-01-v2-交付.md`。
