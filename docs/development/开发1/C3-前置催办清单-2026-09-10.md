# C3 前置催办清单（开发一 → PM / 开发二 / 开发三）

> 发起：开发一（成员 B）｜日期：2026-09-10
> 依据：`三岗前置任务卡-2026-09-10.md` 卡 #1 / 卡 #2 / 卡 #3；`docs/development/开发1/C3-前置核对与契约提案.md`
> 基线：`develop`（`bdf6e88`），`.\mvnw.cmd test` → 135 tests / 0 failures / BUILD SUCCESS

## 0. 一句话

开发一的 C3 已核对完毕：**几何与巡行前置全绿**（12 项前置里 1–6 项就绪），现在**只差 3 份输入**（开发三 2 份、开发二 1 份）与 **PM 的裁决+签卡**。下表逐条列出「我需要什么 / 什么格式 / 什么时候要 / 验收怎么判」；**截止时间为开发一按关键路径的建议**，请各位确认或还价。

---

## 1. 给开发三（最优先 · 卡 #3 第 2 条）

### 1.1 稳定 mechanism / actor ID 规则　【我 C3 的第一阻塞】

| 项 | 内容 |
| --- | --- |
| 我需要什么 | 全项目稳定的 **mechanism ID** 与 **actor ID** 生成规则（命名、唯一性、生命周期、跨轮次/跨重开是否恒等） |
| 格式 | 书面规格（Markdown）：规则正文 + **示例值表**（每关每个机关的实际 ID）+ **反例/禁止项**（如禁用对象地址、集合下标、本地化显示名） |
| 截止 | **2026-09-10 EOD**（建议，最优先） |
| 验收怎么判 | 我照规则能给出 `DockEvent.mechanismId`、`actorId` 的**具体字符串**，且同一关从第一轮重开后 ID 不变 |
| 为什么最优先 | 它**同时**是卡 #1（开发二的排序键）与 C3 的共同前置；它不落地，开发二也动不了 |

### 1.2 autoDock 只读查询规格　【我 C3 的第二阻塞】

| 项 | 内容 |
| --- | --- |
| 我需要什么 | `LevelGeometry`（或独立只读接口）上的 autoDock 查询能力 |
| 格式 | **接口签名 + 语义表**，逐项写清：<br>① 区域判定（形状：圆/圆角矩形；边界；单位＝世界逻辑坐标）<br>② **停驻中心** dock center<br>③ **合法出口**（离开方向集合）<br>④ 是否 `autoDock=true` 的标记<br>⑤ 占用/释放规则（谁能占、何时释放、**双 autoDock 重叠时的取舍**） |
| 截止 | **2026-09-10 EOD** |
| 验收怎么判 | 附 1 张**可直接构造的固定测试几何**（含 ≥1 个 autoDock 区域 + 停驻中心 + 合法出口），我能在**纯 JUnit**（无 JavaFX Toolkit）里断言进入/离开 |
| 边界说明 | 我**不改** `mechanism/**`；只要只读查询与规格，机制实现仍归你 |

### 1.3 卡 #3 第 ① 条收尾确认　【次要但影响我依赖判断】

| 项 | 内容 |
| --- | --- |
| 我需要什么 | 确认 `replay/EchoLifetimeManager`（第二套时钟 + 全局单例）已废弃，develop 上只剩开发二的 `RecordingSession` + `EchoQueue` |
| 格式 | 一句话确认 + 删除/迁移提交哈希；`.\mvnw.cmd test` 仍全绿 |
| 截止 | 越早越好（**建议 09-10 EOD**） |
| 为何要 | 我 C3 要接“共享逻辑刻”，若仓里存两套时钟来源，接错一处就是系统性缺陷 |

---

## 2. 给开发二（卡 #1）

### 2.1 `TimelineEvent` 正式类型 + 稳定排序键　【我 C3 的硬阻塞】

| 项 | 内容 |
| --- | --- |
| 我需要什么 | `replay/` 下正式的不可变 `TimelineEvent` record + 稳定 `Comparator` |
| 格式 | ① **字段清单**（名称 / 类型 / 单位 / 是否可空）；② **排序键定义**；③ 单测类名与用例清单 |
| 必须字段 | `tick`、稳定 **actor ID**、**sourceRound**、稳定 **mechanism ID**、事件类型、离开方向/原因 |
| 至少事件类型 | `DOCK_ENTERED` / `DOCK_LEFT` / `OCCUPANCY_RELEASED` |
| 排序规则 | 同 tick：先类别优先级，再稳定 ID（确定性，不依赖集合迭代顺序） |
| 截止 | **2026-09-11 12:00**（依赖开发三 §1.1 的 ID 规则；ID 规则一到即可动工） |
| 验收怎么判 | ① 排序确定性测试（同输入 → 同顺序，重复 N 次一致）；② 与我书面确认：事件在 **`PlayerKinematics` 之前采集、在 `advance()` 前记录** |

### 2.2 需要你回我的三个接口细节

1. 我提案的单刻顺序（输入边沿 → 队列/相位 → 推进+提交转向 → 解析 autoDock/终端 → **统一发布事件** → tick 末记录 `PlayerKinematics`）是否就是你要的顺序？
2. `DockEvent → TimelineEvent` 的转换**由谁做**：我发 `DockEvent` 你转，还是我直接发 `TimelineEvent`？
3. 排序键里 `actorId` 与 `sourceRound` 的优先级（我倾向 `(tick, categoryPriority, mechanismId, actorId, sourceRound)`，请确认）。

---

## 3. 给 PM（卡 #2）

### 3.1 裁决 A–H（逐条勾选即可）

| # | 待裁决 | 开发一建议默认 | 裁决 |
| --- | --- | --- | --- |
| A | `InputIntent` / `LogicalKey` 归属包 | 放 `core/input/`（纯 Java），开发一主责 | ☐ 采纳 ☐ 改 |
| B | JavaFX `KeyEvent → InputIntent` 适配位置 | 放 `app/`（PM 边界层），开发一给映射表 + 无头测试替身 | ☐ 采纳 ☐ 改 |
| C | 同 tick「进入 autoDock + 又按键」唯一规则 | 进入即清旧队列并快照 `held`；**同刻新 `pressed` 视为进入后边沿，允许同刻离开** | ☐ 采纳 ☐ 改 |
| D | 双 autoDock 区域重叠唯一规则 | 按稳定 `mechanismId` 升序取**第一个**，同刻不得再进第二个 | ☐ 采纳 ☐ 改 |
| E | 失焦漏 `release` 唯一规则 | 失焦时 `held` 全量记为 `releasedThisTick` 并清空 `held`；无新 `pressed` 不触发任何动作 | ☐ 采纳 ☐ 改 |
| F | C1 装配责任（循环+输入+控制器接线） | 由 PM/集成在 `app/` 装配，开发一只提供纯逻辑端口 | ☐ 采纳 ☐ 改 |
| G | 同刻多方向 | 按 `directionEdges` 顺序取**末位**胜出 | ☐ 采纳 ☐ 改 |
| H | `DockEvent.reason` 取值集合 | `NEW_DIRECTION / ECHO_EXPIRED / SESSION_RESTART` | ☐ 采纳 ☐ 改 |

**格式**：逐条书面裁决（采纳/修改/否决 + 一句话理由）。
**截止**：**2026-09-11 12:00**（可与卡 #1/#3 并行，不互相阻塞）。

### 3.2 签发 C3 单模块任务卡　【开工许可】

| 项 | 内容 |
| --- | --- |
| 我需要什么 | 签发的 C3 任务卡：允许/禁止路径、已批准接口版本、验收条件、验证命令 |
| 效果 | 我即可从 `develop` 建 `codex/dev1-c3` 开工 |
| 截止 | 裁决完成**立即**（建议 09-11 当天） |

### 3.3 请顺带确认 C2 的两个口子（避免开工后再拆卡）

1. `PatrolController.advance()` 现**硬编码**返回 `MovementState.CRUISING` —— C3 改为按 tick 返回 `DOCKED`/`SLOWED`，是否属 **C3 范围**？
2. `PlayerKinematics` 是否新增「当前交互目标 ID」字段，还是**走事件**（我建议走事件，保持帧记录不含 UI 语义）？

---

## 4. 汇总时间线（开发一建议，请各位确认或还价）

| 时间 | 交付 | 负责 |
| --- | --- | --- |
| **09-10 EOD** | 稳定 mechanism/actor ID 规则 + autoDock 只读查询规格 | 开发三 |
| **09-10 EOD** | 确认 `EchoLifetimeManager` 已废弃、135 测试仍绿 | 开发三 |
| **09-11 12:00** | `TimelineEvent` + `Comparator` + 单测 | 开发二 |
| **09-11 12:00** | 裁决 A–H | PM |
| 裁决后立即 | 签发 C3 任务卡 | PM |
| 齐备后当天 | 建 `codex/dev1-c3` 开工 | 开发一 |

---

## 5. 阻塞影响（供 PM 排优先级）

- 开发一 C3 每顺延 1 天，**C4/C5**（`resonanceAura` 相交判定、Canvas 渲染）顺延；开发三 **P4**（共振 + 第 4/5 关）依赖 C4/C5 → **三岗共同关键路径**。
- 因此 §4 的两条「09-10 EOD」不是开发一的私事，是整条链的最早一环。

---

## 6. 开发一这边已就绪（不需要你们等我）

- 几何 / 巡行 / 帧记录 / 枚举 前置**全绿**（135 测试）。
- `InputIntent` / `DockEvent` 字段提案已出（见 `C3-前置核对与契约提案.md`），**可直接采纳或直接改**，不必等我再改一版。
- 拿到 §1/§2/§3 的输入后，**当天可开工**。

---

*本清单为开发一单方面起草的催办与接口需求，不修改任何生产代码；截止时间为建议值，最终以 PM 排期为准。*
