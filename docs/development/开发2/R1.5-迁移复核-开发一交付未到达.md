# R1.5 迁移复核：开发 1 交付未到达本仓库，暂无法开工

- 时间：2026-09-09 16:24
- 复核人：开发 2（时间与状态系统）
- 触发：开发 1 报称「`core.GamePhase` 已交付 10 值 + `advancesLogic()`，全仓 40 个测试通过」

---

## 1. 结论

**本机工作区与远端所有分支上的 `core/GamePhase.java` 仍是 C0 的 4 值版本，没有 `advancesLogic()`。开发 1 的改动尚未到达本仓库，R1.5 无法开工。**

不是"我不敢改"，而是**物理上做不到**：`RoundClock` 的 `LEGAL` 转移表要用 `GamePhase.MENU / LEVEL_SELECT / TUTORIAL / RESETTING / RESULT / FAILED` 六个常量，它们在当前 `GamePhase` 里不存在，改完必然编译失败。而 `core/` 是任务卡明令禁止修改的路径，我也不能自己去补。

---

## 2. 核查证据

### 2.1 工作区文件（16:24 实读）

`src/main/java/org/example/timeloop/core/GamePhase.java` 全文：

```java
public enum GamePhase {
    BOOT,
    READY,
    PLAYING,
    PAUSED
}
```

- 文件修改时间：今天 14:58，未变动
- `core/` 目录下只有 `FixedStepClock.java` 和 `GamePhase.java` 两个文件
- 全 `src/` 检索 `advancesLogic`：只有 `FixedStepClockTest.java:153` 的一个**测试方法名**含该词，**无方法定义**

### 2.2 Git 侧（已联网 `git fetch origin` 拉取最新）

| 检查项 | 结果 |
| --- | --- |
| `git status` | 无任何已跟踪文件改动，只有 4 个未跟踪的文档 |
| `git stash list` | 空 |
| 本地分支 | `develop`、`feature/replay-r2-recording`（当前），均指向 `2462bef` |
| 远端分支（`git ls-remote`） | 仅 `develop`、`main`、`feature/content-p1` |
| **`feature/core-c1`** | **不存在（本地与远端都没有）** |
| `origin/develop` 的 `GamePhase.java` | 4 值 |
| `origin/main` 的 `GamePhase.java` | 该文件在 main 上不存在（main 为老分支） |
| `feature/content-p1`（`9f91dea`）的 `GamePhase.java` | 4 值 |

### 2.3 测试数量对不上（旁证）

- 开发 1 报：**全仓 40 个测试通过**
- 本机实测（`./mvnw.cmd -o test`）：**33 个测试通过** —— AppTest 1 + FixedStepClockTest 11 + RoundClockTest 12 + TickContextTest 9

差 7 条。说明开发 1 所处的仓库状态与本仓库不是同一个，进一步佐证其改动未同步过来。

---

## 3. 需要的动作（三选一）

| 方式 | 说明 |
| --- | --- |
| **A（推荐）** | 开发 1 把 `feature/core-c1` push 到远端，并把分支名或 commit SHA 告诉我，我 `git fetch` 后合并/切过去验证 |
| B | 若开发 1 与我在同一台机器、不同目录工作，请告知其仓库路径，我直接读取那份 `GamePhase.java` 核对 |
| C | 若开发 1 只是在文档/消息里"声明完成"但尚未落盘，请先让其真正提交代码 |

**我这边的前置校验脚本已就绪**，拿到后只需跑三条命令即可确认是否可开工：

```bash
# 1) 值数量与顺序
grep -c "" src/main/java/org/example/timeloop/core/GamePhase.java
# 2) 方法存在性
grep -n "public boolean advancesLogic()" src/main/java/org/example/timeloop/core/GamePhase.java
# 3) 依赖方向（必须无输出：core 不得 import replay）
grep -rn "import org.example.timeloop.replay" src/main/java/org/example/timeloop/core/
```

---

## 4. 迁移内容已就绪，解阻塞后可立即执行

| 文件 | 改动 |
| --- | --- |
| `replay/RoundClock.java` | 新增 `import org.example.timeloop.core.GamePhase;`；`RoundPhase` → `GamePhase`（字段、`transition` 入参、`phase()` 返回值、`LEGAL` 表、`apply()` 全量）；`phase.advances()` → `phase.advancesLogic()`；javadoc 中的 `{@link RoundPhase#X}` 引用同步改名 |
| `replay/RoundPhase.java` | **删除** |
| `replay/RoundClockTest.java` | 12 条用例的类型替换；**新增 1 条**遍历测试：断言 `GamePhase.values()` 中仅 `PLAYING` 的 `advancesLogic()` 为 `true` |

不变量（逐条保持）：`durationTicks` / `maxRounds` 仍 final；`LEGAL` 10 组边照搬不增删；`READY`/`PLAYING`/`RESETTING`/`FAILED`/`BOOT`·`MENU`·`LEVEL_SELECT`·`TUTORIAL`/`PAUSED`·`RESULT` 的副作用照搬；`advance()` 仅 PLAYING 推进、末刻返回 `ROUND_END` 不递增不切轮。

---

## 5. 关于开发 1 提的两个接口问题

**不依赖前置条件，已完整答复**，见 `C1-接口答复-Tick顺序与轮末结果.md`。要点：

- **A（Tick 顺序）**：方向正确但漏了三步，应以 C1 §3 已冻结的 7 步为准，其中「渲染快照在 `advance()` 之前生成」是硬约束。
- **B（轮末结果）**：`void` 确实致命，且**无法靠查 `phase()` 绕过**（ROUND_END 后 phase 仍是 PLAYING）。建议采用 `core.TickStepResult` 三值枚举，但**放置位置属 C1 §5.1 未裁决项，需 PM 拍板后开发 1 才能动**。
