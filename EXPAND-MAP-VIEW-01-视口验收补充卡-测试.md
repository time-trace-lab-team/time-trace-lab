# 任务卡（补充）· 视口自适应人工验收 + README 同步后回归（测试 / QA）

> 任务 ID：**EXPAND-MAP-VIEW-01-VIEWPORT-ACCEPT**
> 主责：**测试（QA）** ｜ 配合：PM（集成与截图登记）、开发一（render）、开发三（mechanism/level）
> 依据：`EXPAND-MAP-VIEW-01-PM-app对接卡.md` §五 + 开发一 `docs/development/开发1/MOVE-TURN-RULES-SYNC-开发一最终规则表.md`
> 基线：`origin/develop` = **`1aa58ab`**（PM clean 复跑 **381 / 0 / 0**，PR #72 配对 + PR #73 文档同步已合）
> 状态：**已发卡**（PM 已完成「投影与六层对齐」部分，本卡补齐「与玩法状态无关」和 README 口径回归）
> 日期：2026-09-13

## 一、背景：PM 已经做完的部分（不要重复劳动）

PM 于 2026-09-13 在本机实跑 JavaFX（非模拟），已完成：

| 档位 | 客户区 | PM 结论 |
| --- | --- | --- |
| 默认 | 1280×720（外框 1294×758） | 世界完整、居中、六层对齐 |
| C1 基准 | 958×568 | 世界完整、左右对称留边、格子为正方形 |
| 竖高 | 1398×892 | 上下对称留边、无拉伸、无裁切 |
| 扁宽 | 1598×592 | 左右对称留边、无拉伸、无裁切 |

**本卡要补的是 PM 无法自证的玩法侧不变量**：resize 是否**改变**了任何玩法状态、以及 README 改口径后的行为回归。

## 二、验收 A：resize 与玩法状态无关（核心）

在目标机上，用**同一段操作脚本**跑两次并对比：

1. 固定窗口 `1280×720` 跑到 `roundTick = 300`（中途不要 resize），记录：
   玩家世界坐标、朝向、`roundTick`、当前轮次、录制帧数、各机关占用/状态、残影位置；
2. 重开应用，同一脚本跑到同一逻辑刻，但**中途连续 resize ≥ 4 次**（含拉扁、拉高、缩到最小），再记录同样字段；
3. **逐项比对必须完全一致**（允许说明「如何读取这些值」的方法，例如 HUD + 测试探针）。

要求给出：两组数据表 + 结论。任何一项不同都按缺陷回填（责任板块：`render` 投影 / `app` 接线）。

## 三、验收 B：resize 中途的操作连续性

1. 连续 **≥ 10 次侧向转向**（含路口吸附），其中**至少 3 次发生在 resize 刚结束的同 1 秒内**；
2. **段中间掉头**（`BUG-001` 规则）≥ 2 次；**静止路口吸附** ≥ 2 次；
3. 全程观察：角色不跳位、不穿墙/穿门、不出现「投影与碰撞不一致」（视觉位置与阻挡不一致）；
4. resize 期间 HUD 不被裁切、不随投影缩放，`roundTick` 与轮次显示连续。

## 四、验收 C：README 改口径后的行为回归（**新增**）

README（PR #73 合入）已把以下规则写成正式口径，请逐条给出**游戏内可复现证据**：

1. 段中间**先按转弯键、再松开原方向**：角色沿原中心线到下一节点转向，**同 tick 不丢距离预算**；
2. 段中间**新按反方向**：同 tick 原路返回（主动掉头），不需要走到端点；
3. **真死路**按反方向可离开，且角色不会被锁死；
4. **同时按住相反方向且无新边沿**：完全静止（不随机选向）；
5. **第一轮首个方向输入前不计时、不写帧**：进入关卡后可长时间读图，读秒不动；第一次方向输入后开始读秒。
   （该规则来自 PR #65 门槛，PM 已写入 README §三，请确认与实现一致，并确认**第 2 / 3 轮**进入 `PLAYING` 即开始读秒。）

## 五、验收 D：完整通关一次 + 截图

1. 在 **resize 过的窗口**下完整通关第一关一次（两轮：第 1 轮占一块驻留板 → 第 2 轮残影占该板、玩家占另一块 → 门解锁 → `E` 结算）；
2. 保存**三张完整窗口截图**（含 HUD）：默认 `1280×720`、明显更宽、明显更高 → 登记到 `FINAL-ACCEPT` §A 第 4 条；
3. 记录本轮全量测试数字与慢测试表（基线 `1aa58ab` = 381，`Level01MapRenderSnapshotTest` 1.247 s 已由开发一另一张卡处理）。

## 六、交付格式

一份《EXPAND-MAP-VIEW-01 视口验收补充报告》：
结论（通过 / 不通过）→ A/B/C/D 四节逐条结论与证据（操作序列、数据表、截图文件名）→
失败项**责任板块 + 最小复现** → 是否放行「第二关开工」的建议。

## 七、边界

- 测试**不改任何 `src/main/**`**；发现问题写成回填条目（责任方 / 复现 / 期望 / 实际）；
- 跨模块问题交 PM 裁决，不在两个板块之间自行折中；
- 不修改 README（口径由 PM 维护）。
