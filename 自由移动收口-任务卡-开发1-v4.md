# 任务卡 · 视口适配 + 转向规则收口 v4（开发一）

> 任务 ID：**EXPAND-MAP-VIEW-01-RENDER** ＋ **MOVE-TURN-RULES-SYNC**（开发一 v4）
> 板块：**开发一**（`render/**`、`entity/**` + 测试、`docs/development/开发1/**`）
> 状态：**已发卡，可开工**
> 基线：`origin/develop` = `442b895`（PM clean 复跑 **347/0/0**；带 PM 侧 PR #66 时为 350/0/0）
> 日期：2026-09-12

## 一、A 部分（最高优先）：EXPAND-MAP-VIEW-01 的 render 侧

PM 已立项窗口/视口改造（`EXPAND-MAP-VIEW-01-窗口与视口-PM裁决.md`）。结论是：**窗口基准取 C1 §2 的
960×576 + 等比居中适配**，不做固定倍数的临时缩放。**这部分的 render 侧由你实现，PM 负责 app 接线，两者必须同批合并。**

### 1. 等比适配纯函数

```java
// render/WorldTransform
public static WorldTransform fit(double worldW, double worldH, double viewW, double viewH);
// scale = min(viewW / worldW, viewH / worldH)（等比，不允许拉伸）
// originX = (viewW - worldW * scale) / 2, originY = (viewH - worldH * scale) / 2（居中留边）
// 非有限或非正输入 → IllegalArgumentException
```

### 2. 图层必须每帧取当前变换（**这是 A2 的真实成本**）

现状：五个图层（`GroundWallLayer` / `MechanismLayer` / `PlayerLayer` / `EchoTrailLayer` / `PathNodeHintLayer`）
都在**构造时**把 `WorldTransform` 存成 final 字段 → resize 后重算的变换**传不进已有图层**。
请改成 `Supplier<WorldTransform>` 或引入 `WorldTransformHolder`（二选一并写进交付说明），全部图层一并迁移；
渲染仍只读、不得回写玩法状态。

### 3. 测试

1:1、极端宽高比（很扁/很高）、留边对称、`toWorld`/`toCanvas` 往返一致、非正尺寸防御。

## 二、B 部分：转向/掉头规则表 + 测试补强

`BUG-001` 修复（`d48aa5a`）**改变了移动规则**，而 README §三/§十六 仍是旧措辞（禁止掉头、段中间不转向、松键停在原地）。
请交付一张**「转向/掉头最终规则表」**（一页即可），逐条给出：规则文字 + 实现位置 + 对应测试名，至少覆盖：

| 现行实现（据你的修复反馈文档） | README 现状 |
| --- | --- |
| `newestEdge` 入单槽 `pendingDirection`；**反方向不再被过滤** | 写的是「不允许原地掉头」 |
| 段中间按住新方向：先判该方向在**当前段终点**是否合法；合法则继续沿中心线推进，到节点中心精确吸附后提交转向 | 写的是「段中间按垂直方向不会转向，当前方向仍按住时继续沿原方向前进」（未覆盖“松开当前方向、只按住新方向”的情形） |
| **段中间按反方向 → 交换有向段起终点，同 tick 沿原中心线原路返回** | 无此规则 |
| 静止辅助：上一 tick 静止且距节点 ≤ `0.15 × tileSize`（第一关 7.2）时，新按合法 90° 方向可吸附最近节点并同 tick 转向 | 无此规则 |
| 同时按住相反方向且无新边沿 → `IDLE`（不猜集合顺序） | 写的是「同刻按住相反方向 → 停止」✔ 一致 |
| 第一轮在首个方向输入前不写帧、不推进 `roundTick` | 无此规则（且与「进入 PLAYING 后从 roundTick=0 启动」冲突） |

**PM 已决定**：README 按现行实现同步（由 PM 改），因此你的规则表要能直接落到文档里。
另外请**补一条测试**：段中间**松开当前方向、只按住垂直方向** → 角色继续推进到下一节点并提交转向（若已有，请给测试名）。

## 三、边界提醒（流程）

`PR #65` 里你改了 `app/Level01Assembly.java`（12 行）。`app/**` 是 PM 路径 —— 本次**保留不追责**，
但后续凡涉及 `app/**` 的改动（含签名适配）请**停下来交接**，由 PM 落地；否则会出现“谁改了装配层”无人知晓的情况。

## 四、允许 / 禁止

- 允许：`render/**`、`entity/**`、对应测试、`docs/development/开发1/**`。
- 禁止：`app/**`、`core/path/**`、`mechanism/**`、`level/**`、`replay/**`、`snapshot/**`、`ui/**`、
  `pom.xml`、`src/main/resources/**`。

## 五、验收

1. `WorldTransform.fit(...)` 纯函数 + 5 个图层“每帧取变换”迁移完成，单测齐；
2. 与 PM 的 app 接线（窗口 960×576 + resize 重算 + 世界尺寸从 `levelData` 推导）**同批**合并后，
   `.\mvnw.cmd -o clean test` 退出码 0、**任何单测 ≤ 1s**、数字不回退；
3. 规则表交付，且与实现逐条对得上（PM 据此改 README）；
4. 补充测试（段中间松当前方向只按垂直方向）有名字有断言；
5. JavaFX 手工复测配合：连续完成侧向转弯与原路掉头，确认手感（BUG-001 的 ⏳ 项）+ 新窗口整体截图。
